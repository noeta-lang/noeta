//! `noeta` command-line interface — name-reservation placeholder.
//!
//! Published as `0.0.0` only to hold the `noeta-cli` crate name (and the
//! `noeta` command) while the language is under development. See
//! <https://noeta.dev>.

fn main() {
    println!("Noeta is not released yet. Follow development at https://noeta.dev");
}
