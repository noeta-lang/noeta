# Makes this case a package, so its modules derive `pkg.…` from where their files sit
# rather than declaring it. A case with no manifest is not a package and nothing derives.
[package]
name = "local/pkg"
version = "0.1.0"
