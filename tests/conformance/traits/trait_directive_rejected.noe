// The `@`-directives are type-only and do not apply to a trait (L1 user traits, UT6): `@derive` on
// a trait is a misplacement. E0054, like every other misplaced directive.
// expect: error E0054 at 6:7
// expect: exit 1
@derive(Clone)
trait Foo {
    fn a(): int
}
