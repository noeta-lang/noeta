// Pass 1 supports only empty-body capability impls; a standalone `impl` carrying methods would
// need runtime dispatch in both backends (a later slice), so it is rejected for now (E0015).
// expect: error E0015 at 6:1
// expect: exit 1
struct Route { path: string }
impl Clone for Route {
    fn extra(): int { return 1; }
}
