// (No `namespace`: this file sits at `first.noe` in package `pkg`, so it IS `pkg.first`.)

use pkg.types.{Decoder, Target};

impl Decoder for Target {
    fn step(s: string): string { return "first" }
}
