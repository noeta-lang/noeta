// The orphan: package `glue` declares neither `Speaks` (package `a`) nor `Thing` (package `b`), yet
// implements one for the other. Rejected by the package orphan rule (E0070) at the `impl`.
// (This file sits at `glue/impls.noe`, so its module path derives as `glue.impls`.)

use a.speaks.{Speaks};
use b.thing.{Thing};

impl Speaks for Thing {
    fn speak(): string { return "glue says ${self.id}" }
}
