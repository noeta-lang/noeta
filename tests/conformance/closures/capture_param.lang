// An arrow closure captures an enclosing function's parameter and outlives the frame.
// expect: stdout "15"
// expect: stdout "23"
// expect: exit 0
fn make_adder(n) {
    return fn(x) => x + n;
}
add5 = make_adder(5);
add20 = make_adder(20);
echo add5(10);
echo add20(3);
