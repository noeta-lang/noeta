// A named function parameter without a type annotation is rejected: under inferred-static
// typing, signatures are mandatory at named boundaries (only closures and locals are inferred).
// expect: error E0022 at 5:11
// expect: exit 1
fn double(n): int {
    return n * 2;
}
echo double(21);
