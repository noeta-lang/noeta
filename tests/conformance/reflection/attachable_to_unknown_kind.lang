// An unknown target kind in `@attribute(...)` is rejected (E0030) at the offending name — the
// kinds are a fixed, closed set.
// expect: error E0030 at 5:12
// expect: exit 1
@attribute(Bogus)
type Route = { method: string };
