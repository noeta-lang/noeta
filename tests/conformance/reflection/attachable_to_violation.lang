// `@attribute(Method)` forbids `Route` on anything but a method. Attaching it to a type
// declaration is a placement error (E0030) at the misplaced attribute.
// expect: error E0030 at 8:3
// expect: exit 1
@attribute(Method)
type Route = { method: string };

#[Route("GET")]
type User = { id: int };
