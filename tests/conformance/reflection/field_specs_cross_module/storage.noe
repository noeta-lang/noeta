// The declaring module. `Todo`'s runtime identity — and the key the reflection registry stores its
// field schema under — is the qualified `app.storage.Todo`.

pub struct Todo {
    id: int
    title: string
}
