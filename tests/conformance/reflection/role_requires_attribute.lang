// A role rides on what an attribute attaches to, so `@role(...)` may only tag a struct that is
// itself an attribute (`@attribute`). Tagging a plain struct is a checker error (E0031).
// expect: error E0031 at 6:8
// expect: exit 1
@role(Semantic.EntryPoint)
struct Route { path: string }
