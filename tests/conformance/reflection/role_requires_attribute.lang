// A role rides on what an attribute attaches to, so `@role(...)` may only tag a record that is
// itself an attribute (`@attribute`). Tagging a plain record is a checker error (E0031).
// expect: error E0031 at 6:6
// expect: exit 1
@role(EntryPoint)
type Route = { path: string };
