// Tier W1: a fixed-width integer literal outside its type's range is a static error (E0044), not a
// silent wraparound. `256` does not fit `u8` (valid range 0..=255). Wraparound arithmetic is a Tier
// W3 concern; a literal that cannot represent its own value is always rejected up front.
// expect: error E0044 at 6:6
// expect: exit 1
echo 256u8
