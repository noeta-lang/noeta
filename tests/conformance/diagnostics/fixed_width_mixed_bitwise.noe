// Tier W5: symmetric bitwise `& | ^` on fixed-width integers require both operands to be the same
// type — no implicit widening, just like arithmetic. `u8 & u16` is E0044, not a silent widen; an
// explicit conversion (`.to_u16()`) is the way across. (Shifts differ — their count may be any int.)
// expect: error E0044 at 6:6
// expect: exit 1
echo 1u8 & 1u16
