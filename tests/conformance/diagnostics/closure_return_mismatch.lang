// A closure's body must satisfy its declared return-type annotation — a mismatch is E0007, exactly
// as for a named `fn`. (The annotation drives bidirectional checking of the body.)
// expect: error E0007 at 5:27
// expect: exit 1
f = fn(v: int): string => v + 1
echo f(5)
