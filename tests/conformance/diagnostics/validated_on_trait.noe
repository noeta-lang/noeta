// `@validated` on a trait is a misplacement (E0053, the trait-directive code). Previously silent
// for the same reason as `validated_on_enum`: `TraitDecl` had no field to record it in.
// expect: error E0053 at 5:2
// expect: exit 1
@validated
trait Shape { fn area(): int }
echo "unreached"
