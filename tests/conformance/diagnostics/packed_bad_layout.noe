// `@packed(Layout.X)` rejects an unknown layout variant (E0037); the two variants are
// `Layout.Row` and `Layout.Column`.
// expect: error E0037 at 5:16
// expect: exit 1
@packed(Layout.Bogus) struct V { a: int }
echo "unreached"
