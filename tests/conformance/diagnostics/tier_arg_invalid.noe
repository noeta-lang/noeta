// A tier directive's arguments construct the tier's config attribute (`@bench(true)` ⇒
// `#[Bench(true)]`, binding `iterations: int` positionally), so a boolean argument is rejected by
// the ordinary attribute construction gate (E0007) — surfaced, not silently ignored. (A normal run
// validates the block in place; the block is then stripped, so the program's own output is
// unaffected.)
// expect: error E0007 at 8:8
// expect: exit 1
@bench(true) fn slow(): void { return }
echo "unreached"
