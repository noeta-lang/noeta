// P-PACK Phase 3: assignment between numeric types is strict (like `int`/`float` today — `let x:
// float = 1` is rejected). A `float` literal does not silently narrow to `f32`, nor does `f32` widen
// to `float` in assignment (only arithmetic widens). Construct an `f32` with the `f32` suffix instead.
// expect: error E0007 at 7:14
// expect: error E0007 at 8:16
// expect: exit 1
mut a: f32 = 1.0
mut b: float = 1.0f32
echo a
echo b
