// Track A.1 — the module top level is implicitly async when it contains a top-level `.await` (Track A
// decision): the `.await` is legal and runs its future to completion, no `block_on` bridge needed.
// expect: stdout "42"
// expect: exit 0
async fn work(): int {
    return 42
}
result = work().await
echo result
