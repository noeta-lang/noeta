# Makes this case a package, so its modules derive `di.…` from where their files sit
# rather than declaring it. A case with no manifest is not a package and nothing derives.
[package]
name = "local/di"
version = "0.1.0"
