// The entry `use`s a sibling function that divides by zero. The error originates in the merged-in
// `boom` declaration, so it must be reported at its real location in `Math.noe` (line 8), not
// at some bogus offset projected onto this short entry file. The expectation's line/column are
// Math.noe coordinates; a match proves the diagnostic resolved to the right source.
// expect: error E0008 at 8:12
// expect: exit 1

use App.Math.boom;

echo boom();
