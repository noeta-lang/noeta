// Importing a module-private (non-`pub`) declaration is a hard error (E0019). Once the
// namespace is found, resolution is strict: only `pub` declarations are importable.
// expect: error E0019 at 7:16
// expect: exit 1
// (No `namespace`: the entry of package `App` derives `App.main` from its own file name.)

use App.Models.Secret;

echo "unreachable";
