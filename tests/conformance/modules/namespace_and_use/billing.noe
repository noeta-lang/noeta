// The `App.Billing` library module the entry imports with the grouped `use` form — two names from
// one module, which is the form this case exists to pin.
namespace App.Billing;

pub struct Invoice {
    pub number: int
    pub paid: bool
}

pub struct Receipt {
    pub number: int
}
