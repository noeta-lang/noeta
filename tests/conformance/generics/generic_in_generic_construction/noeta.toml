# Makes this case a package, so its modules derive `live.…` from where their files sit rather
# than declaring it — which is what makes the RECORDED tag's qualified identity observable. A case
# with no manifest is not a package and nothing derives.
[package]
name = "local/live"
version = "0.1.0"
