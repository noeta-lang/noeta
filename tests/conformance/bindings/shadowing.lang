// Lexical shadowing: a function parameter shadows a same-named binding in an outer scope
// for the duration of the call; the outer binding is untouched.
// expect: stdout "5"
// expect: stdout "100"
// expect: exit 0
base = 100;
fn f(base) { return base; }
echo f(5);
echo base;
