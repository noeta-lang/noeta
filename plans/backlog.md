# Backlog — everything open, in one place

The single registry of work we have said we will do and have not yet done. It merges the old
`deferred.md` (slice deferrals), the old design-proposal backlog, and the open tails of every
completed arc directory (deleted from `plans/` once shipped — their full slice histories and
design sketches live in git history).

**Discipline:** when an arc defers something non-gate, add a row here in the same commit. When a
slice or arc picks an item up, strike the row and point to the work that closed it. Each row names
its **source** and, where useful, a concrete **trigger** — the condition that should make us do it,
so we neither do it prematurely nor forget it. Items marked *(active)* are worth picking up on their
own; everything else is trigger-gated.

Nothing here is a correctness gap in shipped behavior unless explicitly marked **bug**.

## Language & type system

| Item | Source / trigger |
|---|---|
| **Two positions read a fixed-width integer's erased word, by definition** — a `dyn` local inside a generic body, and a generic function taken as a value with nothing to pin its instantiation. Neither has an instantiation to carry, so this is the shape of the position rather than a gap in the render-hint mechanism; `docs/Fixed-Width-Integers.md` states both. Recorded so a future reader does not re-derive it as a defect | render-hint arc (2026-08-19). Trigger: a mechanism that gives a `dyn` position a static type, which would be a different feature |
| ~~**bug** — `?` on a `Result` inside a `void` function silently discards the error~~ **SHIPPED (2026-07)** — the deliberate call was **both**: reject it where a return type exists, abort where none does. A `?` on a `Result` in a function whose declared return is neither a `Result` nor deferring is now **E0012** — the same code, span, and shape as the `Option` half (`check_try_error` no longer bails on a non-`Result` return; its help names the fix). Where there is no declared return to check — top-level code, a `dyn`/inferred return — an `Err` that reaches the top **aborts** with the error's `message()` and a non-zero exit, the new **E0069** `UnhandledError`, raised by both backends through one shared builder in the catalog. Measured after: the `void` repro is E0012 at `noeta check` (exit 1, was 0 errors/exit 0); the top-level repro prints `before` then aborts E0069 with the transport message at exit 1 (was silent at exit 0). Blast radius was **7** corpus cases, not the 6 predicted — `std/http_options.noe` also carried an `async fn run(): void { …? }` — plus the `docs/Concurrency.md` sample; all rewritten to declare the `Result` their `?` needs. Conformance: `results/try_result_return_type`, `results/try_unhandled_at_top_level`, `results/try_result_deferred_return`. Docs: `docs/Error-Handling.md` (both halves as one rule) | stream-status session (2026-07) |
| ~~**bug** — a `u64` above `i64::MAX` displays as its signed reinterpretation~~ **FIXED (2026-08)** — `echo 18446744073709551615u64` printed `-1`, and so did `"${x}"`, `x ~ y`, and every element/field/payload of a rendered collection or object. Representation was never wrong (`>`, `==`, `/`, `%` were all correct); the *display* path read the erased i64 word as signed because the signedness lives only in the static type. The checker now records a structural `noeta_ast::RenderHint` at each display site (`echo`, an interpolation hole, a display-based `~` operand), lowering wraps the atom in `Rvalue::Render`, and both backends walk their own value model against that one hint — so a `u64` renders unsigned at any depth (list, set, map key or value, tuple, object field, enum payload). A declared type with its own `Display` is exempt at top level and keeps dispatching `to_string`. `Op::Stringify` carries the hint, so `FORMAT_VERSION` moved 21 → 22. It survived because the corpus echoed only *derived* large `u64`s that happen to fit (`u64::MAX / 2`) and never a large `u64` itself; `types/unsigned_display.noe` now pins all three boundaries through every door, with the narrower widths as controls. **The fourth display door closed 2026-08-17:** a REPL / debug-console entry's trailing bare expression is rendered by the HOST, not by an `echo` in the program, so typing `x` printed `-1` where `echo x` printed the value. The session checker names that expression (`noeta_ast::desugar::trailing_expr_span`, read by both the checker and the sessions so they cannot disagree about which expression is echoed), the `Stmt::Expr` arm records its hint off the type it already computed, and `VmSession::eval_checked` renders through `display_hinted`. It needs its own `echo_hint_sites` map because lowering CONSUMES `render_hint_sites` — an entry marked there would have its value replaced by a string. No `Display`-trait exemption: the echo is structural (`Gauge {v: 1}`), where `echo` dispatches `to_string`. A `--no-check` prompt runs no checker, so it has no static type and still shows the erased word, exactly as a `dyn` position does. Coverage is `crates/noeta-cli/tests/cli/repl.rs` (subprocess, both shapes), not the corpus: a file's trailing expression is discarded rather than echoed, so no `.noe` case can reach this door | found 2026-08-16 |
| ~~**bug** — a `u64` above `i64::MAX` serializes to JSON as its signed reinterpretation~~ **FIXED (2026-08)** — `json.stringify(18446744073709551615u64)` wrote `-1`, bare and at every depth, and so did a derived `to_json()`/`inspect()`: a *data* encoding carrying the wrong number into an API response or a persisted record, with nothing to tell the reader. The JSON twin of the display fix, and the same mechanism: the checker records a structural `noeta_ast::RenderHint` at each JSON door — the `json.stringify` argument however its callee is spelled (qualified, selectively imported, piped, or a native derive's forward), a derived `to_json()` receiver, a derived `inspect()` receiver — lowering replaces the serializing call with `Rvalue::JsonRender`, and both backends deep-marshal their own value and run one shared hinted walk (`noeta_ast::json_stringify`, which delegates every unhinted subtree to the single `json_text::stringify` engine). So a `u64` is written unsigned in a list, a map value, a map KEY, a tuple, a struct field and an enum payload. `Op::JsonStringify` is a new op, so `FORMAT_VERSION` moved 22 → 23. **Decoding was broken too, and further than the report said:** `type_to_recipe` had no `IntN` arm at all, so *no* fixed-width integer could be decoded from JSON — `@derive(Deserialize<Json>)` on a struct with a `u8` field was E0050 and `json.parse::<u64>` was E0007, i.e. nothing `stringify` wrote could ever be read back. `TypeRecipe::IntN { signed, bits }` decodes a JSON number that fits the declared width and reports one that does not, and `Json::Uint` keeps the range past `i64::MAX` exact on the way in (the *dynamic* `json.parse(text)` has no target type, so a number beyond `int` is still the `float` it always was). Conformance: `std/json_unsigned.noe` pins stringify AND round-trip at `i64::MAX`, `i64::MAX + 1` and `u64::MAX`, bare and in every nesting position, with the narrower widths and `i64` as controls and the out-of-range refusals as the negative cases. **The last position closed 2026-08-17:** a LiveView binding's push (`reactive::binding_value_json`) serializes a reactive node's current value on a flush tick, where there *is* no call site — so the hint is captured at the call that BOUND the value and kept with the binding. A native type declares which argument it keeps (`ExtType::push_hint_args`, `View.expose`'s parameter 1), the checker records that argument's element type into `binding_hint_sites`, lowering bakes it onto `Rvalue::Method::push`, and both backends hand it to the dispatch as `NativeCtx::push_hint` — the tree-walker off the IR node, the VM off a span-keyed `Module::binding_hint_sites`, exactly the two channels the ordering hint uses. The frame then goes through the same `json_stringify` walk every other JSON door reaches, so a pushed value and a `json.stringify` of it cannot disagree. Declared by the type rather than named in the checker, so a package's own extern type with the same shape gets the same treatment. `FORMAT_VERSION` 24 → 25 for the module table; `ABI_VERSION` 20 → 23 across the three additions (the `render_hint` move, `packed_names::display_hinted`, and this). Conformance: `reactivity/view_diff_unsigned` (all three boundaries, at snapshot and at a later patch, bare and in a list and a struct field, with an `int` and a `u32` as controls and the equivalent `json.stringify` alongside) | found 2026-08-16 alongside the display defect |
| ~~**`u64` above `i64::MAX` orders as signed**~~ **FIXED (2026-08-16)** — `[u64::MAX, 1u64].sorted()` was `[u64::MAX, 1]`, and `to_set()`'s canonical order, a `Map<u64, _>`'s key order, `keys()`, `values()`, `min`/`max` and a `for` over a set or map were all the erased word's order; a `@derive(Comparable)` struct with a `u64` field compared `-1 < 1`. The fix is **two mechanisms, split on where the signedness can live**, and the split is the whole design. A declared `u64` **field** is a property of the *type*, so it rides on the type's runtime description (`Shape::unsigned_slots` / the tree-walker's `FieldSpec::unsigned`, both projected from the declaration through one `noeta_ast::unsigned_field_slots`) and the structural compare reads it at every door, `dyn` launder included. A **bare** `u64` in a collection has no such description — its signedness is only in the static type of the site — so the checker records an `order_hint_sites` `RenderHint` (the ordering twin of the display hint) at the doors that reveal an order a program can see, and lowering bakes it onto `Rvalue::Method::order` / `Stmt::For::order`. **A hint never reaches an identity order:** a set's canonical buffer and `MapKey::cmp` stay a pure function of the erased word, because they are built at one site and probed at another — a hint there would let a `dyn`-positioned probe miss a key that is present, which is worse than the wrong order. So a `Map<u64, _>` still finds every key while its rendered, `keys()`, `values()` and iterated orders all agree. `FORMAT_VERSION` 23 → 24 (the shape field plus `Module::order_hint_sites`) — 23 had already been claimed by the JSON row above, which is exactly the collision that constant exists to prevent. Conformance: `types/unsigned_ordering` (collections + the find-again property), `types/unsigned_ordering_fields` (declared fields, `dyn`-laundered). **Closed, same family (2026-08-17):** a `@packed` map key with a `u64` field *ordered* right everywhere but a rendered map printed its erased word (`Tick {at: -1}`) while `keys()` printed the value — `map_key_display` reached only `MapKey::Int`. It now has the packed arm `map_key_order` always had, threading the hint through `packed_names::display_hinted`, with the same slot numbering at the same depths, so the two views of one entry cannot disagree. The nesting was a second hole in the same shape and on the *ordering* side too: `packed_fields_order` fell through to the derived `Ord` at a `PackedKeyField::Struct` slot, so a packed key nesting a packed struct ordered its `u64` signed. Both descend now. `MapKey::Ord` is untouched — the identity order never sees a hint, which is what keeps every key findable. Conformance: `packed/packed_key_unsigned` (both boundaries at both depths, the find-again property, a hint-free packed key and a `dyn`-laundered map as the negative half). **And one the two halves create together:** `echo m` and `m.keys()` now show a `Map<u64, _>` in its unsigned key order while `json.stringify(m)` emits the deep marshal's erased-word order, so the same map reads out in two different key orders. Only the ORDER differs — every key's digits are correct on both paths — and JSON object order carries no meaning, which is why this is a consistency row rather than a defect; closing it means ordering the marshalled entries under the key hint in `json_stringify` | found 2026-08-16 alongside the display defect |
| ~~**bug** — a generic call's type argument is silently unpinned, erasing every per-instantiation fact riding it~~ **FIXED (2026-08-19)** — two independent gaps, both observable as a `u64` rendering as `-1` and neither one a rendering defect. **Inference:** a container literal written at the call (`wrap({"a": big})` against `fn wrap<T>(m: Map<string, T>)`) bound `T` to `dyn` rather than to what the literal held, while the identical value bound to a local first bound it correctly. The absorbing list/map arms report the EXPECTATION rather than what they saw — deliberately, so `List<dyn> = [1,2,3]` tags `List(Dyn)` — and the generic call built that expectation with `subst_or_dyn`, so an unbound parameter arrived as the real type `dyn` and was echoed straight back as the instantiation. It now arrives as an inference HOLE (`subst_or_hole`), which the absorbing arms decline (an expectation with nothing to say is not an expectation) and a non-empty literal then pins from its own elements. `type_name::<T>()` is what proves this is inference and not rendering: the same call was **E0023 cannot infer `T`** before the fix, so every hidden-slot consumer was affected, not only the hints. **Slot layout:** a call instantiated at a COMPOSITE of the caller's own parameter (`fn outer<T>(xs: List<T>)` calling `wrap(xs)` at `List<T>`) had no channel at all — the render slots were one-per-type-parameter, so `render_hidden_arg` erased anything that was not a bare `Param`, an explicit `wrap::<List<T>>(xs)` included. A render slot is now a TEMPLATE like a forwarding one: each type parameter, then every composite of them its PARAMETER types name, sub-terms included (the parameters and not the return, or the ordinary `fn load<T>(text): Result<T, E>` would intern a slot at every call that nothing reads), laid out by the one function both the declaration and every call site read (`render_slot_templates`), and a composite that still mentions the caller's parameters passes the caller's matching slot through with `HiddenArg::Forward` — off the forwarding half too, since a forwarding slot's table entry carries the same hints. Conformance: `types/unsigned_composite_forward`, `types/unsigned_literal_argument`, `generics/literal_argument_pins_type_argument` (the type argument itself, at every literal kind), `generics/empty_literal_pins_nothing` (the negative half). No version constants moved | found 2026-08-19 |
| ~~**A composite instantiation assembled INSIDE a generic body carries no per-instantiation data**~~ **FIXED (2026-08-19)** — `wrap([v])` in `fn f<T>(v: T)` instantiates `wrap` at `List<T>`, and the premise of the row is still true: the hidden-slot layout has to be identical at the declaration and at every call site, so it is declaration-derived, and `f`'s parameter types never name `List<T>`. What the row missed is that the **leaf** is on a slot and the shape around it is static, so the callee's slot value is arithmetic on the slot `f` already has — no inference-derived layout needed. `HiddenArg::Compose` names a `HintComposition` whose cases the checker precomputes, one per combination of values its leaf slots can hold, and `Op::ComposeTypeArg` reads the leaves and hands the callee an ordinary table index; the callee resolves through the same splice over the same table, so nothing downstream knows it happened. The cases are enumerated over the program's own interned instantiations plus, along recorded call edges only, the answers of the compositions that can reach each leaf — so `a<T>` handing `b<U>` a built `List<T>`, and `b` building on that, gets `List<List<u64>>` while nothing interns the deeper composites no program can produce, and an entry is interned only where its hints are non-empty, so a program that renders nothing gains no table entry and the composition folds to a constant at lowering. It closed the same forward from inside a generic type's method, which has no hidden slot at all and composes off the receiver's tag — including the **bare** parameter `wrap(self.tag)`, which its layout carried no slot for either and which nobody had noticed. `FORMAT_VERSION` 27 → 28 (`Op::ComposeTypeArg` is declared beside `SelfRenderSlot`, so every discriminant after it shifts) and `ABI_VERSION` 23 → 24 (`HiddenArg::Compose`). **The recipe channel is deliberately untouched:** a composed entry carries no `TypeRecipe`, because it is reachable only from a render slot — which may legitimately name nothing — and letting a recipe-consuming door resolve off one would move an unbuildable instantiation from E0058 to a runtime abort. Conformance: `types/unsigned_composite_forward` (every container shape, two levels deep, a nested `fn`, a generic type's method, a two-leaf `Map<K, V>`, a two-frame chain, and an `int` instantiation as the control), `types/unsigned_identity_order_survives_hint` (the composed hint reaches the rendered and sorted orders and never the placement one), `noeta-check`'s `render_slot_compositions` (the table does not grow for a program that renders nothing; a composed entry has no recipe). Docs: `docs/Fixed-Width-Integers.md` | boundary of the 2026-08-19 fix above; closed the same day |
| **bug** — a hand-written `compare` is ignored by `sorted`/`min`/`max`/`to_set`, so the ordering they use contradicts `<` — a type whose `impl Comparable` reverses the order sorts *ascending*: `struct Rev { v: int; impl Comparable { pub fn compare(other: Rev): Ordering { return other.v.compare(self.v); } } }` gives `a < b` = `false` while `[a, b].sorted()` is `[Rev {v: 1}, Rev {v: 2}]` and `.min()`/`.max()` are both inverted. `@derive(Comparable, via: field)` has the same shape: the sibling fields it exists to exclude are what `sorted` orders by, and two values equal under the declared order stay two distinct members of a set. The operators dispatch the declared `compare` (a pushed callee frame in the VM, `call_method_on` in the tree-walker); the collection surfaces always take the structural comparator, which never asks whether a `compare` exists. The checker *admits* those types at `min`/`max` precisely because the `impl` is there, so the check promises a contract the runtime does not keep. Closing it means calling user code from inside a sort comparator in **both** backends, which the VM has no mechanism for today — its operator dispatch pushes a frame and returns to the interpreter loop, so a native method cannot invoke it synchronously. Either a re-entrant call from a native method, or a sort that suspends (the shape `map`/`filter`'s `iter_next_apply` applier has for closures, which is the nearest existing precedent) | found 2026-08-19 while gating `sorted` on `Comparable`; measured on both backends |
| **`count` means two different things one `.iter()` apart** — `xs.count()` on a `List<bool>` is the popcount of `true`s; `xs.iter().count()` is the number of remaining elements. Measured: `[true, false, true].count()` is `2` and `[true, false, true].iter().count()` is `3`. Both are deliberate on their own (the list has `len()` for the size; a lazy iterator has no length to ask for), and together they are one name with two answers on the same values. Renaming either is a breaking surface change, so this is a naming decision to take deliberately rather than a defect to patch | found 2026-08-19 in the `Iterator<T>` vs `List<T>` census |
| **Iterator terminals the eager list has and the lazy pipeline does not** — after `min`/`max` landed the remaining gaps are `product`, `checked_sum`, `any`, `all`, `last`, `contains`, `join` and `to_set`; `first` is spelled `next()`. Each is a `.collect()` plus the eager method today, which materializes the whole tail to answer a question that streams — `any`/`all`/`contains` especially, since they can short-circuit. The pattern the ordering terminals set is the cheap one to follow: a directly list-backed iterator hands its remaining range to the eager reduction, an adapter chain drains into a temporary list, so the lazy and eager spellings are the same value by construction | found 2026-08-19 in the `Iterator<T>` vs `List<T>` census. Trigger: a pipeline in real code that pays to materialize |
| **Payload-carrying variant as a first-class constructor** — `map(Shape.Circle)`. `some`/`Ok`/`Err`/`panic` are first-class callables (poly-values F3) but a *user* enum's payload variant is not: it is a clean `E0007` in value position (`enums/payload_variant_is_not_a_value.noe`). It previously type-checked and then died in the backend; the guard closed that, and making it *work* needs a variant-constructor value form both backends construct identically (`Fn(payload…) -> Enum`), the enum analogue of `Op::LoadNativeFn` | stream-status session (2026-07), found via the now-located `internal error`. Trigger: real code wanting to pass a variant constructor to a higher-order function |
| **`Members`/`DynamicCall` protocol dispatch** — registered in the trait table (`get(name)` / `call(name, args)`) but with no behavior: no language construct consumes dynamic member access today (`obj.f(args)` field-calls, `Callable`, and `next`-driven iterators all shipped without it). Deliberately deferred rather than shipping speculative machinery | M1.8b tail (the last unwired protocol names). Trigger: a concrete consumer — a dynamic-proxy/ORM-style use case, or reflection-driven member access |
| ~~**Generic-method call-site-typed forwarding**~~ **SHIPPED (generic-forwarding arc, 2026-07)** — a generic METHOD's own type parameter now forwards into a call-site-typed position (`json.try_parse::<T>`, `attributes_of::<T>`, `type_name::<T>()`) inside a method body. **The measurement above was right about the obstacle and wrong about the fix.** The obstacle was real: five entry points, four name-keyed with no static receiver type, all binding positionally — so a *prepended parameter* corrupts arity on four of them. But the answer was not a trailing defaulted parameter; it was to stop putting type arguments in the value-argument list at all. `Rvalue::Call`/`Rvalue::Method` (and `Op::Call`/`Op::CallGlobal`/`Op::CallMethod`) carry a dedicated `type_args` operand, and `Func::hidden`/`Chunk::hidden` say how many leading slots it fills; value parameter positions no longer shift, so the four dynamic entry points simply supply *nothing* and abort naming the callee (`no_instantiation_message`, the precedent `generics/class_type_param_unknown_instantiation.noe` set), while a checked call by name fills them. The predicted costs mostly evaporated with the premise: `supplied` now survives verbatim at a forwarding call (it used to be discarded — a *pre-existing* wart this removed), `params_of` never saw `$ty` (it reads the reflection artifact, not the closure), and the JIT's direct-call path falls back rather than needing default-filling. What it did cost: `Chunk::hidden_base`, because a method's receiver sits at register 0 *ahead* of the hidden block while an associated call passes that receiver as an ordinary argument. Conformance: `generic_methods/method_forwarding`, `generic_methods/method_forwarding_unreachable`; `generics/class_type_param_shadowed` and `reflection/type_name_diagnostics` turned from refusals into answers. **Still deferred, and re-measured 2026-07-29 — the boundary is narrower than this row claimed:** transitive forwarding through a member call is deferred only for a **compound receiver** (`self.inner.load::<T>(x)`, `f().load::<T>(x)`). `self.other::<T>(x)` — the very spelling written here — *works*, and so do `s.load::<T>(x)` (a local) and `Store.load::<T>(x)`: all three parse as the module-call atom `recv.func::<T>(args)` on a **bare identifier**, which is the pre-pass's recipe consumer, so the caller gets its slot and the call passes it through. Verified end to end (`generic_methods/forward_receiver_bound_local.noe`). What the pre-pass genuinely cannot do is name the callee behind a receiver that is itself an expression, so *that* stays refused — with an accurate diagnostic now (see the E0058 row below) whose help names the local-binding route that does work. A second, narrower refusal shares the seam: a bare-name member turbofish whose callee's own slot is a **composite** (`h.mid::<T>(xs)` against `mid<V>`'s `List<V>` slot) — the pre-pass registers the turbofish type as written, not the callee's substituted template, because substituting it means resolving the receiver | poly-deferrals arc D3 (2026-07); re-measured and shipped in the `generic-forwarding` arc, 2026-07; boundary re-measured 2026-07-29 |
| ~~**Reflection-intrinsic forwarding** — `roles_of::<T>`/`from_bytes::<T>` with a forwarded type parameter stay clean checker errors~~ **RESOLVED (2026-08-01), and the claim it made was wrong on both counts.** Neither was a *clean* error. `roles_of::<E>()` reported ``requires a `@semantic` enum, but `E` is not one`` — a capability gate firing on an operand that had no compile-time type to gate, so the author was sent to mark an enum that was already marked at every call site — and `from_bytes::<T>()` reported "requires a packable element type" for a `T` that really was `@packed`. Both were **misdiagnoses**, not refusals. And they were not one case: `roles_of` and `attributes_of` shipped, `from_bytes` will not. The split is the operand contract. `attributes_of`/`roles_of` are **name**-keyed — `materialize_attributes(&str)` and `materialize_roles(Option<&str>)` filter a name-keyed manifest and are total on an arbitrary name — so they take `TypeOperand` like `field_specs_of` and resolve a parameter on whichever of the two per-instantiation channels reaches the body, through the shared `type_param_name_atom`. `from_bytes` needs the element's packed **layout** (field kinds, bit widths), and *neither channel carries one*: the hidden slot carries a name and an optional JSON decode recipe, the receiver's tag carries a name. A by-name resolution is technically available (`NativeCtx::make_packed` does exactly that scan in both backends) and is still the wrong trade, because nothing in the language can bound `T` to a packable type — it would convert a compile-time guarantee into a runtime abort. So `from_bytes::<T>()` is now its own **E0058** naming the missing layout and pointing at decoding where the element type is concrete. Reopen only if a `Packable` bound *and* a layout projection on `Module::type_args` (the shape `type_arg_reprs` already has) both exist | poly-values arc F2b (2026-07); resolved in the reflection-operand unification, 2026-08-01 |
| ~~**Name-keyed reflection forwarding for `field_specs_of::<T>` / `variants_of::<T>` / `construct::<T>`**~~ **SHIPPED (2026-08-01)** — the row above predicted "pure sugar, one choke point in `lower_type_operand`" and was right about the shape but a third of the way through the work: the surfaces refused at the **checker**, so lowering never saw the site, and the forwarding pre-pass never marked the demand, so no slot existed to read. Three sites, one per stage, all of them the same edit already made for `type_name`: `forwarding::walk_expr` marks a bare parameter in a static `TypeOperand` as a **name-only** consumer (`mark(p, false)` — no build recipe, so it forwards for instantiations that have none); `check_type_operand` tries `record_type_param` before `reject_erased_type_param`, which is what routes the receiver-tag channel *and* the slot channel through the one helper `type_name`/`.as<T>()`/`is T` already share; `lower_type_operand` takes the span and emits `type_param_name_atom` in place of the folded `Const::Str`. **No new opcode, no `.noeb` bump, no runtime change at all** — the rvalues already took an `Atom`, so a resolved parameter simply arrives on the surface's own dynamic arm and `field_specs_of::<T>()` is `field_specs_of(type_name::<T>())` instruction for instruction. Boundaries preserved exactly: the static arm still resolves a real name at compile time (an unresolvable `T` is E0013, never a silent empty list), the dynamic arm's leniency is untouched, and the receiver tag is still consulted before the slot. E0058 narrowed to the residue no channel reaches — a nested `fn`'s own parameter and a class's parameter inside a nested `fn` — and its help lost the "pass the name instead of the type" advice, which had become a recipe for a second error. Conformance: `reflection/reflect_over_type_param` (rewritten from a refusal into an answer, with the composed spelling pinned to agree), `reflection/reflect_type_param_in_method` (all three channels), `reflection/reflect_type_param_no_channel` (the residue + E0013). Docs: `docs/Attributes-and-Reflection.md`, `docs/Generics-and-Traits.md`, `noeta explain E0058`. **Left open, same class — CLOSED same day:** `attributes_of::<T>()` forwarded on the **slot** channel only, because `Op::AttributesOf`'s `dynamic` operand was a `$ty` slot *index* rather than a name, which is why a generic type's parameter inside an instance method was E0058 there while `field_specs_of::<T>()` answered. The prediction that opening the tag channel is a bytecode-contract change rather than a lowering one held exactly: `Op::AttributesOf` became `{ dst, src }` and `Op::RolesOf` `{ dst, src: Option<Reg> }` — one name-string register each, the shape `Op::FieldSpecsOf` already had — for `FORMAT_VERSION` 17 → 18 (the layout-digest gate caught it before the bump, as designed). See the reflection-intrinsic-forwarding row above | poly-values arc F2b tail, 2026-08-01 |
| **Trait-method generics** — a trait's required-method set stays monomorphic: a per-method `<U>` on a trait method is a clean `E0058` (poly-deferrals D3 pinned this — the trait is dispatched dynamically, so each `impl` would have to agree on the method's own parameters). Generic methods live on concrete `class`/`struct`/`enum` types. Supporting them needs per-impl method-parameter unification at the dispatch seam | poly-deferrals arc D3 (2026-07). Trigger: a trait abstraction wanting a method-scoped type parameter |
| ~~**bug** — the E0058 forwarding diagnostic misdescribed the language~~ **FIXED (2026-07-29)** — one message served every way a call can reach the forwarding seam with no slot to pass through, and it was false in all of them: it claimed "call-site-typed forwarding is supported in top-level generic functions only" (generic methods have forwarded since the generic-forwarding arc — in the very repro that raised it, `Store.load<T>`'s own `json.try_parse::<T>` compiles — and the *inferred* case fires from inside a top-level generic `fn`), and its help advised spelling an explicit turbofish that two of the three reaching spellings already write. The fix is that **spelling**, not the resolved callee, is what the syntactic pre-pass's reach is a property of: a new `ForwardSpelling` (`Inferred` / `Turbofish` / `CompoundReceiver`) rides beside the `hidden_site` key from the five producers into `check_generic_call_seeded`, and the seam answers each in its own terms. `Inferred` keeps the turbofish help, which is the real fix there and only there. `CompoundReceiver` says the receiver is typed by checking while the slots are computed before it, and helps with the route measured to work — bind the receiver to a local, and the call is spelled on a bare name again. `Turbofish` (a bare-name member turbofish against a callee whose slot is a composite) names the missing slot and gives **no** help, there being no route to point at; a help line that repeats what the source already does is worse than none. Conformance: `generic_methods/forward_compound_receiver` (the refusal), `generic_methods/forward_receiver_bound_local` (the help's route, end to end). Wording is pinned where conformance cannot reach it — `noeta-check`'s new `forwarding_refusal_gallery` snapshot renders all three. Docs: `docs/Generics-and-Traits.md` (“Forwarding `T`” gained the bare-name-spelling boundary and the local-binding sample) | found reading E0058 against the shipped generic-forwarding arc, 2026-07-29 |
| **Editions S3/S4** — the first real edition-gated behavior (S3, pending a deliberate language divergence) and edition-aware diagnostics + a `noeta fix` migrator (S4, depends on S3) | editions arc. Trigger: a breaking language change we want to ship |
| ~~Validating-constructor hook for typed JSON decode~~ **SHIPPED (validation arc, 2026-07)** — the `Validate` built-in trait (`validate(): Result<void, E>`, `E` a `string` or an `Error`), auto-run **bottom-up** at every recipe door: `json.parse::<T>` (aborts, E0007), `json.try_parse::<T>`/`decode_typed` (recoverable `Result.Err(JsonError)` with `field[i]: <msg>` path), and `from_bytes::<T>` (packed, aborts at `[i]`). Plus `@validated` static construction channeling — outside-the-impl literal/record-update construction is E0060, forcing a validating constructor. Docs: `docs/Validation.md`. Conformance: `tests/conformance/validation/`. **Deferred within-arc:** `@validated` on an `enum`/`trait` is silently dropped rather than a misplacement diagnostic (structs/classes are the only literal-constructed types); dev-tier (`in_dev_tier`) does not bypass E0060 (a white-box test builds through the constructor). Trigger for the enum/trait diagnostic: a user hitting the silent drop |
| ~~**Multi-source `From` impls on one target**~~ **SHIPPED** — coherence keys `From` on the SOURCE, and **every** declared conversion is named after the source it converts (`from<JsonError>`, `noeta_ast::conversion`), so a method table with one slot per name holds them all. No new dispatch category: both reaching sites carry the source type (a `?`'s propagated `Err`, an argument's type) and resolve statically; the candidates are the declared conversions plus an enum's built-in backing conversion, and a call resolves when exactly one remains (several and an unresolved argument is E0023, naming them). Naming is **unconditional** — a conversion named `from` only while it had no siblings would be renamed by declaring an unrelated `impl From<B>`, action at a distance on a caller that never mentioned `B`. **It fixed a live bug rather than only generalizing one:** a backed enum reserves `Plan.from("free")` for its backing value, and an `impl From<Raw>` anywhere in its body used to take that slot and turn the string form into E0007 against `Raw`; the two coexist now. A by-name lookup (`invoke`, a `dyn` receiver) carries no source, so it finds no conversion and says which to call instead | error-ergonomics arc (2026-07) |
| **`From<Source>` as a generic bound** (`<T: From<int>>`) — `From` impls are recorded with their instantiation, but the built-in bound checker (`satisfies`) is name-only; an instantiated *built-in* bound needs the user-trait bound machinery (`TraitBound` args) extended to built-ins. Explicit `Target.from(x)` calls and the `?` position work today | error-ergonomics arc (2026-07). Trigger: first generic helper wanting to construct over a conversion |
| ~~**bug** — a bare `Ok(v)`/`Err(e)`/`[]`/`{}`/`none` **assigned to an already-annotated `mut` binding** is E0023~~ **FIXED (2026-07-28)** — the un-annotated `Stmt::Binding` arm now **resolves the name before it checks the value**, exactly as the entry named. When the name resolves to a binding whose type is *resolved*, that type is the expectation, and it is fed into `check` for the literal forms that can absorb it (`Checker::absorbs_expectation`, kept beside the absorbing arms of `check_inner` it mirrors: `[]`/`{}`/`none`/`some`/`Ok`/`Err`, a target-typed `.{ … }`, and a closure against an `Fn`). `E0023` is now understood as a *binding-site* diagnostic: it fires for a fresh binding, and for a reassignment whose target is still an unresolved hole (`mut acc = []` then `acc = []` refines nothing), never for one whose type is already known. Both must-not-regress rules hold: an expectation the value cannot absorb is *not* flowed, so the tailored `E0007` that names the binding and offers the union stays the single report on a mismatch, and nothing here introduces a binding. Bonus: `mut p: Point = …` then `p = .{ … }` (the same bug for a target-typed literal, not in the entry) now works, and `mut xs: List<int> = [1]` then `xs = Ok(3)` reports once instead of E0007 *and* a spurious E0023. Conformance: `bindings/reassign_annotated_mut_literal`, `bindings/reassign_annotated_mut_mismatch` | found writing para/ai's MCP client (2026-07-28) |
| Const generics → explicit SIMD (`Simd<T, N>`) | bitwise Tier P. Trigger: user-code SIMD demand the columnar/autovectorization path can't meet |
| ~~**bug** — **`construct(name, fields)` does not type-check a nominal field's value**~~ **FIXED (enum-construction arc, 2026-07-29)** — `plan_construct`/`plan_construct_named` validated a supplied value only when `enforced_scalar(spec.ty)` answered `Some`, so a field declared as a struct/class/enum was unchecked. Both planners now run one shared `check_field_value`, so the positional and named forms cannot accept different things, and a nominal mismatch is the same clean `Err` a scalar mismatch already was (`` field `inner` of `Outer` expects Inner, got map ``). **Measured worse than this entry recorded:** after `.as<Outer>()` narrowing, `type_of` on the bad field reported `Type.Struct(Inner, [])` — the *declared* type — so the value was not merely wrongly stored but actively misdescribed, which is why it survived so far from its cause. An enum-typed field took a bare `int` the same way (`Holder {m: 7}`). Names are compared **kind-agnostically** (a declaration reaches reflection as `Type.Named(Inner)`, a value classifies as `Type.Struct(Inner)`; both qualify consistently under a `namespace` — measured), and generic arguments are deliberately not compared since they are erased on some runtime paths. A `dyn Trait` field has its own repr, so an implementor is never false-rejected. Fixed here rather than deferred because the enum-payload construction shares this exact code path. Conformance: `reflection/construct_nominal_field_checked` | para/ai phase 5 (structured output + struct-typed tool parameters), 2026-07 |
| ~~**No runtime door builds an `enum` value from JSON — `variants_of` closed only half of the gap it was asked for**~~ **FIXED (enum-construction arc, 2026-07-29)** — all four doors open, on one principle: **an enum is constructible from the same data its schema advertises.** (1) `TypeRecipe` gained an `Enum` form (`VariantRecipe`/`VariantTag`), so `type_to_recipe` stops answering `None` and `@derive(Deserialize<Json>)` accepts an enum-typed field; (3) came with it — `json.try_parse::<T>`/`json.parse::<T>`/`decode_typed` all walk that one recipe. A **backed** enum decodes by its BACKING (`enum Plan: string { Free = "free" }` decodes `"free"`, and refuses `"Free"`), a **plain** enum by its CASE NAME — exactly what each derives as its `{"enum": […]}` schema. Backings of any scalar type work, so an `int`-backed enum decodes from JSON numbers. (2) `construct("Enum.Variant", payload)` builds a case: the variant is named where the type name goes (as in source, and as the attribute manifest already keys a variant target) and `fields` is the payload, in either of `construct`'s two existing shapes — validated by the very planners a struct's fields go through, against the very `List<FieldSpec>` payload schema `variants_of` reports. A bare enum name is a teaching rejection. (4) `Enum.try_from(v)`/`Enum.from(v)` now match **backing first, then case name**, so the recoverable door finally reads what comes off a wire; the aborting door is kept. `variant_for_wire` owns that rule for both backends, and the compiler orders `Op::EnumFromStr`'s cases by declaration index so an ambiguous probe resolves the same way in both. **Validation holds at the door:** a value naming no case is a path-carrying `JsonError` of the new `unknown_variant` kind listing every accepted value — never a panic, never a silently-wrong value; an enum implementing `Validate` re-enters like a struct. An enum with a **payload-carrying** variant still declines *as a whole* (a data-carrying sum has no canonical JSON spelling, and decoding its payload-free half would accept documents against a schema that cannot describe the type) — that case is built by `construct` instead. `ABI_VERSION` 9 → 10 (`TypeRecipe::Enum`, plus `NativeOut::Variant`'s new required `has_validator`). Conformance: `enums/decode_plain_enum_field`, `enums/decode_backed_enum_by_backing`, `enums/from_backing`, `enums/from_backing_panic`, `reflection/construct_enum_variant`, `reflection/construct_enum_rejections`. Docs: `docs/Derives.md` ("Enum-typed fields"), `docs/Attributes-and-Reflection.md` ("Constructing an enum case"), `docs/Structs-Classes-and-Enums.md` ("Converting a wire value to a case"), `docs/Validation.md`, and `std.json`'s registry prose | para/ai phase 5 (DESIGN §6/§8.5's U-6 tail), 2026-07 |
| ~~**Name-keyed reflection forwarding for `type_name::<T>()`**~~ **SHIPPED (2026-07)** — the premise held exactly: the hidden type-argument slot already carried the instantiation's name in the same `TypeArgInfo` entry `attributes_of::<T>()` has read since F2b, so this needed a name-only consumer in the forwarding pre-pass, a `dynamic_type_name_sites` map, and one new `Rvalue::TypeSlotName`/`Op::TypeSlotName` in place of the folded constant. No recipe is involved, so it forwards for instantiations that have none (an enum). Fixed a live bug on the way in: the table interned the instantiation's **`Display`** form, which renders the SHORT name, while every name-keyed registry is keyed on the linker's qualified name — so a forwarded `attributes_of::<T>()` under a `namespace` silently answered the empty list. It now interns `Type::head_name`, the lattice twin of the `TypeRef::head_name` the static form folds to. Boundaries preserved: bare parameters only (`type_name::<List<T>>()` heads at `List` and stays folded), unpinnable instantiation still E0023, forwarding-fn-as-value still E0058, and a generic METHOD's own `<U>` still E0058. Consequence for consumers: a generic body can now name its own `T`, so `field_specs_of(type_name::<T>())` answers the real per-instantiation schema and the doubled call-site name (`Output.of(type_name::<Extraction>())`) is no longer forced. Conformance: `poly_values/forward_type_name`, `poly_values/forward_type_name_namespaced/`, `poly_values/forward_attributes_namespaced/`, `reflection/type_name_diagnostics` (rewritten onto the method case, which is what still holds) | para/ai phase 5, 2026-07 |
| ~~Range-*checked* narrowing conversions (`checked_to_u8(): u8?`) alongside the wrapping casts~~ **SHIPPED (2026-08-15)** — `checked_to_i8`/`checked_to_u8`/…/`checked_to_u64`/`checked_to_int` on every numeric receiver, each answering `?dest` (the row's `u8?` is not this language's spelling; annotations are `?u8`). One contract, stated as an equivalence rather than a list of cases: `x.checked_to_T()` is `some(x.to_T())` wherever the total cast is exact and `none` at exactly the inputs where it has to wrap (integer receiver) or saturate (float receiver) — which decides the float question the row left open, since truncation toward zero is shared with `to_int` and is not a range failure, so `(3.9).checked_to_int()` is `some(3)` while `(1000.0).checked_to_u8()` is `none`. Float *destinations* get no checked spelling (`to_f32` loses precision, not range) and `checked_to_f32` is simply not a method. It rides the existing conversion registration — one appended `IntMethod::CheckedConvert` decoded by `IntMethod::from_name`, so arity, method-existence and the checker's parameter list all came for free — and no new opcode: a fixed-width receiver already lowers to the width-carrying `WidthIntMethod`. **The design crux is a bit the naive version drops.** A fixed-width value is erased to an i64 word, so a `u64` past bit 63 carries a *negative* one: read as signed, `18446744073709551615u64.checked_to_int()` answers `some(-1)` where the truth is `none`, and `checked_to_i32()` answers `some(-1)` where the truth is also `none`. Every source narrower than 64 bits is already its exact mathematical value, so only a `u64` receiver diverges — but it diverges in both directions, so the receiver's signedness travels: the checker records the real `(signed, bits)` in `width_sites` (it recorded `(false, bits)` unconditionally before, signedness being irrelevant to the bit intrinsics) and lowering rewrites `src_signed` into the method. Backends cannot drift by construction: the shared `int_method_outcome` returns an `IntOutcome` enum, so a dispatch that forgot to build the option would not compile rather than silently answering with the wrapped word. Conformance: `types/checked_width_conversions` (both boundaries of every width, both signedness crossings, the `u64` crux, float receivers including `NaN`, and the `2^64` float bound that an inexact `<= u64::MAX as f64` test would accept), `types/checked_width_conversion_is_optional` (the negative half: `?u8` is not `u8`, and `checked_to_f32` does not exist). Docs: `docs/Fixed-Width-Integers.md` ("Range-checked conversions"), `docs/Standard-Library.md` | bitwise W4 note. Trigger: first fallible-narrowing use case |
| E7 — editor injection for `${…}` holes inside foreign-language expr-tier bodies | expr-tiers, gated on the text-tiers editor grammar slices |
| ~~**bug** — a `use` inside a tier block resolves an attribute's name but does not qualify it, so the attribute silently does nothing~~ **FIXED (2026-07-28)** — `noeta-loader` now builds a **per-tier-block overlay** next to each unit's rewrite tables (`qualify::UnitMap::tier_scopes`, keyed by the block's span) and `qualify_stmt_scoped` swaps it in while that block is qualified. So `@test { use std.test.{Skip}  #[Skip("…")] fn f(…) }` resolves to `std.test.Skip` and the runner skips `f`, matching the top-level-`use` and inline-FQN spellings, while the import stays **block-scoped** (the same name outside the block is still the E0029 "cannot be used as an attribute") and stays **inside the block** (a rewrite table, not a hoisted import — an inactive build drops the `use` with the block and leaves no dangling import). Top-level blocks only: a block in *statement* position is code, not a file scope, and a `use` there was and remains a loud `E0005`. Gates: `tests/conformance/tiers/block_scoped_use_strips.noe`, two `noeta test` CLI tests, three `noeta-loader` unit tests | found writing para/ai's `#[Tool]` test fixtures (2026-07-28) |
| ~~**bug** — **a whole-module `use` of a package's own *collapsed root* module resolves to an empty module name when that package is consumed as a dependency.** `para/aether` has `aether.noe` at its root, so that file collapses into the prefix and its module path is exactly `aether` (standalone) / `para.aether` (as a scope-array member). A sibling doing the whole-module import `use aether` then fails **`[E0019] no module `` in this project`** — an *empty* name, not a wrong one. Narrow and context-dependent, which is what makes it easy to miss: `noeta check` on the package alone is **clean** (2 files, 0 errors), and it only appears once the same file is also analyzed through a consumer's graph — in this case the package's own `examples/*`, which bind it via `para = [{ path = "../.." }]`. Named imports of the same module are fine (`use aether.{App, Route, Status, route_for}` resolves), so it is specific to the whole-module form. A synthetic package with what looks like the identical shape (package `para/aether`, `aether.noe` + `openapi.noe`, whole-module **and** named import of the collapsed root) does **not** reproduce it when checked standalone — the consumer context is required, which is the part I did not isolate further.~~ **FIXED (2026-07-30), and the suspicion filed above was wrong** — the splice does not mis-fire, it never runs. The parser splits a `use` into a dotted `path` prefix plus the imported `names`, the leaf always landing in `names` (`build_use`), so a bare single-segment `use aether` parses as path `[]` + name `aether`. `reroot_path` keys entirely on `path.first()`, so an empty path early-returns and this one import form is never re-rooted, while the module it names *is* re-rooted (`aether.noe` derives `para.aether` under the example's scope array). `module_with_namespace(modules, [], "aether")` then looks for a namespace of length 1 and the module's is length 2 → no match → `Resolution::NoModule`. Standalone the derived path really is `["aether"]`, which is why it only appears through a consumer, and `use aether.{App, …}` takes the group arm where path is `["aether"]` and re-roots correctly — matching both observations exactly. **A second, independent defect explains the empty name:** `unknown_module_error` renders `path.join(".")`, but a whole-module import's module is path **+** name, so its message under-names the module by one segment for *every* whole-module import (`use geometry.vec` → "no module `geometry`") and degenerates to the empty string when path is empty. Fix shape: re-root through the whole `path` + leaf pair rather than `path` alone (or normalize a single-segment `use` before re-rooting), and have the diagnostic append the leaf when the import resolved as a module rather than an item. **Both halves fixed here.** `reroot_use` re-roots the dotted spelling whole and re-splits it; when the trailing segment changes, an un-aliased import pins its original spelling as an explicit alias, because the local binding must not follow the module (a consumer keying the package `myweb` re-roots the module to `myweb` while the source that wrote `use aether` still says `aether.App`). A scope-array member keeps its trailing segment and needs no alias, so the common case is untouched. `unknown_module_error` and its `closest` input both name the leaf when the path is empty — the empty path can only be the whole-module reading, and measuring `""` against every module had been suggesting whichever was shortest. Verified by ablation: with the re-rooting reverted the end-to-end test fails ``no module `aether```, which is also the proof the message fix stands alone (pre-fix that printed nothing between the backticks). Tests: `noeta-loader` `a_single_segment_use_carries_its_module_in_the_name_and_is_still_rerooted` (all four re-rooting shapes) and `a_single_segment_use_of_a_dependencys_collapsed_root_links` (the real shape, a package consumed under `para.aether` importing its own collapsed root). para/aether's named-import workaround (`635fff4`) is clearer anyway and stays | found migrating the para/* repos onto derivation (2026-07-30) |
| ~~**bug** — four tests fail on `main` in extension/session registry scoping~~ **CLOSED (2026-07-29), and the row was wrong about the diagnosis.** The four were `noeta-embed/tests/ext_constraint_enforcement.rs` `an_extension_tier_site_restriction_is_enforced` (`unknown directive @audit`), `instance_registry.rs` `hot_swap_checks_against_the_session_registry` and `the_checker_scopes_the_tier_namespace_to_the_session_registry`, and `noeta-ext-abi/tests/constraint_fields.rs` `every_classified_field_has_a_live_reader`. **They do not "fail deterministically in isolation"** — measured, each passes alone, its whole test binary passes, and its whole crate passes. They failed only under whole-workspace concurrency, and the failure *set* grew with parallel pressure: one workspace run failed 12 tests across 5 suites, two competing runs failed 21 suites and pulled in a test neither failed alone. A set that widens under load is shared mutable state, not a regression — which is why "not bisected to a commit" was never going to succeed. Two independent fixes closed them: `eee54fd45` "resolve an ext tier globally when no binding regime is in effect" (another slice, landed 51 minutes *after* this row was written), and `9d14cd76a`, the `noeta-pm` fixture sweep — these four were collateral from machine-global fixed-name fixture paths that concurrent test binaries `remove_dir_all`'d out from under each other, so fixing one crate's fixtures turned all twelve green. Verified: two full `cargo test --workspace --no-fail-fast` runs on merged `main`, 166 suites, 2847 passed, 0 failed, all four green by name. **The row's closing argument was right even though its diagnosis was wrong** — a red suite on `main` does stop the next agent telling its own breakage from inherited breakage, and it cost exactly that here: two agents were briefed to hunt a deterministic regression that did not exist. The deeper miss is that `cargo test --workspace` was not in the gate set at all, so this surface went unwatched while conformance, the differential oracle, examples, doc samples, clippy and fmt were all green; it is in the gate set now | found by the tier-block `use` slice running the full suite as its gate (2026-07-29); re-measured and closed the same day |
| ~~**bug** — **the prelude enums are invisible to `variants_of` / `field_specs_of`**~~ **FIXED (2026-07-29)** — all five now answer the pair the way every other enum does, and so does every **native** (`ExtEnum`) enum, which the row guessed at and nobody had measured: `variants_of("std.http.Framing")` was `[]` on the very identity `type_of` stamps on its values. `reflect::build` walks the program AST and neither kind is in it, so both are seeded by `extend_reflection` from the tables every other consumer already reads — the shared `reflect::prelude_enums()` projected through a new `reflect::prelude_type_infos()` (payload types via `PreludeFieldTy::repr`, the reflection twin of the checker's `prelude_field_type`, so `Type.List`'s payload reports `Type.Enum(Type, [])` and not a hole), and the registry's `ExtEnum`s through `extension_enum_types()` (payload types via the existing `sig_type_to_repr`; a **backed** native enum's per-variant constant rides through as `backing`). Seeded under the guard the extension attribute shapes already used — *the program's own declaration wins* — so a user's `enum Ordering` still shadows, in a REPL session too. Both backends read one accessor (`ReflectionInfo::variant_specs`/`field_specs`), so the differential agrees by construction. **Two more bugs found standing in this one, both fixed here.** (1) A **leaf**-imported native enum's short name (`use std.http.{Framing}` → `variants_of::<Framing>()`) answered `[]` right after the program imported the type: the loader deliberately aliases only attribute structs there, so the turbofish folded to a name nothing is registered under. `noeta_ir::lower` now resolves it from the program's own `use` statements (`native_enum_imports` — import-driven, so a program's own `Framing` is never redirected to a native type it never mentioned), and `type_name::<T>()` folds through the same helper, since its whole job is to produce the key the others consume. A *dynamic* operand is untouched: `variants_of("Framing")` still means the literal name. (2) The **tree-walker REPL** (`noeta_eval::Session`) never re-embedded after `accumulate`, where the VM's `SessionCompiler` always has — so `variants_of("Ordering")` was 3 on one REPL and 0 on the other, and the same asymmetry had been hiding the *extension attribute* shapes from the tree-walker REPL since they were introduced. `session_parity` covers it now. **`Op::EnumFromStr`'s baking stays, and the reason is measured, not assumed:** its `cases` carry an interned `Shape` id beside each name and backing, and only the compiler can mint one — the artifact makes the *data* recoverable at run time (same table, same declaration order, case for case) but a dynamic path would additionally need runtime shape interning, so the baking is now redundant as data and still load-bearing as shapes. **The other queries, checked rather than assumed:** `attributes_of` reads this same `TypeInfo` table and was already seeded; `traits_of` reads `trait_impls`, which `native_trait_impls` already joins over `registry.enums()` — and a prelude enum declaring no trait is the correct empty, not a hole; `roles_of` reads role *bindings*, not type declarations. `params_of`/`returns_of` do have a native blind spot, but a different one — its own row below. Gates: 1083 conformance (0 failures), differential in full agreement, `noeta-cli` examples + doc samples, `cargo test --workspace`, clippy, fmt. Conformance: `reflection/prelude_enums_reflect`, `reflection/variants_of_native_enum`, `session_parity::prelude_enum_reflection_agrees_across_entries`. Docs: `docs/Attributes-and-Reflection.md` ("The prelude enums are ordinary enums", both halves) | found measuring `Enum.try_from`'s baked-case table during the enum-construction arc (2026-07-29): the VM cannot resolve a prelude enum through reflection, which is why `Op::EnumFromStr` still bakes its cases |
| ~~**bug** — **`params_of` / `returns_of` report every NATIVE callable as nonexistent**~~ **FIXED (2026-07-30)** — the signature index is now seeded from the installed extensions exactly as the type index is (`extension_param_records`, guarded the same way: a program's own `fn`/method of that target wins). Keyed on each declaration's own identity: a module function under its root-qualified path (`std.math.sqrt`, covering `functions` + the higher-order `ctx_functions` + the call-site-typed `typed_functions`), a native type's / fielded type's / enum's method under `Type.method` on the type's **qualified** identity (`std.id.Uuid.to_string`), a native trait's method under `Trait.method` — the convention `reflect::build` already keys a `.noe` trait's signatures under. A native method's `params` exclude the receiver, like a `.noe` method's. **The row's one blocker was wrong, and measuring it took a minute:** it warned that shipped `ExtFn`s "mostly leave `param_names` empty", which would have made a DI framework inject by a blank name. Measured over the installed std registry: of **214** module functions and **180** native methods, **every one that takes a parameter names it** — the 119 empty `param_names` are all on zero-arity signatures, where empty is the only correct value. (A positional `_0`/`_1` fallback is in place for a declaration that genuinely has none, the convention a native enum's payload already reflects under; std never reaches it.) The polymorphic return forms resolve as far as the declaration does rather than flattening to `dyn` like the checker's `ret_to_typeref`: `SameAsArg(n)` is parameter `n`'s declared type, `NumericPreserving` is the `int | float` union it means, and a `TypeArg` is its declared wrapper around a `dyn` hole (`T` is named at the call site; a signature has none). **One live bug found standing in this one, fixed here:** `sig_type_to_repr` spelled a nominal by its **bare** registry name while `type_of` stamps the **qualified** identity, so `returns_of("std.id.uuid")` said `Type.Named(Uuid, [])` about a value that says `Type.Named(std.id.Uuid, [])` — one type, two names, out of the two queries the docs promise share a decoder, so a framework matching a declared return against a runtime tag missed on every native type. It resolves through the registry now, staying kind-**agnostic** `Named` because that is what a `.noe` annotation of the same type reflects as. The dynamic-operand rule is untouched: `params_of("math.sqrt")` (the import-relative spelling you *call* by) is a different name and still answers empty, the same line `variants_of("Framing")` holds. Cost measured: ~421 `ParamRecord`s ride in every reflection artifact. Conformance: `reflection/params_of_native.noe`, `session_parity::seeded_signature_and_prelude_struct_reflection_agree_across_entries`. Docs: `docs/Attributes-and-Reflection.md` ("A native callable is indexed like any other") | found measuring the adjacent queries while closing the prelude-enum reflection row (2026-07-29) |
| ~~**bug** — **a native fielded type's fields are invisible to `field_specs_of`**~~ **FIXED (2026-07-30)** — every `reg.fielded()` (native classes *and* value structs) is seeded through one `fielded_type_info` now shared with the attribute arm, with the kind taken from `ExtFielded::kind`, so `field_specs_of("std.http.Frame")` reports its four fields and their declared types. **The `construct` blocker this was split off for does not exist, and the measurement is worth keeping.** (1) The named hazard cannot occur: `std.http.Client` is an `ExtType` — an extern *handle* with no field list — not an `ExtFielded`, so widening `fielded()` never reaches it and `construct("std.http.Client", {})` still answers "not a constructible type". std ships exactly **one** native fielded type (`std.http.Frame`, a value struct, four public plain fields, no methods) and **no native class at all** — the class half exists only in out-of-tree/fixture extensions. (2) For a native class the refusal is structural rather than a special case: an `ExtField` carries no literal default, so every native field is mandatory and the shared `plan_construct`/`plan_construct_named` reject the omission — `construct("fx.Handle", {})` is `Err("missing required field `guard` of `fx.Handle`")`. Supplying it means supplying a real extern handle, which is exactly what the source literal `Handle { guard: g }` requires (both backends already registered every native fielded type as constructible). So no `construct` refusal had to be granted separately, and no `TypeInfo` wire field was needed. **What did need fixing, and the differential is what found it:** a native fielded type is the one place where the reflection key (qualified `std.http.Frame`) and the runtime shape name (short `Frame`) differ, so seeding it made the two backends disagree — the VM built an object under the qualified name that equalled no literal, and the tree-walker aborted with "cannot find type in this scope" (its scope binds only the short name). Both dynamic-construct paths now build the shape/`TypeDef` under the registry's canonical name and stamp the qualified reflected identity — the pair a source literal carries — so a constructed instance `==` a literal one, dispatches its native methods, and reports one `type_of` on both backends. **A second bug fixed on the way:** the leaf-import lowering rewrite covered native *enums* only, so `field_specs_of::<Frame>()` answered the empty schema right after `use std.http.{Frame}`, and `type_name::<Uuid>()` answered `"Uuid"` — a name nothing is registered under, which is the worse half, since handing out keys is that surface's whole job. `collect_native_type_imports` resolves every native nominal kind now (new `Registry::find_fielded_qualified`). **Three measured leftovers, each its own row below:** `construct` bypasses field **privacy** (it can set `fx.Handle.guard`, which a source literal cannot) and does not run `@validated` invariants — both pre-existing and equally true of `.noe` types; and narrowing a native fielded value keys on the **short** name, so `d.as<std.http.Frame>()` answers `none` for a literal-built value as much as for a constructed one (backend-consistent, pre-existing). Conformance: `reflection/field_specs_of_native_struct.noe`, `ext_class_seam::native_class_reflects_its_schema_and_constructs_from_it_on_both_backends` (the class half — the corpus cannot reach it, std declaring no native class) | found measuring native reflection while closing the prelude-enum row (2026-07-29) |
| ~~**bug** — **the prelude *structs* are invisible the same way the prelude enums were**~~ **FIXED (2026-07-30)** — with the clean fix the row named, not the names-only fallback: `PreludeStruct` carries `field_types` now, a closed `PreludeStructFieldTy` vocabulary that each consumer projects onto its own — `PreludeStructFieldTy::repr` onto reflection's `TypeRepr`, `prelude_struct_field_type` onto the checker's lattice — and the eight hand-written `&[Type]` lists at the `register_prelude_struct` call sites are gone, along with the drift they invited. **Measured before choosing:** `prelude_structs()` could supply nothing but names, so the choice really was "a real field-type source or names with no fields"; the set the eight declarations actually use is closed and small (21 fields over 10 arms: `Str`/`Bool`/`Dyn`/`TypeAdt`/`EnumKind`/`Struct(name)`/`Param(name)`/`List`/`Option`/`VoidFn`), and every field's type comes out precise. Exactly two reflect as `Type.Dyn` and both honestly are: `FieldEntry.value` *is* `dyn`, and `RoleBinding.role` is the abstract `Enum` **kind**, which the checker's own lattice→reflection projection maps to `Dyn` too (the value in that field carries its own concrete enum tag). So the "a wrong type travels" objection does not apply to any of them. A nominal field reflects kind-**agnostically** (`FieldSpec.type` is `Type.Named(Type, [])`), matching what a `.noe` declaration of the same type reflects as. Noted while there: the prelude **enum** payload projection is the one place that spells a nominal kind-*aware* (`Type.List`'s payload is `Type.Enum(Type, [])` where a `.noe` enum's would be `Type.Named(E, [])`) — a pre-existing asymmetry left alone, since its conformance case pins it and the kind-aware answer is the more informative one. Conformance: `reflection/prelude_structs_reflect.noe`, `session_parity::seeded_signature_and_prelude_struct_reflection_agree_across_entries` (which now also asserts *what* the two REPLs agreed on, not only that they agreed). Docs: `docs/Attributes-and-Reflection.md` ("The prelude enums are ordinary enums", struct half) | found measuring while closing the prelude-enum reflection row (2026-07-29) |
| ~~**bug** — **`construct` bypasses field privacy and `@validated` invariants**~~ **FIXED (2026-07-30)** — both halves closed with the fix shapes the row named, and both measured first. **Privacy:** `reflect::TypeInfo` now carries `field_public: Vec<bool>` beside `field_optional` (bundle `FORMAT_VERSION` 13 → 14, the pattern versions 8 and 11 set), populated from the *same statement* the checker reads — a value `struct`'s fields are always public, a `class`'s default private with a per-field `pub` opt-in, and a native fielded type's `ExtField::is_public` verbatim (kind-**un**adjusted there, matching `seed_ext_fielded`, which treats a native struct's non-`pub` field as private too). A shared `check_field_visible` in `plan_construct`/`plan_construct_named` refuses a **supplied** private field by name, in the checker's own E0035 wording (``cannot set private field `secret` of `Box` from outside it``), before the type check — visibility is the prior question. Two boundaries that had to be got right: reading the raw `is_public` bit instead of applying the kind rule would have refused **every struct field in the language** (a struct field carries `is_public: false` for want of a keyword it has no need of); and only a *supplied* field is refused, so an omitted defaulted private field still fills from its thunk — exactly what an outside-the-class literal that omits it does, since E0035 only ever looks at the fields a literal names. The refusal is **context-free**: a runtime door knows neither its caller's type nor its tier, where the checker's gate relaxes inside the declaring type's own methods and inside a dev-tier body. That is the conservative direction and those are precisely the sites where the literal spelling is available, so nothing expressible became inexpressible. **Validation:** both backends re-enter `validate()` on the freshly-built value through the same `validate_message` the recipe doors use, on the same condition they use — implementing `Validate`, **not** carrying `@validated` (`@validated` decides where a *literal* may be written; the validator's presence decides whether a data door enforces it, and `json.try_parse` already validates an undecorated implementor). The decision itself is shared (`reflect::construct_validates` over the same `trait_impls` table `traits_of` and `x is dyn Validate` read), so the backends cannot disagree about which constructions are checked. The enum-case arm validates on the **enum's** name, not the `"Enum.Variant"` spelling, mirroring the decode door's `NativeOut::Variant` arm. **Bottom-up, measured and pinned:** it holds *structurally* rather than by an ordering step — `construct` never builds a nested value, so every field value it is handed already passed its own door, and `construct_object` fills the defaulted slots before the type's own `validate` runs. An invalid inner is refused at its own `construct` call and the container's validator never speaks for it, the same short-circuit `json_validate.noe`'s `Cluster` pins for the recipe walk. **Siblings checked, per the question the row could not answer: `construct` was the only hole reachable from `.noe`.** `invoke` cannot call a private method because **the language has no method visibility** — `FnDecl::is_public` is documented "meaningless for a method", `ExtFn` has no such field at all, and the only privacy enforcement anywhere is `field_visible`/`report_private_field` at four field sites (read, assign, literal-set, and now `construct`). The decode doors cannot set a private `.noe` field either: `Deserialize<Json>` is derivable only for a **value struct**, whose fields are always public (a `class` is E0050 → E0007, measured), and `from_bytes::<T>` requires a `@packed` struct. One asymmetry remains and is deliberately left: a **native** fielded struct with `is_public: false` is JSON-decodable (`type_to_recipe` reads `symbols.records`, which ignores visibility) and its private field would be set — unreachable today (no shipped extension declares a private field; only the `fx.Handle` fixture does), and arguably right, since a decode is opt-in whole-shape reconstruction against an explicitly named call-site type where `construct` is ambient over every declared name. Filed as its own row below rather than decided here. **Coverage:** `reflection/construct_field_privacy` (positional + named refusal, supplied-vs-omitted, struct-vs-class, context-free-inside-the-class), `validation/construct_validate` (a `@validated` type, an undecorated implementor, the validator seeing a defaulted slot, the validated enum case, the nested bottom-up ordering, and the rejected build never becoming a value); plus the two native seams the corpus structurally cannot reach — `ext_class_seam` refuses `fx.Handle.guard` by name and positionally, and `ext_validated_recipe_seam` runs the native validator through `construct`. That last one is the **differential** case worth naming: the recipe door shapes a native fielded value under its QUALIFIED identity while `construct` shapes it under the registry's SHORT name and stamps the qualified one as the reflected tag, and the validator re-entry keys off the shape name — so the two doors reach `validate` by different routes and each backend resolves them independently, exactly the divergence shape the previous native-reflection change produced. They agree. Gates: build 0 warnings, conformance **1105**, differential full agreement (789 ran and agreed), workspace 0 failures, clippy, fmt. Docs corrected too — both pages *documented the holes as behavior* ("`construct` is not a door", "a `@validated` type really does construct reflectively without validating") | found measuring `construct` on a native class while closing the native-fielded reflection row (2026-07-30) |
| ~~**bug** — **a field's attributes are unreachable from the descriptor that describes the field**~~ **FIXED (2026-07-31)** — `FieldSpec` gained `attrs: List<dyn>`, populated exactly as `ParamInfo.attrs` is and for the reason that one exists: a schema-deriving library walks a callable's parameters with `params_of` and a type's fields with `field_specs_of`, and the two are meant to be **the same walk producing the same bytes** — which they could not be while only one of the two descriptors carried its annotation. It is a **view of the attribute manifest**, not a second table: the rows come from the same `"<Type>.<field>"` keys `attributes_of::<T>()` reports, through a new `ReflectionInfo::field_attributes_for` that is the field twin of `param_attributes_for`, and both backends build the instances through one shared `materialize_attr_instances` — so a field attribute cannot be visible through one query and missing from the other. The key format itself moved into `reflect::field_attr_target`, the write half `push_field_attrs` already used. An unannotated field reports the empty list, never an absence. A **variant payload** slot reports an empty `attrs` — the true answer rather than a stub, since a payload slot has no attribute syntax; a variant's own attributes stay in the manifest under `Enum.Variant` (`VariantSpec` deliberately gained nothing, and the docs now say why the struct half diverged). `FieldSpec` reflects its own new field correctly, which is a query a schema deriver really makes on its result type. **Consumer simplified:** `para/ai`'s `tools.noe` deleted `fn field_help(owner, field)` — an O(attributes)-per-field whole-program `attributes_of::<Arg>()` scan re-implementing the target-key format — and now reads `help_of(f.attrs)` off the `FieldSpec` it already holds, the same one-line body that reads `help_of(p.attrs)` off a `ParamInfo`. Conformance: `reflection/field_specs_attrs.noe` (both doors read alike, descriptor-vs-manifest agreement, the empty-list case, the payload case, `FieldSpec`'s own schema); `reflection/prelude_structs_reflect.noe` updated. Docs: `docs/Attributes-and-Reflection.md` (`field_specs_of`, the prelude-struct walk, and the variant-attributes paragraph) | found by a live agent app deriving one schema through two doors (para/ai `tools.noe`), 2026-07-31 |
| ~~**a JSON decode of a NATIVE fielded struct sets its private fields**, where `construct` on the same type now refuses to. `type_to_recipe` builds a `TypeRecipe::Struct` off `symbols.records`, which carries every field regardless of visibility, so `json.try_parse::<T>` on a native struct declaring `ExtField { is_public: false }` writes that field — while a source literal is E0035 and the reflective `construct` door now refuses it by name. **Unreachable today** (no shipped extension declares a private field; only the `fx.Handle` fixture does, and it is a class, which has no JSON recipe at all), and the `.noe` side has no such hole (`Deserialize<Json>` derives only for a value struct, whose fields are always public; `from_bytes` needs a `@packed` struct). **Deliberately left open rather than closed with `construct`**, because it is not obviously a bug: a decode is opt-in whole-shape reconstruction against a type the *call site* names, which is the serialization use case (privacy has never meant "not serializable"), where `construct` is ambient over every name in the program. If it should be closed, the shape is a `field_public` read in the recipe builder mirroring `plan_construct`'s, plus an `ExtField`-level opt-out; if it should stay, the asymmetry belongs in `docs/Validation.md`'s door table so nobody rediscovers it as a hole~~ **NOT A BUG — closed on the rule, 2026-08-15.** `pub` governs *access*; a derive governs *shape*. `@derive(Deserialize<Json>)` is written **inside** the declaration, so it speaks for the whole type — private fields included — exactly as a method written there does. A caller-side reflective door (`construct`, `fields_of`) is the other case and is correctly refused: it speaks for nobody. Refusing the decode as well would leave a type serializable and never recoverable, since `Serialize` writes every field regardless — a wire form with nothing able to read it back. The sibling row this was found beside was a real hole; this one is the same rule seen from the inside | found checking `construct`'s siblings while closing the construct-guards row (2026-07-30), closed 2026-08-15 |
| ~~**`fields_of(value)` reads a private field's VALUE**, which `x.secret` cannot. Measured: on a `class Box { pub label: string; secret: int }` built inside the class, `fields_of(b)` from outside answers ``[FieldEntry {name: "label", value: "hi"}, FieldEntry {name: "secret", value: 42}]`` — the read half of the hole the construct-guards row closed on the *write* side, under a different name. Distinct from `field_specs_of`, which reports a private field's *name and type* deliberately (the construct door needs the name to refuse it by name, and `ext_class_seam` asserts `fx.Handle.guard` is reported): a schema is a description, a value is the thing the declaration was protecting. **Not closed with the write side**, because it is a documented surface with real consumers (the fully-defaulted `Display`-over-`self` pattern in `docs/Attributes-and-Reflection.md` walks `fields_of(self)` from *inside* the type, where a private field SHOULD be visible) — so unlike `construct`'s, this gate cannot be context-free, and a runtime door has no caller context to read. That is the real question to answer before touching it: either `fields_of` gains a caller-relative notion the rest of the runtime does not have, or private field *values* stay readable through reflection and the docs say so out loud~~ **FIXED (burndown slice 1, `10604a637`).** `fields_of` now reports what the call site could have read: the checker records a per-site decision and the backends filter to it, so a private field is absent rather than exposed. `field_specs_of` is deliberately unfiltered — it answers about the *shape*, which `Serialize` writes in full | found checking `construct`'s siblings while closing the construct-guards row (2026-07-30), closed 2026-08-15 |
| ~~**a native fielded value's NARROWING identity is its short name, while its reflection identity is qualified.** `d.as<std.http.Frame>()` on a `dyn` holding a `Frame` answers `none` — for a source-literal-built value exactly as for a `construct`-built one, and identically on both backends, so this is consistency rather than divergence — while `d.as<Frame>()` (the imported spelling) answers `some`, `type_of` reports `Type.Struct(std.http.Frame, [])`, and every type-level reflection query is keyed on that qualified name. So the one type answers to two names depending on which surface you ask, and the qualified one — the name reflection hands you — is the one narrowing rejects. Deliberately not touched in the reflect-holes arc: the runtime shape name is what patterns, `is`, `==` and native method dispatch all compare, so changing it is a native-identity decision with its own blast radius, not a reflection fix. Fix shape: resolve a narrow target's `TypeRef` through the same `collect_native_type_imports` rewrite the reflection operands use, so the qualified spelling folds to the runtime name the value carries~~ **FIXED (burndown slice 1, `05be5be32`).** A native type named through its module resolves to its qualified identity, so `d.as<std.http.Frame>()` and `type_of` agree. The cause was `collect_type_aliases` gating on a namespace check that a module-qualified name never passed | found measuring `as<T>` on a constructed native struct (2026-07-30), closed 2026-08-15 |
| ~~**the conformance single-file harness never LINKS**~~ ✅ **DONE** (burndown slice 2, 2026-08-12) — both case paths link now: the single-file one through `noeta_loader::link` with a **deliberately empty sibling pool** (a case directory holds dozens of unrelated cases, several intentionally malformed, and the loader fails every entry in a directory when one module there is broken), the multi-file one unchanged. The two share `outcome_of_linked`, so they cannot drift again, and the divergent 87-line pipeline is deleted rather than kept in step. Pinned by `tests/conformance/reflection/native_enum_through_module_import.noe` and by `linking_is_what_resolves_a_module_qualified_native_type`, which runs the same source both ways and asserts 3 against 0. Whole corpus: 1194 passed, 0 failed. Original report: so anything the loader's rewrite tables decide is invisible to it: it lexes, parses and checks, and `noeta_loader` is only on the *directory*-case path. Measured concretely — `variants_of::<http.Framing>()` (the group-import spelling) answers correctly under `noeta run` and answers `[]` in the harness, because the loader's `add_native_type_aliases` never ran. Not a language bug, but it means a whole class of behavior can only be pinned by a directory case or a doc sample, and a single-file case asserting it will pass or fail for the wrong reason | found writing the native-enum reflection case (2026-07-29) |
| ~~**bug** — a `use` inside a tier block resolves a **std** name but not a **package** name, and `noeta check` says nothing. Minimal, three files, no dependencies: package `probe/lib`, `side.noe` declaring `pub struct Thing`, and `entry.noe` whose whole body is `@test { use probe.lib.side.{Thing}  fn t(): void { x = Thing { n: 3 } } }`. `noeta check entry.noe` reports **0 errors**; `noeta test entry.noe` fails the test with `cannot find type probe.lib.side.Thing in this scope`. Both import forms fail (`use probe.lib.side.{Thing}` and whole-module `use probe.lib.side` + `side.Thing`), the same `use` at **top level** works, and the same shapes over **std** work inside the block (`use std.math` → `math.abs(-3)`, and `use std.http.{Frame}` → a `Frame` literal, both pass). So the tier-scope overlay above qualifies the name correctly and the module is never **linked**: a std module resolves through the registry, a package module has to be in the unit graph, and the graph is built from a unit's *top-level* imports before tier activation hoists the block's.~~ **FIXED (2026-07-29)** — the **full linking** fix, not the check-time diagnostic: `link_core`'s entry loop now drives a top-level tier block's own `use`s through the same `drive_use` a top-level `use` goes through, against a block-scoped name table (the unit's, plus the block's own declarations). Driven **unconditionally**, not per active tier — which tiers are live is the build target's call, taken downstream in `noeta_check::tiers`, and the linked program is one memoized salsa value shared by `check`/`run`/`test`, so making linking tier-dependent would have been the restructure this slice was told to avoid. Merging unconditionally is safe because every merged declaration lands under its **qualified** identity: it binds no short name, collides with nothing, and is unreferenced (hence stripped with the block) when the tier is inactive — exactly what a top-level `use` that only `@test` references already does. The unresolved remainder is **dropped** rather than retained, so the `use` stays *inside* the block and an inactive build still drops it with the block, leaving no dangling import. Both properties the sibling fix established therefore hold unchanged: the import is block-scoped (the same name outside is still E0029/E0005) and is not hoisted. Only the **entry**'s blocks are driven — a pooled module's tier block is never merged into the program (a `TierBlock` is not a qualifiable declaration), so only the entry's blocks can ever activate. A side effect worth having: an import naming no such module inside a block is now an **E0019 at check time** with a "did you mean", where it used to check clean and fail at every use site under `noeta test`. Gates: `tests/conformance/tiers/block_use_links_package` (inactive half — output unchanged, both backends agree, nothing hoisted) and `tests/conformance/tiers/block_use_unknown_module` (the load-bearing half, verified to fail on the pre-fix loader by ablation), three `noeta test` CLI tests, three `noeta-loader` unit tests | found writing para/ai's phase-7 AG-UI suite (2026-07-28), where a `@test`-only `use para.ai.mock.Mock` checked clean and failed every use site at `noeta test` |

## Concurrency & runtime

| Item | Source / trigger |
|---|---|
| ~~**Interruptible host IO — the one case `h.cancel()` still cannot reach.**~~ **FIXED (interruptible-host-io slice, 2026-08-15).** A host now takes the same cancellation token the executor already got: `Cancellable::set_cancel(flag, wake)` is an arm of the `Host` union (defaulting to a no-op, so the sandbox, the browser host and WASI are untouched and the oracle stays byte-identical), and the isolate worker arms host and executor from one pair at startup. `RealHost` registers **one** hook that fans out to every live party — a child's output buffer, a process exit slot, an open stream, a request in flight — held weakly, so a program spawning children in a loop does not accumulate a closure per child. **The flag decides and the wake only rouses:** a roused party re-reads the flag and returns the new `ErrorKind::Interrupted`, distinct from `Io` because a `read_line` answering `none` means end of stream and a cancelled read is not one. Interrupted: `read_line`/`read`/`read_err_line` (a `notify_all` under the buffer's own lock — anything less is a lost wakeup, and the ablation test pins that a flag store alone leaves a parked reader parked), `wait` (which detaches its reap onto a background thread on a cancellable run, because `waitpid` is a syscall nothing here can end early), the sync `fetch` (`select!`ed against a `Notify`, reported as the new `NetErrorKind::Interrupted` — its own kind so a `try_fetch` caller does not retry a shutdown, and `retryable()` answers false), and a streaming body's next frame (a sentinel down its own channel, since an mpsc receiver has no other door and a timeout loop would wake a quiet stream a tick late). **`fs` is left uninterruptible, as the plan recommended and now with the reasoning in `docs/Concurrency.md`:** it blocks in the OS, the only file read that never returns is a FIFO or a character device, and the answer stays a deadline on the operation. **Two findings the build added.** (1) `Interrupted` unwinding as a diagnostic made a *cancelled* worker a *failed* one — its parent's `join()` re-raised a panic instead of yielding the cancelled outcome — so `std_dispatch_error` honors it as the cancellation it is; (2) it reached that function from only one of **30** call sites, the other 29 spelling `self.error(stdlib_error_code(e.kind), span, e.message)` by hand, so every one now routes through the single rule (`Exit` interception included, which those sites also lacked). Tests: `noeta-host-real` (each of the four leaves under a cancellation, the wake ablation, a spent cancellation no longer interrupting, an armed `wait` reporting the same outcome as an unarmed one, and an interrupted async read letting its runtime tear down promptly — the counterpart to `a_started_blocking_body_outlives_the_executor_it_was_spawned_on`), `noeta-cli` `run_real_isolate_cancel_reaches_a_worker_parked_in_a_child_read` (a `cat` with a stdin nobody writes: the house rule structurally, since cancellation is the only exit), and `tests/conformance/std/os_stream_end_is_not_an_error.noe` pinning that the sandbox's answer at end of output stays `none` through every door | isolate-cancel slice (2026-07-31), closed 2026-08-15 |
| ~~**JIT-compiled native code carries no cancellation poll**, so a cancellable run has to stay in tier 0 wherever it might need to stop~~ **FIXED (jit-cancel-poll slice, 2026-07-31).** The JIT emits an `atomic_load` of the run's cancellation flag at every **loop header** (the target of a backward branch — already its OSR entry), branching to that pc's ordinary bail block, and emits it **only** when the engine was built for a run that carries a flag. The stopgap below is therefore gone: `jit_osr_backedge` no longer looks at `cancel_flag` and a bounded run tiers up like any other. **Measured under `noeta test`** (median of five, quiet machine, both binaries interleaved): the 200M-iteration counting loop — the fully-JIT-sustainable shape that paid the whole tax — went from **6.53 s bounded / 0.64 s unbounded** to **0.76 s bounded / 0.66 s unbounded**. The 10× tax is gone; the poll itself costs about half a nanosecond per iteration. The 60M-iteration loop that already carried a bail site measured 5.49 / 5.57 before and 5.61 / 5.66 after — unchanged, as expected, since it never ran natively. **Three design points, each of which could have gone the other way.** (1) *No `noeta-jit-abi` change was needed.* The flag's address rides the existing baked-immediate seam — the same one `frame_template_addr` and the inline-cache slot addresses use — so the calling convention is untouched and the poll is one absolute-addressed load rather than an indirection through the `Vm` pointer or a new `Frame` field. The row used to call for "a stable `*const AtomicBool` at a known offset"; that turned out to be the more expensive of the two shapes, not the necessary one. (2) *The poll never unwinds — it deopts.* A set flag hands the frame back to the interpreter at the loop header and the interpreter's own back-edge safepoint decides. That keeps every rule about when a cancellation may be honored in one place, and it means the JIT structurally **cannot** truncate a destructor (`run_destructor` lifts the flag; native code that still sees it set merely bails). (3) *`atomic_load`, not `load`.* Cranelift runs at `opt_level=speed`; a plain load may be hoisted out of the loop or folded across iterations, which is exactly the check-once-then-never-again bug the poll exists to remove. `atomic_load` carries `other_side_effects` and lowers to a single `movzbl` on x86-64. **Lifetime.** The engine holds a strong `Arc<AtomicBool>` clone declared *after* its Cranelift module, so the flag outlives the code pages that bake its address — which matters because `observe_cancel` drops the VM's own clone the instant a request is honored. It crosses to the off-thread compile service as the `Arc`, not as a `usize`, for that reason. **Oracle.** `--jit-differential --cancel-poll` re-runs the whole corpus with a never-set flag armed, so the poll-bearing bodies get the same byte-identity / zero-residency / zero-anomaly gate as the ordinary ones; both arms green at 818 agreed, 2625/2632 prototypes native (`jit_differential_cancel_poll_agrees` is the permanent gate). An uncancellable run emits no poll at all, so its code is the same bytes as before. **Found here, FIXED in the isolate-cancel-race slice (2026-07-31):** `crates/noeta-cli/tests/cli/isolates.rs::run_real_isolate_cancel_is_joined_before_the_block_closes` was not flaky in the usual sense — it was **a pure function of machine contention, inverted**. Its worker did 4000 real `fs.write`s racing the parent's fixed `sleep(100)`, so on an *idle* box the worker won every time and `join` honestly reported `Ok(4000)`. Sampled 30 runs per regime on one binary: **quiet 0/30 expected, 16 spinners on 20 cores 0/40, 48 spinners 39/40**. It passed in CI and on a loaded dev box and failed on a quiet one, which is the worst possible signal from the regression guard for a *memory-safety* fix (the abandoned worker racing exit-time heap teardown into an allocator segfault). The fix is not a bigger loop — that only re-tunes odds that contention re-narrows, multiplicatively. The worker's loop is now `while true`, so **cancellation is its only exit**: `cancelled` is the sole reachable outcome, a working cancel is the only way the program terminates, and a broken one is killed by `assert_cmd`'s 60 s bound (verified in its source: on timeout it `child.kill()`s, so `.success()` fails — bounded and loud, never a hung job). New shape: **110/110 expected across quiet, moderate and heavy load**; the real suite `-- isolate` is 20/20 quiet and 20/20 under 48 spinners. Two siblings had the same bounded-worker-vs-fixed-sleep shape with a wider (19x) but still finite margin — `..._cancel_stops_a_compute_bound_worker` and `..._cancel_still_runs_the_workers_destructors` — and got the same treatment; the first's `elapsed < 1400 ms` ceiling was load-sensitive and is now a loose smoke bound, since an unbounded loop makes the stop structural rather than timed. Three others deliberately keep a fixed sleep because their worker cannot win the race for a *structural* reason (a bare `return`; a real-time 1000x5 ms sleep budget; a FIFO read unwedged from outside at 700 ms) — the file now carries that house rule where the tests are | timeout-cancels slice (2026-07-31), closed by the jit-cancel-poll slice the same day |
| ~~**A worker parked in one long `sleep` observes its cancellation only when the sleep ends.**~~ **FIXED (interruptible-io slice, 2026-07-31).** The answer is not another safepoint but a **wake**: the problem was never that the worker failed to look, it was that the worker was not there to look. `CancelWake` (`noeta-ext-abi`, beside `Executor`) is a hook list the canceller fires immediately after the flag store — `request_isolate_cancel` is now `store(true)` then `wake()` — and anything that can block outside the interpreter registers on it. `RealExecutor` grows its own per-executor `tokio::sync::Notify`, selected on at **every** point `advance` can block (the timer sleep *and* the `JoinSet` wait for a pending IO leaf, which the row did not name and which would otherwise have been an arbitrary half-fix). `Executor::set_cancel_wake` defaults to a no-op, so `SandboxExecutor` — whose `advance` jumps logical time and cannot block — is untouched and the oracle stays byte-identical. **The row's plumbing worry was the right problem and the wrong direction:** the notifier does not have to travel back, because a *hook* can travel forward. The parent creates the `CancelWake` at spawn beside the flag, hands the worker a clone, and the worker arms its own executor (built on its own thread — a `RealExecutor` owns a non-`Send` tokio runtime) before any user code runs; registration on an already-fired wake runs the hook immediately, so a request landing during worker startup cannot be swallowed. No second channel, no tokio in `noeta-vm`. **The wake decides nothing** — it only ends the block, and the worker's own poll one round later honors the request, so `observe_cancel`'s clear-once-honored and `run_destructor`'s lift stay the single authority on *when* a cancellation lands (the same discipline as the JIT's deopt-only loop-header poll), and a spurious wake is one extra scheduler round. Measured: the 3 s sleep cancelled at 200 ms ends the run at **0.21 s**, against **3.01 s** with the wake ablated on the same binary. Tests: `noeta-ext-abi` (hooks fire; a late registration fires at once; an armed `SandboxExecutor` still jumps logical time), `noeta-host-real` (a 60 s timer sleep and a 60 s pending IO wait are both woken at 50 ms; a pre-fired wake does not park), `noeta-vm` (a worker arms its own executor before the body, and a wake with the flag unset is spurious → `Done`), `noeta-cli` `run_real_isolate_cancel_reaches_a_worker_parked_in_one_long_sleep` (a **ten-minute** sleep cancelled at 200 ms — the house rule structurally: a real-time bound only grows under load, so cancellation is the only exit). Docs: `docs/Concurrency.md` (the "sleep in slices" rule is retired), `docs/Concurrency-Internals.md` ("The fourth safepoint is not one") | isolate-cancel slice (2026-07-31), closed the same day |
| ~~**A `noeta test` case parked in a long `sleep` is abandoned rather than stopped**~~ **FIXED (interruptible-host-io slice, 2026-08-15).** Done the way the row said to: `CancelSignal` bundles the flag and the `CancelWake` into one object, `RunOptions::cancel` takes it, and every `cancel` parameter along `run_one_test` → `execute_real_host` → `run_module_real_host` → `noeta-runner` carries the wake for free — no new parameter anywhere. `stop_overrun_case` is `cancel.request()`. `run_module_with` then arms the run's **host and executor** with it before the VM owns them, which is the same pair a worker isolate arms itself with; `IsolateSlot`'s two halves folded into the same object, so `request_isolate_cancel` is one call. **The JIT cost the row priced in did not arise:** the signal holds the flag as its own `Arc<AtomicBool>` rather than inline, so `Jit::cancel_flag` and its baked immediate are untouched and no JIT-oracle gate was needed. One thing the fold made possible and worth having: `observe_cancel` now **clears** the request as well as disarming the poll — the unwind runs destructors, which perform real IO through a host reading that same token, so a request left standing would have interrupted every leaf on the way out. Test: `test_a_test_parked_outside_the_interpreter_is_stopped_and_joined` — a case in a ten-minute `sleep` and a case blocked reading a silent child, both reported at their bound with **no abandonment warning** and both destructors run (the evidence that they were joined). Ablated by disarming the host/executor arming: both are abandoned, which is the reproduction | interruptible-io slice (2026-07-31), closed 2026-08-15 |
| App-lifetime `TaskScope` patterns: DI-managed workers, durable queues, schedulers; overlaps the "background-work extensions" proposal below | §7.2 design. Framework/extension patterns, not language constructs |
| Safepoint GC inside **re-entrant runs**: mid-run collection is gated to the outermost interpreter loop (a nested run's outer register stacks live in Rust locals the poll cannot enumerate), so cycles built inside `map`-applied closures, `NativeCtx`-driven loops (the HTTP serve loop), or a single mammoth un-awaited task body still accumulate until the next outermost safepoint / exit. Rooting the outer stacks (an active-stack registry) would lift the gate | memory-management 6.x follow-up. Trigger: a program that exhausts memory inside a nested run — the outer register stacks live in Rust frames the collector cannot scan, so mid-run collection is gated to the outermost loop and a re-entrant run accumulates until it returns |
| ~~**A blocking child read parks the whole scheduler**, so a *synchronous* API cannot bound a `Process.read_line` without killing the child~~ **CLOSED by the awaitable twins (2026-07-28)** — `Process` now has one for **every** blocking read, not just the one the entry named: `read_line_async()`, `read_err_line_async()`, and `read_async(n)`, each `Future<?string>`, alongside the existing `wait_async`. All three, deliberately — the entry's reasoning is about the *blocking* read parking the isolate, and stderr and the count-read park it identically, so a chosen subset would have left the same hole under a different method name (`read_err_line` argued explicitly rather than defaulted). They ride the `ExternIo` seam: one new `Os::os_proc_read_spawn(handle, ProcRead)` builds the descriptor, whose default resolves through the blocking methods at spawn (deterministic in the sandbox, in-oracle) and which `RealHost` overrides with a `RealBody::Blocking` body over the child's shared stream buffer. The read cursor moved *into* `SharedStream`, under the buffer's own lock, so a sync read and an async read on one child share a position by construction rather than by copy-out/write-back (which an off-thread body has nowhere to write back to). Measured on a child that speaks after 3 s: `read_line()` held the isolate **3.07 s**; `race([p.read_line_async(), deadline(300)])` returned `none` at **308 ms** and the next statement ran 5 ms later. **Left open on purpose:** items (3) and (4) of the entry — `h.cancel()` on an isolate parked in `sleep` reporting `Err(Cancelled)` while the isolate runs on, and `concurrent { … }` being E0040 in a synchronous `fn`. Neither is a subprocess concern and neither is closed by this; they want their own look. Conformance: `async/proc_read_async`. Docs: `docs/Concurrency.md` (a new "Streaming a subprocess" section) | found building para/ai's MCP stdio transport (2026-07-28) |
| Intrusive free-list registry — closes the trace collector's ~10% acyclic overhead on alloc-churn micro-benches | **The only held row with a large measured share behind it** — `alloc_with` is 25% of assoc and 17% of wordcount (measured 2026-08-16). But the attribution ladder puts those allocations on the *strings* being built, so not allocating 200k throwaway keys ranks ahead of allocating them 2x faster. Un-hold **second**, after the two rows below, worth roughly 12% of assoc at a 2x cheaper allocator |
| ~~**A real isolate's program output never reaches the run's `RunResult`.** `run_isolate_worker` builds the worker its own `Vm`, and nothing merges that VM's `out.stdout`/`out.stderr` back into the parent's — so `concurrent { h = isolate worker(1) … }` silently discards everything the worker echoed. Masked until now by batch capture (nobody could see the difference between "dropped" and "shown at the end"); the live-output slice makes the worker's lines appear under `noeta run`, because each isolate host inherits the driver's streaming choice and writes straight to the terminal. Still dropped wherever output is *collected* rather than streamed: the `@test` runner, `noeta test --json`, and any embedder reading `RunResult`. Fix shape: marshal the worker's two buffers home with its result (they are plain `String`s, so they ship like any `Wire`) and append them at harvest — which also settles the ordering question a streamed run currently answers by luck~~ **FIXED (isolate-output slice, 2026-07-31).** A worker now ships its **output home with its outcome**: `run_isolate_worker` returns an `IsolateReport { output, outcome }` instead of a bare `IsolateOutcome`, the two buffers taken *after* the worker's own teardown (so a `__destruct` that echoes on the way out is included) and shipped on **every** arm — a cancelled or failed worker wrote what it wrote. The parent merges the block at its harvest point (`poll_isolate`, before the outcome becomes a value / a cancellation / an abort, so a failing worker's `echo` still lands ahead of the error it raises) and at `Vm::teardown` for a worker an early exit joined without harvesting. The merge routes through `emit_stdout`/`emit_stderr`, so a live parent streams the block exactly like its own writes. **The ordering contract, chosen and written down** (`IsolateOutput`'s doc comment, `docs/Concurrency.md` § *What an isolate prints*): **one contiguous block per isolate, appended at the point the parent joins it.** Within a block the worker's own program order is exact; the block lands at a point in the *parent's* program order, so isolates started and joined one at a time have a fully determined transcript; concurrent isolates appear in completion order and no total order is invented. Interleaving by timestamp was the alternative and is rejected — it needs a wall clock the deterministic sandbox does not have, it would make ordering depend on thread scheduling at *line* rather than block granularity, and it would shred each worker's own transcript for a global order that never existed. **No live-output regression:** what a streaming host already wrote has been drained from the worker's buffer, so only the unterminated tail travels; measured on `noeta run`, each line appears exactly once and the tail — previously dropped outright — now arrives at the join. `noeta test` needed no rule of its own: the case reads `RunResult.stdout`, so a worker's output is hidden on a pass and printed under the failure like the case's own, and `--json` carries it in the per-case `stdout` for both outcomes. **The row below's caveat narrowed with it:** a *stopped* case's `TestOutcome` was being discarded whole, and now its stdout is kept (its verdict still is not) — so a timed-out case that stopped when asked reports everything it printed, its worker's lines included, which is exactly the "it got this far and then wedged" evidence the rail exists to produce. Only an *abandoned* case still reports nothing, because it really is still running. Conformance: `async/isolate_output` pins the contract in the sandbox (both backends agree). CLI gates: `test_a_worker_isolates_output_is_captured_like_the_cases_own`, `test_isolate_output_lands_as_a_block_at_the_harvest_point_in_json` (byte-exact transcript, two isolates in **separate** `concurrent` blocks so nothing races), `test_a_stopped_case_reports_the_output_it_managed_to_produce` (wedge is a `while true` — structural, not a race against a sleep), `run_real_isolate_output_appears_once_and_its_tail_is_not_dropped`, plus the in-crate `a_worker_ships_its_program_output_home` | found diagnosing the live-Ollama stall (2026-07-31), while confirming the live-output change did not double-print |
| ~~**`noeta test` has no per-test timeout**, so one unbounded call hangs the whole suite with nothing to show for it. `run_tests` hands each case to a worker thread and `thread::scope` joins them all; a case blocked in a native read is never interrupted, the report is only printed after every case returns, and the run has no deadline of its own. Measured: `examples/providers` sat for 25 minutes on a single wedged socket, and because the report had not been reached, `noeta test` printed nothing at all — the suite looked hung rather than slow, and there was no way to tell which of the ten cases was the one. The live-output slice fixes the *evidence* half (a `noeta run` now streams as it goes) but not this: a test's stdout is deliberately captured, so a hung test stays silent by design. Fix shape: a `--timeout <ms>` (and a default) that fails the case with "did not finish within …" and names it, which needs the case to run somewhere interruptible — a per-case process, or a watchdog that can abandon the worker~~ **FIXED (test-timeout slice, 2026-07-31).** Every case now runs under a deadline — `--timeout <SECONDS>` (default **60**, `0` disables) and a per-test `#[std.test.Timeout(N)]` that wins over it in both directions, `#[Timeout(0)]` opting one test out. A case that blows its bound is its **own** outcome, not a failure: `TIME` in the report, `"outcome": "timedOut"` plus a `timedOut` total in `--json`, counted separately from `failed`, and the message names the bound, where it came from, and the exact spelling that raises it. The suite reports every other test, exits `1`, and terminates. **The mechanism is deliberately "stop waiting", not "stop the test"**, and the entry's "needs the case to run somewhere interruptible" turned out not to be a precondition: the case runs on a detached thread, the worker `recv_timeout`s on its result, and on expiry it reports and takes the next case, abandoning the overrunning thread. That leaks one OS thread and its whole isolate per timed-out case (a spinning one keeps burning a core), which is stated in the code, in the seam's doc comment, and in `docs/Testing.md` rather than papered over — and it is the right trade, because the alternative is a suite that never returns. Measured on the real shapes: two `while true` tests under the default bound reported at **60.01 s** and the process exited (2 cores burned, nothing hung on teardown); a test parked in a **native** call (a FIFO read with no writer — the class no VM safepoint can ever interrupt) reported at its bound and exited at **5.007 s**; and the unbounded path (`--timeout 0`, byte-identical to the pre-fix runner) still hangs indefinitely and prints only the `running N tests` header, which is the reproduction. Abandonment lives in **one function** (`stop_overrun_case`) so real isolate cancellation — the `isolate-cancel` branch, where `h.cancel()` currently sets a flag an OS-thread isolate never reads — is a one-place change to "cancel, join with a grace period, abandon only if that expires"; no beacon file and no watchdog isolate were built, precisely so there is nothing to unpick. **UPDATED (timeout-cancels slice, 2026-07-31):** the one-place change landed. `stop_overrun_case` now asks (`RunOptions::cancel` arms the case's top-level run with the same flag a worker isolate carries), waits a **1 s grace** on the case's own result channel, and abandons only if that expires — so the leak is gone for the class that used to dominate it. Measured under a 1 s bound: every compute-bound shape (pure arithmetic, the same loop calling a fn, the same loop allocating) stops and is **joined**, run ends at 1.05 s, and each case's live locals' `destruct` fires — the direct evidence that it was joined rather than abandoned. A case holding a 400 000-object heap when asked stopped in ~165 ms, teardown included. The native-blocked class (a FIFO read with no writer) is unreachable by any safepoint and is still abandoned: reported, exit 1, run ends at 2.07 s (bound + grace), and the message now *says* it was abandoned and points at bounding the operation instead of the test. **A cancellable run declines OSR** — see the row below — which is what makes the compute-bound half real. On the exit-time race that made abandoning a *worker* unsound: it does not reach here, established over 240 runs of the hostile shapes (a spinner, six cases allocating flat out, a FIFO-blocked case, alone and together) with zero abnormal exits, and structurally because a `@test` case shares no heap graph with the runner (`noeta-value`'s object registry is thread-local) so it is leaked rather than freed-out-from-under, and mimalloc runs `mi_process_done` with `destroy_on_exit: 0`. **NOT fixed here, and since fixed by the isolate-output slice (2026-07-31):** a timed-out case reported no captured stdout, because a *stopped* case's `TestOutcome` was discarded whole *and* the row above (an isolate's output buffers never reaching the parent's `RunResult`) meant a worker's lines would not have arrived anyway. Both halves are closed: a stopped case's stdout is now kept (its verdict still discarded) and printed under the `TIME` line, isolate output included. Only an **abandoned** case still reports nothing, which is the honest answer — it is still running | found diagnosing the live-Ollama stall (2026-07-31). Trigger: any second suite that touches a network or a subprocess |
| ~~**bug** — unary `-` and `!` on a heap-boxed `int` each strand one object in the VM~~ **FIXED (2026-08-16)** — `a = 4611686018427387904; echo -a` exited cleanly with 1 object still live, and `!a` did the same; the tree-walker was clean and so was `0 - a`, which localized it to `Op::Unary`. The dispatch arm retained its result unconditionally, on the reasoning that `...xs` (spread) is the runtime identity and therefore aliases the operand's live reference. That reasoning holds for spread and for nothing else: `Neg`/`Not` build a **fresh** value which already owns its reference, so a retain there is a second owner nobody releases — the arithmetic path takes `apply_binary`'s result the same way, with no retain, which is what made the two paths' disagreement the whole bug. Now only `UnaryOp::Spread` retains. Invisible on an ordinary `-x` because an `int` inside the 48-bit inline range is NaN-boxed and never allocates, so only an operand past `2^47` can show it. **Why the leak oracle never saw it:** the oracle asserts residency 0 at clean exit over the whole corpus and did so on every run — no case had ever applied `-` or `!` to a large `int`, and the corpus's large integers all arrived through *binary* arithmetic (`expressions/integer_wrapping.noe`) or a constant. `gc/unary_boxed_int.noe` closes that, pinning both operators against a boxed operand, the fixed-width `i64` form, a destination aliasing its source, and a spread of boxed elements as the control for the retain that must stay: residency 7 → 0 | found 2026-08-16 writing an unrelated conformance case |

## Reactivity & web

| Item | Source / trigger |
|---|---|
| OTEL **surface residuals**, one row because they are one gap: the spec has more surface than the implementation. **Observable/async instruments** (`ObservableCounter`/`ObservableGauge` with pull callbacks — the sync four ship). **Histogram views** — custom buckets and delta temporality; every histogram uses the OTel default explicit bounds and only cumulative is emitted. **A stdout/structured-logging bridge** — `std.log` records reach the OTLP sink only, and mirroring them to stdout is a separate decision about who owns the console. | native-otel metrics-logs, §deferred/§decision 3. Trigger: someone running Noeta against a real collector who hits one of them — none is reachable from this repo's own use, which is why three separate rows were three separate fictions |
| OTEL: **metrics cardinality limits** — no per-metric attribute-set cap today (unbounded `BTreeMap` per instrument); a hard cardinality-limit policy slice deferred | native-otel metrics-logs, §deferred. Trigger: a server running long enough to show it — this is a **memory leak**, not a missing feature: the per-instrument attribute-set map is unbounded, so a metric carrying a request id or a user id grows without limit for the life of the process |
| Synced **store** (a whole reactive dataset, the §9.12 merge point) and `.history()`/time-travel over the p2p append log | p2p "later/open" R&D |
| ~~**Richer CRDTs — a last-write-wins register and an add/remove OR-Set.** **Not work for this repo**, which is the finding rather than the row: the p2p/local-first stack left `std` with the para-namespace arc, so every CRDT lives in the out-of-tree `para/p2p` package (`/home/niklas/Code/para/para-p2p`) — its `noeta-crdt` crate holds the `Mergeable` lattice contract plus `GCounter`/`PnCounter`/`GSet`, and `para.crdt` is where a fourth registers. Nothing in this workspace could host one: there is no CRDT crate here, no `Mergeable` Rust trait, and no `tests/conformance/crdt/`. Building either type here would re-open the `std` surface that arc closed. What this repo does supply is the seam that makes both buildable there and, meanwhile, buildable by an app: `para.crdt.Mergeable` is an ordinary native `ExtTrait` with a **required** `merge`, so a program can define its own LWW register today — the package's README shows exactly that, and its corpus pins it. The remaining blocker on packaged versions is stated in the package itself: both carry arbitrary application values rather than primitive state, so they need the retained-arena extern seam the three shipped CRDTs deliberately avoid.~~ **SHIPPED where it belonged (para-p2p `25c74b4`) — and the blocker this row inherited was not real.** The claim that value-carrying CRDTs "need the retained-arena extern seam" came from a stale module comment: the arena keeps a language value *with identity* alive backend-side, but a CRDT's whole state must serialize to bytes for a peer, so an arena handle is meaningless to it. The mechanism was already in the pinned ABI — `ExtType::deep_marshal`, honored by both backends — and `AutoDoc` was already a value-carrying, delete-capable CRDT using no arena, in the same file as the comment. The real constraint is the wire: both types hold data (primitives, and lists/maps of them; a struct arrives as its field map), and an extern handle or enum value is refused at the call rather than stored as a shape it would not come back as. LWW merges to the maximum under a total order whose last tie-break is the value itself — unreachable in correct use, and exactly the case (one replica id on two machines) where its absence leaves two replicas permanently disagreeing while both believe they converged. The shipped three were refactored onto the new shared `assert_lattice_laws` harness so none keeps a weaker check: commutativity, associativity, idempotence, convergence across all six reconciliation orders of three replicas, and a stale re-merge as a no-op | cut from the p2p arc; re-pointed 2026-08-16 after checking where the surface actually lives. Tracked in the package's own repo. Trigger: an app that needs one — and it lands in para-p2p, not here, closed 2026-08-16 |

## Database (para/db)

| Item | Source / trigger |
| --- | --- |
| ~~**Per-dialect migration overrides** — v1 runs each migration body verbatim in the target dialect's native SQL (write portable SQL). A `migrations/postgres/` (or `sqlite/`) sub-directory shadowing a base file for a divergent backend (e.g. `SERIAL` vs `AUTOINCREMENT`), selected by the connected driver's dialect. `load_dir` already ignores sub-directories, leaving room for this~~ **SHIPPED (para-db `1243994`).** A file under `migrations/<dialect>/` replaces the body of the same-named file above it when the connected driver reports that dialect, selected by a single `Self::DIALECT` const each driver also renders DDL with — so the dialect that picks the override and the dialect that generates schema cannot drift. Three calls the row left open, each argued in the commit: an **orphan override is a hard error on every backend**, not just its own (the tracking table is keyed by filename, so a dialect-only step is one another backend never runs and never records — structurally divergent and permanently invisible). The **checksum identity is per-dialect**, which does not break "one migration, one identity" because no database ever sees both bodies, and the rule it defends — never hashing *lowered* DDL — is untouched. The override keeps the base's **whole filename**, extension included, so ordering is dialect-independent by construction and the ledger can never record one name while running another's body. `--status` and the apply lines print `[<dialect> override]`, since a base filename over a different body was the feature's main failure mode | migration engine (aether DB6, 2026-07-19). Trigger: a project genuinely needing to target both SQLite and Postgres from one migration set, closed 2026-08-16 |
| ~~**A native package's own `.noe` cannot be checked standalone** — `noeta check packages/para-db/…` failed at `use para.db` (E0019)~~ ✅ FIXED — `resolve_graph` now adds the **root** package's own `package.native` as a `NativeCrate` (no self-trust gate, like cargo and your own `build.rs`), so `compose::maybe_delegate` composes it; a self-contained module of a native package (`query.noe`, `sql.noe`) now checks clean standalone. **Still open, and separate:** a file that imports a *sibling* module checks the sibling's body *without the sibling's own `use` imports*, so `reactive.noe` (imports `repo.noe`) still reports false errors in `repo.noe`. This is the single-file-in-a-multi-module-package limitation — NOT native-specific, pre-existing, orthogonal to the compose fix | native-compose half fixed 2026-07-22; sibling-body half *(active)* — trigger: wanting `noeta check <pkg-file>` to be a full-package gate |
| ~~**Out-of-order migration detection** — the runner applies pending files in filename-sort order and gates history integrity via checksum + deleted-file checks; it does not warn when a *newly added* migration sorts before an already-applied one (a rebase inserting an earlier prefix). Timestamps make this rare; a warning (not a hard error, to keep legitimate merges unblocked) could flag it~~ **FIXED (burndown slice 3, para-db `e1367c2`).** `Plan` carries `out_of_order` and the runner warns after applying — deliberately a warning and not an error: the rebase that produces it is ordinary, and refusing would block a deploy over something the operator usually wants to know rather than be stopped by | migration engine (aether DB6, 2026-07-19). Trigger: an out-of-order incident in practice, closed 2026-08-15 |

## Packages, registry & distribution

| Item | Source / trigger |
|---|---|
| ~~**Publish the toolchain + registry repos**~~ ✅ **DONE** (audited 2026-08-12) — both halves. The toolchain repo is `github.com/noeta-lang/noeta` with `origin/main` level with local `main`; the registry repo is `github.com/noeta-lang/noeta-registry`, also level. The nine `para` packages live in their own repos at `/home/niklas/Code/para/` and depend on each other through the **registry** (`{ version = "^0.4", package = "para/aether" }`), not the pre-publish `file://` git-dep form this row was written against | para out-of-tree follow-on |
| ~~**`noeta add` should print the full import path, not just the root.** It reports ``using import root `para` (derived from the package name)`` and ``added `para` to noeta.toml``, which leaves the reader to guess the spelling that actually compiles — `use para.cli.{…}`. The quickstart friction log has this as its #3, alongside the key-vs-derived-key ergonomics (when you need `para = { … }` under a different binding and what that does to the import root)~~ **FIXED (burndown slice 3, `01eb9f08d`).** `noeta add` prints the import paths a program actually writes (`use para.cli.{…}`), capped at six with a count for the rest, instead of leaving the reader to guess them from the root | pm-alpha burn-down, quickstart friction log #3. Trigger: the next packaging-docs or `noeta add` pass, closed 2026-08-15 |
| ~~**`noeta rotate` — a client verb for the scope token-rotation endpoint.** `POST /v1/scopes/{scope}/rotate` shipped with the registry hardening (current-token or admin auth) and has no CLI door: rotating today means calling the endpoint by hand. Promote its wire shape into the canonical fixtures at the same time, as the other registry verbs did~~ **FIXED (burndown slice 3, `01eb9f08d`).** `noeta scope rotate <scope>` ships, with the response shape promoted into the canonical wire fixtures (`rotate-response.json`) as the other registry verbs are | pm-alpha burn-down, registry hardening note. Trigger: a token believed leaked, or the next `noeta scope` pass, closed 2026-08-15 |
| Dynamic extension loading (the dyn dispatch tables exist; every compiled-in extension monomorphizes past them) | higher-order-abi. Trigger: a plugin that can't be compiled in |
| TUF-based Sigstore trust-root refresh (today: build-time-embedded root + env override); registry-side keyless *requirement* is a policy flag away | keyless-signing v-next. Trigger: **a date** — the build-time-embedded Sigstore root approaching expiry, which is the one gate here that arrives whether or not anyone is watching. A registry-side keyless *requirement* is a separate policy flag |
| Per-dependency **capability enforcement** (a dep's `[trust]` grant actually bounding what it can reach) — research phase; static effect analysis is the tractable first step | package-manager phase-4 L3. Trigger: the static-effect-analysis row un-defers — capability enforcement is downstream of it and cannot start first |
| ~~Git-deps of *published* packages aren't expressible in the index `Dep` shape~~ **ALREADY CLOSED — the check the row asked for says publish refuses** (`fix/pm-git-dep-publish`). Measured against the real binary: a manifest with `helper = { git = … }` aborts at publish with exit 1 *before* touching git, so nothing unresolvable ever reaches the index — and it cannot, since `Dep` is `(identity, req)` in both the index and the wire body, with no field a URL could occupy. The unexpressibility is the design, and refusing is the right answer to it: a registry whose entries point at arbitrary git URLs has traded reproducibility and a supply-chain boundary for a convenience the publisher can get by publishing the dependency. What the row *did* find was two residues, both fixed here: the git half had no test at all (only the path half did, sharing a match arm that anyone could split), and a **scope array** blamed the wrong thing — `para = [ {version=…}, {git=…} ]` reported ``dependency `para` is a path/git dependency`` and advised replacing the key, which would have dropped the innocent registry sibling. The validation now lives in `noeta_pm::registry::release_deps` (unit-tested) behind one CLI refusal, names the actual source kind, the URL and ref, and points at the offending scope *member*. Target-scoped deps (`[targets.dev.dependencies]`) stay unvalidated on purpose: they apply to the root only, never travel to a consumer, and are Cargo's dev-dependency case | package-manager v-next, closed 2026-08-15 |
| ~~**`noeta publish` never opens the tag it releases** — identity, version, license, and docs all come from the *local working tree* while the coordinates pin whatever SHA `--tag` resolves to (`resolve_tag_sha` is a bare `ls-remote`), so a repository whose working copy has drifted from the tag publishes coordinates that do not hold what they say — and the attestation signs that claim. The **consumer** side now refuses such a release (a served tree must be the release it was selected as), so nothing installs it; the cost is that the publisher's mistake surfaces for every consumer instead of at publish. A publish-time check would materialize the tag (the `--depth 1` clone a consumer pays anyway) and compare its `[package]`. Deliberately not done with the resolver half: it is a UX gate, not a security control — a hostile publisher simply skips it, and the server is where a binding check belongs. Trigger: the first real mis-published release, or the hosted registry gaining the server-side equivalent~~ **FIXED (burndown slice 3, `01eb9f08d`).** Publish prints the tag's and the commit's web URLs, so the thing it just pushed is one click away rather than reconstructed by hand | registry-identity (2026-07-31), closed 2026-08-15 |
| ~~A **path/git** dependency whose tree declares a *built-in* scope (`[package] name = "std/fs"`) resolves without complaint — `reserved::is_builtin` guards the registry *selector* and the import-root key, never the identity a local tree declares for itself. Measured, not theoretical. Low urgency (pointing a `path` at a tree is already a trust act, and the import prefix is the consumer's key either way), but it buys package-orphan-rule membership in a scope the toolchain owns~~ **FIXED (burndown slice 4, `6e9bff7e5`).** `parse_package` asks `reserved::is_builtin` about a tree's *own* declared identity, with a message that suggests `yourco/<pkg>` rather than accusing — a local tree named `std/fs` is far more often a naming mistake than an attack, unlike a registry serving one. The guard caught a fixture relying on the hole on its first run | registry-identity (2026-07-31), closed 2026-08-15 |
| **Advisory-intake residuals** (arc landed 2026-07-19: three-tier feed — operator/publisher/imported — + public report queue + `noeta advisory watch`; merged both repos, unpushed). ~~(a) client verb to **promote a report**~~ ✅ DONE — `noeta advisory promote <report-id>` (operator via `NOETA_REGISTRY_ADMIN_TOKEN` → operator advisory; scope owner → keyless publisher advisory, prefilled from the report) + `noeta advisory reports [--scope S]`; new `GET /v1/reports/{id}`. ~~(b) **CVSS-vector scoring**~~ ✅ DONE — CVSS v3.1 base-metric equations both sides (`src/cvss.ts` / `noeta-pm::cvss`, unit-tested vs published vectors); import derives the band from an OSV `CVSS_V3` / GHSA vector, `noeta audit` shows band + re-derived score; text severity is the fallback. ~~(c) per-ecosystem source adapters + pagination~~ ✅ DONE — `src/sources.ts`: OSV api.osv.dev (per mapped package), GHSA GraphQL (`GITHUB_TOKEN`), RUSTSEC OSV feed, paginated, per-source env-gated, dedup by upstream id; `OSV_IMPORT_URL` kept as manual override. **(d) OPEN — design only:** `advisory watch` state is a local file; a shared/attested watch ledger — see the sketch in [`plans/attested-watch-ledger.md`](attested-watch-ledger.md) (recommendation: near-term opt-in committed signed-state file; long-term witness cosigning à la CT/Sigsum; trigger = a second registry operator or a cross-party compliance need) | cut from a shipped arc, kept as inventory rather than tracked: nothing here fires on its own. Trigger: a program that needs one — take it off the list then |
| Hosted edge-platform proof (Fastly/Fermyon Spin) — **guide + script pre-written** (`docs/Edge-Deployment.md`, `examples/edge-hello/`; the `--serve` component verified serving on local Spin 4.0.2 — all routes + real entropy/wall-clock). **Remaining:** an account + one `spin deploy` + screenshot/notes; and Fastly Compute confirmation (unverified — its SDK world differs from the `wasi:http` proxy world we emit) | wasm W4.2. Needs an account; stays a user action |
| **Package the prebuilt `noeta-wasm-serve` component with the toolchain** — the bridge to offline one-click `noeta build --serve`. Today it resolves via `NOETA_WASM_SERVE` → sibling `noeta-wasm-serve.wasm` → on-demand `cargo build` (`wasm32-wasip2`); a binary-only released toolchain has neither of the first two, so `--serve` can't build offline there. Same distribution decision already pending for the AOT runtime + wasip1 runner | wasm W4.2 follow-on. Trigger: an offline or air-gapped `noeta build --serve`, which today needs the component fetched; otherwise before 1.0 |
| Desktop packaging (Tauri); with it the p2p packaging polish (within-feature DCE pruning, capability-gating) | M3 roadmap item; the one roadmap entry the README carries |

## Tooling

| Item | Source / trigger |
|---|---|
| ~~**Two benchmark instruments report a pass over what they never examined.** (1) `xrun3.py`'s tier-1 check runs on **wall-clock**, and on the 2026-08-16 field it put `strcat` 3.0% apart — one tenth of a point from printing "THE JIT IS NOT RUNNING HERE" on a row where instructions retired say the JIT is 1.36x ahead. The guard against a wrong reading is one noisy field run away from manufacturing one, on the very box whose load `RESULTS.md` says makes wall-clock unusable. (2) `measure.sh`'s `[tier-1 decline]` section prints empty for **all four** rows because its op-name grep no longer matches, so it cannot distinguish "nothing declined" from "the grep missed" — the same failure mode as the `--jit-stats` blind instrument this repo has already been burned by twice. Both should read instructions retired, and an empty decline list should be a stated "none" rather than an absence~~ **FIXED (2026-08-16)** — both now read the instrument the rest of this workstream already uses. (1) `xrun3.py`'s tier-1 check is **instructions retired** (min of 5, pinned), signed rather than absolute — a JIT column doing *more* work is never an `ok` row — with the threshold at **5%** and its derivation written into the script: a non-running tier 1 lands 0.88–2.15% apart (0.00% with one binary in both columns) and the smallest real win on this field is 20% fewer instructions, so the band is empty and the number sits in it. Both directions are demonstrated rather than asserted, via `NOETA_JIT_ABLATE=0x7f` (the leaf-op set turned back off, so tier 1 declines the string/map loops for real): declined rows print `THE JIT IS NOT RUNNING HERE` at 0.88/0.98/2.15% while `loop`/`fib` stay `ok` at 9.7–9.8×, and an interpreter-only binary in both columns is 0.00% apart on all five. Every row is corroborated against `--jit-stats` in print, and a contradiction between the two instruments is exit 2. (2) The decline list — in `measure.sh` **and** `xrun3.py`, one parser (`jitstats.py`) so they cannot drift — resolves to one of five *states*: `declined: <ops>`, a stated `none`, `n/a` for a JIT-less binary, and two loud ones for "no report" and "the report shape changed", the last of which exits non-zero and fails `measure.sh` instead of printing an empty column. `jitstats.py --selftest` pins all five against recorded output, including two mutated reports. **Two further blind spots surfaced while verifying and are fixed in the same change**: a missing binary in the JIT column read as a *50,796× speedup* because `perf` happily counts its own process (a non-zero workload exit is now CANNOT MEASURE), and a JIT binary in the *interpreter* column was undetectable (that column is now probed for a JIT once, up front). The wall-clock regression table is labeled indicative-only and points at `measure.sh`; no third wall-clock-decides-a-claim site exists in the harness (`attrib*.sh`, `prof-*.sh` and `measure.sh` were already on instructions). Live contrast under load 11.7: the old wall-clock rule put `strcat` 30.2% apart **with the sign inverted** (JIT 4.75 ms vs int 3.65 ms) on a row where instructions retired have the JIT 20.4% ahead | Found by the 2026-08-16 re-measurement. Trigger: fired |
| ~~**bug** — **a green `noeta check` could be followed by a `noeta test` that does not compile.** The baseline build makes no tier live, so `check` (which mirrored `run`'s front half) never looked inside a `@test`/`@bench`/`@debug` block: `noeta check .` said `0 error(s)` about a file whose `@test` body failed to type-check, and only `noeta test` reported it. `check` is the inner-loop command every project's `AGENTS.md` teaches ("run this before claiming code compiles"), so a whole session's `@test` blocks could accumulate errors nothing reported.~~ **FIXED (2026-07-31)** — the exclusion had **no rationale**: `fe14117` ("`--tier`/`--profile` activate dev-tier blocks before checking, **mirroring the sibling commands**") inherited the empty set from `run`, and the one documented "why" for that set (`noeta-check/src/tiers.rs`) is about keeping the *conformance differential* untouched, not about checking. Stripping is a build decision; checking is not building. `noeta check` now checks each entry once as it ships **and once per code tier its own blocks name** (`noeta_check::code_tiers_in`), which is exactly the shape `noeta test`/`bench`/`<tier>` compiles — one tier per pass, because no build compiles `@test` and `@bench` together and a joint pass would invent collisions between them. Text tiers (`@doc`, any `text:` tier), expression tiers, and a *dependency's* blocks add no pass. Safe by construction on the dependency axis: `noeta test`/`bench`/`doc` load under the **default** dependency selection too (`context.rs` passes `&None`), so a tier pass sees exactly the packages the tier's own verb sees; and a pooled module's tier block is never merged into an entry's program, so only the entry's own blocks activate. Cost measured on `para/ai` (19 files): 3.06s → 3.52s CPU. The summary now names what it looked inside (`checked 19 files (tiers: test): …`), and the JSON report carries `tiers_checked`. Blast radius measured across `para/ai` + its 7 examples, `test-ai`/`test-api`/`test-cli`/`test-p2p`, and this repo's `examples/`+`tests/`: **zero** new diagnostics. Gates: 6 `noeta-cli` CLI tests + 5 `noeta-check` unit tests | found writing a real application (2026-07-31): every `@test` block written during a session was unchecked until `noeta test` ran |
| ~~**the docs gate never validates a `toml` block**~~ ✅ **DONE** (burndown slice 2, 2026-08-12) — `doc_toml_blocks_are_valid_manifests` parses every ` ```toml ` fence through `noeta_pm::manifest::Manifest::parse`; ` ```toml ignore ` opts out a block that is deliberately not a `noeta.toml`. Its first run flagged 11 of 37: eight were a `Cargo.toml`, a Spin manifest or a `noeta.lock` excerpt and are now tagged, and **three were real doc bugs** — `acme/noeta-lint` is not a legal package name (a hyphen is not an identifier), and two `[directives]` examples named a provider their own `[dependencies]` never declared, one of them two lines under the sentence saying that is a manifest error. Original report: so a manifest form the parser *refuses* can sit in the docs indefinitely. `doc_samples` extracts `noeta` fences and ignores every other language by design (its own header says so) — which is exactly how three pages went on teaching `[trust] commands = ["acme/tools"]` after `parse_binding_table` started rejecting the array with a migration message. A reader copying the documented shape got a hard error on a form the docs had just handed them. Fix shape: a sibling gate that feeds each `toml` fence in `docs/` through `Manifest::parse`, opt-in per block (many fences are deliberate fragments — a bare `[trust.commands]` table with no `[package]`), so the ones that claim to be whole manifests are checked and the fragments are marked as such. Cheap, and it closes a class rather than an instance. **A sibling gap, same shape:** nothing checks a doc's `](#anchor)` / `](Page#anchor)` links either — a ~20-line slug check over `docs/` found 3 dead ones (fixed in the same change; `Dev-Tiers#build-targets--noetatoml` was stale in two pages). Worth gating together. Note when writing one: github-slugger does **not** trim, so a heading starting with punctuation (`` `??` — coalesce ``) legitimately slugs to a leading `--`, and a checker that trims reports those as dead | found confirming that tiers/commands/directives are consumer-renameable (2026-07-30); the three stale pages fixed in the same change |
| ~~**the MCP `check` tool and the LSP still see only the shipping shape** — `noeta check` now checks each entry once per code tier its own blocks name, but `noeta_mcp::run_check` and the editor both read `noeta_db::linked_checked`, a salsa query with no tier activation, so a `@test` body's type error is invisible to an agent using the MCP tool and to the editor's inline diagnostics.~~ **FIXED (2026-07-31)** — one tier-aware query family in `noeta-db` now serves both surfaces, so the CLI, the MCP tool and the editor cannot drift: `entry_code_tiers` (the salsa form of `noeta_check::code_tiers_in`) → `tier_activated_from(ws, entry, tier)` → `tier_checked_from(ws, entry, tier)`, with `tier_diagnostics_from` folding the passes together on one dedup key (`noeta_db::diagnostic_key`) that all three surfaces share. One tier per pass; text tiers, expression tiers and a *dependency's* blocks add none — the CLI's rules, unchanged, because they are the same function. The **editor** got the row's preferred shape (a query, not a second check pass) and the narrowing is measured, not asserted: the sweep is its **own** family, so hover/inlay/completion — which read `linked_checked_ide_from` on the same keystroke — pay nothing; a file with no code-tier block runs **no** extra check at all; a tier pass records no `expr_types`; and `tier_activated_from` **backdates**, so an edit inside a `@bench` body leaves the `@test`-activated program identical and salsa skips that check (`editing_one_tier_block_does_not_recheck_another` proves it by counting salsa `WillExecute` events: 2 activations, 1 re-check). Cost, para/cli (4 files, 3 with a `@test` block), median of 3, release: MCP `check` 58.2 → 64.2 ms (+10%); editor recheck after an AST-changing single-character edit 8.36 → 9.39 ms/edit (+12%), and after a whitespace-only edit **no measurable cost** (every shape backdates). Blast radius: `para/cli`, `para/aether`, `para/api`, this repo's `examples/` — zero new diagnostics; this repo's `tests/conformance/tiers` goes 0 → **6**, byte-identical to the six the CLI half found in the same two strip-fixtures, which is the agreement being demonstrated. Gates: 6 new `noeta-mcp` tests, 3 new `noeta-ide` tests, 3 new `noeta-db` tests (all four MCP/IDE trap tests fail on `main`), `noeta-mcp`+`noeta-ide`+`noeta-db`+`noeta-check` suites green, `scripts/gate.sh --quick` clean | found closing the `check`-covers-tiers trap (2026-07-31); trigger: an agent or editor trusting a green check that the CLI would have failed |
| ~~**`noeta mcp` and `noeta lsp` never compose a native dependency's toolchain, so they cannot link any project with `[trust] native`**~~ ✅ **DONE** (audited 2026-08-12) — `cmd_lsp`, `cmd_dap` and `cmd_mcp` each call `compose::delegate_server_if_composed()` before their handshake (`crates/noeta-cli/src/cmd/servers.rs`); it delegates to an already-built composition and never *builds* one, because a language server may not disappear into a multi-minute cargo build at startup. The DAP got it too, with a sharper consequence than analysis: a launch in a `[trust] native` project could not resolve the extension's modules at all. Original report: `noeta check`/`run`/`bench`/`expand` all call `compose::maybe_delegate` and re-exec inside the app's composed binary; the `mcp` and `lsp` subcommands do not, and neither does `noeta_mcp::resolve_workspace`. On `para/ai` and `test-ai` (both `[trust] native = ["para/api"]`) the composed CLI reports `checked 19 files (tiers: test): 0 error(s)`, while the MCP `check` tool reports **19 × E0019 `no module `para` in this project`** and the IDE's `linked_from` fails outright — the entry's `use para.…` cannot resolve because `para/api`'s extension (the `@openapi` expansion hook) is not loaded. Same disagreement class as the tier row above, different mechanism, and much louder: an agent asking `check` about any native-dependent project gets a wall of phantom import errors. Pre-existing and unchanged by the tier work (measured identical before and after). Fix shape: delegate `mcp`/`lsp` through `maybe_delegate_cwd` the way an unknown subcommand already does, or make the extension registry loadable per workspace | found measuring the MCP/LSP tier sweep on `para/ai` (2026-07-31) |
| ~~**fmt safety gate — replace the `Pretty` proxy with a span normalizer + derived `PartialEq`**~~ **FIXED (fmt-structural-gate slice, 2026-08-15).** `noeta_ast::normalize` erases every span in an exhaustive walk and the gate is `a == b` on the derived `PartialEq` — the property itself rather than a rendering that stands in for it. **The failure modes are not symmetric, and that is the whole argument:** a printer that forgets a field makes the gate *blinder* (it approves a rewrite it should refuse — twice, for real), while a walk that forgets a span makes it *stricter* (fmt declines and leaves the file untouched). The new gate can be incomplete; it cannot be wrong. Shaped as a private `Normalize` trait rather than one flat function, so **the type system decides what carries a span**: `Span` zeroes, containers recurse, leaves no-op, and every impl destructures by name with no `..`, so a new field is a compile error at the one site that must consider it. The leaf impls are listed explicitly rather than covered by a blanket `impl<T>`, which would silently swallow a new *node* type. The tier-statics relaxation stops being a line filter over an S-expression and becomes `statics.clear()`. **Option C (a `ZeroSpans` proc macro) was declined**: it would put `syn`/`quote` in `noeta-ast`'s dependency path — everything's build path — to remove a human step whose only possible error is already safe. **Two risks did not materialize, both checked rather than assumed.** The printer canonicalizes decorator order, which under a raw `PartialEq` would have made fmt decline every file with decorators written out of canonical order — but `Decorators` is a struct of named fields, so cross-directive source order was never in the AST; and the whole corpus formats green under the strict gate in all three configs, so no other canonicalization was hiding in the printer either. Tests: the two shipped defects as regressions (`Ok()` vs `Ok`, `@test fn` vs `@test {}`), the tier relaxation in both directions, and `every_span_is_erased_across_the_corpus` — which parses every corpus file, normalizes, and asserts no span survives, ablated by dropping one `Expr::Ident` arm (1,104 files reported). `Pretty` keeps its no-`..` rule for snapshot legibility, which is now its own reason rather than a safety one | pretty-gate survey, 2026-07-31, closed 2026-08-15 |
| MCP: prompts, semantic/embedding retrieval, long-lived analysis sessions, TCP transport | cut from a shipped arc, kept as inventory rather than tracked: nothing here fires on its own. Trigger: a program that needs one — take it off the list then |
| ~~**bug** — `noeta mcp` gives its tokio workers no `thread_stack_size`, so a debug build overflows the stack on ordinary real-world files~~ **FIXED (2026-07-28)** — two mechanisms. (1) `noeta_parser::parse_in` now asks `stacker::remaining_stack()` and offloads to its deep-stack worker whenever the caller has less than `INLINE_PARSE_HEADROOM` (6 MiB) free, instead of trusting a nesting-depth heuristic documented against a "~2 MiB test thread" — measured, **four** nested `if`s in one function overflow 2 MiB in a debug build while the inline threshold allows sixteen levels, so ordinary code aborted the process. This makes the guarantee independent of the caller, which is the half that removes the guess. (2) All three servers now size their threads explicitly (`noeta_parser::SERVER_STACK_SIZE`, 16 MiB): the MCP and LSP tokio runtimes (which covers `spawn_blocking` too) and the DAP's hand-rolled run worker, which ran the whole compile front end on `std::thread`'s 2 MiB default. Measured: each mechanism alone is sufficient for the MCP case. Plus `NoetaMcp::call_tool` wraps every dispatch in `catch_unwind`, so a panicking tool is a JSON-RPC `INTERNAL_ERROR` naming the tool rather than a request the client waits on forever (rmcp spawns each request as a task, so the panic killed the reply, not the server). Gates: `crates/noeta-cli/tests/cli/mcp.rs` (a real `noeta mcp` process over stdio), a `noeta-parser` 2 MiB-thread test, `noeta-mcp` unit tests | found running `reflect` over para/ai from a debug `noeta` (2026-07-28) |
| ~~**bug** — a legal-but-very-deeply-nested file still aborts the process instead of parsing or diagnosing~~ **FIXED (2026-07-29)** — and the row's own premise was false, which is what changed the shape of the fix. Re-measured by binary-searching the abort depth against the worker's stack size: the cliff is **exactly linear** in `DEEP_PARSE_STACK` (16 MiB → 38 levels, 32 → 78, 64 → 158, 128 → past 255), so "raising the worker's stack does not close it — depth 255 overflows a 1 GiB stack too" is wrong; stack size and depth limit are two knobs on one budget, and the old row moved neither far enough. The **red-zone mechanism is real but explains something else**: a debug build costs ~412 KiB of stack per nesting level, six times `chumsky`'s hard-coded 64 KiB `stacker::maybe_grow` red zone, so `maybe_grow` waves the recursion through with far less left than the next level needs and a fresh 1 MiB segment is overrun after two levels — heap segments cannot rescue a debug parse, which is *why* raw stack is the binding resource. Release frames fit inside the red zone, so a release build survives depth 255 on a **1 MiB** stack; the debug/release gap is the whole bug. Also measured: the old limit of 256 was 55–97 levels beyond what the worker could hold (abort between 159 and 202 depending on shape — worst is nested `fn(){}` values at ~412 KiB/level, ordinary `if` nesting ~330 KiB, bare `[[[…]]]` far less). Fix: the three budgets are now one derivation from the one measured constant `STACK_PER_NESTING_LEVEL` (512 KiB = worst measured, rounded up) — `MAX_NESTING_DEPTH` 256 → **128**, `DEEP_PARSE_STACK` 64 → **256 MiB** (4× what the deepest legal parse needs; reserved address space, committed page by page, one worker per file). The same sweep found a **second overflow window on the inline path**: `INLINE_NESTING_DEPTH` (16) needed ~6.6 MiB while `INLINE_PARSE_HEADROOM` asked for 6, so a caller holding *just over* the headroom passed the check and then overflowed (measured: a 6.2 MiB caller aborts on 15 nested function values; 6.0 MiB is safe only because falling short gets it offloaded) — inline range now **8**, which fits the headroom with half again to spare and costs nothing, since the deepest of this repo's 1128 `.noe` files nests 8. Gates: two `const` assertions keep the budgets one derivation; `the_deepest_legal_parse_fits_its_modeled_budget` parses the worst-known shape at the limit on a thread sized to the model with *no* margin, so a grammar change that makes frames heavier is red long before production's 4× margin is gone; `a_caller_just_over_the_headroom_survives_the_deepest_inline_parse` pins the inline half; conformance `diagnostics/nesting_{at_the_limit,too_deep}.noe` pin the language-visible boundary from both sides, generated by `scripts/gen-nesting-cases.py` | measured while fixing the `noeta mcp` stack overflow (2026-07-28); re-measured and fixed 2026-07-29 |
| ~~**bug** — an `else if` chain overflows the parser stack, with no diagnostic and no protection from `MAX_NESTING_DEPTH`~~ **FIXED (2026-07-30)** — the row's numbers were wrong in both directions and the re-measurement is what settled the "pre-pass or grammar, or both" question: **both**, because each half closes something the other cannot. Re-measured on an 8 MiB main thread: the chain aborts the parser at **725–730** branches, not ~330 (~11.3 KiB/branch, not ~25), and `noeta run` — check, IR lowering and the Core-IR interpreter, all walking the same right-nesting on the *caller's* stack — aborts at **740–800**, so the parser was the first wall by only ~40 branches and flattening the grammar alone would have moved the cliff, not removed it. Fix: (1) the `if` grammar is now **iterative** (`repeated()` + a right fold into the same right-nested AST, spans identical) instead of a `recursive` handle descending once per branch — the parser now takes ≥ 8192 branches; (2) the pre-pass counts **chain length per delimiter depth** and reports E0032 past a limit, since the stages after the parse cannot be offloaded. The limit is derived, not chosen: `MIN_PIPELINE_STACK` (8 MiB, the CLI's main thread — the smallest stack a *whole pipeline* runs on; the servers give themselves 16 MiB) over the measured per-branch cost rounded up ⇒ **512** branches, 1.5× under the measured abort. Counting is per depth with a reset on a bare `if`, so sibling chains never sum (the over-counting that would have rejected ordinary code). **The sweep also found a second shape in the same class that this row did not mention**: the conditional *expression* chain `if c then a else if c then b …` desugars to a nested `match` per branch, costs ~4× as much, and aborted **inside the parser at ~200 branches, non-monotonically** (200 and 210 abort, 215–230 survive, 250 aborts) — the `stacker` red-zone signature, and a cliff that is not monotone in the input cannot be bounded by a limit at all. So a long chain is now **offloaded** to the deep worker (`INLINE_CHAIN_BRANCHES`, the chain analogue of `INLINE_NESTING_DEPTH`; a chain is invisible to the depth test, which is why it never was) and its limit is **128** from the downstream cost. Gates: four `const` assertions keep the derivation one; six parser tests (both boundaries, the AST shape and spans, sibling chains, the offload decision, a parse at the limit on exactly `MIN_PIPELINE_STACK`); two generated conformance pairs (`{else,ternary}_chain_{at_the_limit,too_long}.noe`) pin both boundaries end-to-end, the accepting side *running* because the parser is no longer the binding stage. **Still open in this class** (all far away, all recursion-per-token with no delimiter): `.b()` call chains abort at ~42k, `a.b.c…` at ~50k, `1 + 1 + …` at ~85k, `!!!…` at ~90k — per-token costs of 90–200 bytes, so they need a 200 KB+ file, and the pre-pass now has the shape to price them if one ever shows up | measured while re-measuring the nesting-depth row (2026-07-29); re-measured and fixed 2026-07-30 |
| ~~**bug** — nested `match` parses in exponential time~~ **FIXED (2026-07-30)** — the suspected cause was the right one and it was confirmed by measurement before touching anything: a block-bodied arm `=> { … }` is ambiguous between a map literal and a statement block, and `arm_body` resolved it by parsing the brace body **twice** — once as a map (which fails) and once as a block. Nested one level per arm, that is 2^depth. Confirmed by removing the ambiguity rather than by reading the code: the same nesting with a *parenthesized* arm body (`_ => (…)`) or a bare one (`_ => match …`) parses in 0.3 s at depth 30, while the brace form took 4.5 s at 12, 23 s at 14 and 62 s at 16. Fix is the complexity, not a cap: `.memoized()` on the value-expression alternative (chumsky's `memoization` feature, which caches a rule's *failures* at an offset — exactly the fact "a map literal cannot start here" that every level below re-asks). Depth 16: **62 s → 0.02 s**; the shape is linear to the depth limit. No grammar change and no behaviour change — `=> {}` / `=> {"k": v}` are still map literals, `=> { f(x) }` still a block, pinned by a new test. Memoization is applied to that one rule, not to `sub`: a garden path is a property of the ambiguity, and memoizing the whole expression grammar would cost every parse a hash lookup per node. Also swept the other nested shapes for the same pattern — closures, map literals, mixed match/map arms are all flat in time at depth 40, so this was the grammar's only garden path | measured while re-measuring the nesting-depth row (2026-07-29); fixed 2026-07-30 |
| REPL: JIT at the prompt | repl-on-vm follow-on. Trigger: demand |
| Salsa: a deleted file's **fixed-size input struct** cannot be freed — salsa 0.27 has no public input-delete API (append-only input table; `evict_lru`/revision-GC act only on LRU tracked functions). Mitigated, not fully closed: on deletion `noeta_db::release_source` reclaims all *unbounded* content (input text + downstream `ast`/`Sites`/`Module` memos, overwritten with empty-program equivalents) and `WorkspaceCache` reuses tombstoned slots for the next new file, so the input table is bounded by the *concurrent* file high-water mark. Remaining residual: N distinct files created-then-deleted with no later adds leaves N small empty input slots. Closes if salsa gains real input deletion (upstream) or we switch to an interned-path input keyed by a reusable id. | audit F9 residual a. Trigger: salsa exposes a public input-delete API — watchable in its changelog, and nothing here can move before it does |
| ~~IDE treats a **resolved bare variant pattern as a local binding**~~ **FIXED (2026-08-15)** — the IDE resolve now has the checker-sites channel this row was waiting on, and it is **mandatory**: `DefUse::build`, `visible_at`, `semtokens::highlights`, `inlay::type_hints`, `completion::complete` and `callgraph::build` each take a `&noeta_check::Sites`, so no path can silently keep walking the bare AST. `Resolver` holds `&Sites::variant_pattern_sites` and the `match`-arm walk skips any `Pattern::Binding` span the checker recorded there — one predicate at the single place patterns bind, so hover, go-to-definition, find-references, semantic tokens, inlay hints and completion all lose the phantom local together. Every caller already had a checked result at hand (`linked_checked_ide_from` on the editor paths, the `Checked` the call-graph callers already read `expr_types` from, the off-graph re-check in `bare_dot_members`), so nothing needed an empty-bundle escape hatch; a bundle from a *failed* link is empty, which degrades to the old behavior exactly where the workspace is red anyway. Tests: `noeta-ide`'s `a_resolved_bare_variant_pattern_is_not_a_binding` (the phantom), plus `an_unresolved_bare_pattern_is_still_a_binding` and `a_variant_payload_binding_still_binds` pinning the other direction. The language rule itself stays pinned by `control_flow/match_bare_variant_pattern.noe` | variant-patterns arc (2026-07-28); fixed 2026-08-15 |
| ~~**bug** — **test fixtures at machine-global fixed temp paths, still in six crates**~~ **FIXED (fixture-paths sweep, 2026-07-30) — and the helper promoted out of the crate that first needed it, so there is no fourth sweep to do.** `noeta-pm`'s unit tests each built their fixture at a *fixed name* under the system temp dir (`/tmp/noeta_git_forge_test`, `/tmp/noeta_graph_test_editions`, …) and opened by `remove_dir_all`ing it — so every checkout and every concurrently-running test binary on the machine shared one path and deleted each other's tree mid-setup. **Measured rather than inferred, and worse than the symptom suggested**: six concurrent runs of the pre-fix `noeta-pm` lib test binary failed **8–14 tests per run**, union **24 distinct tests** across `git`, `git_forge`, `graph`, `manifest`, `registry`, `sources` and `store` — not the 8 the report named, which were only the ones that lost the race most often. The panic names the resource outright once the assertion carries git's stderr (`git ["init", …]: fatal: cannot copy … to '/tmp/noeta_git_forge_parse_fail_mixed/host/acme/thing/.git/info/exclude'`); a bare `assert!(out.status.success(), "git {args:?} failed")` is what hid it. Also a *false* claim corrected on the way: these did **not** "fail deterministically in isolation" — each passes alone every time, and the failure *count* rises with load, which is the signature that separates shared mutable state from a regression. **Now one crate builds every test fixture path: `noeta-test-temp`** (dev-dependency-only leaf, like `noeta-alloc-probe`), roots at `<target-dir>/tmp/noeta-tests/p<pid>.<binary>/` — derived from `current_exe()`, since cargo sets `CARGO_TARGET_TMPDIR` only for integration tests and benches, never for the `src/` unit tests where most of these fixtures live — plus a per-call counter, `Drop` cleanup, and dead-pid pruning. `noeta-pm/src/test_temp.rs` is a re-export of it. The new piece is `TempPath` (`TempDir::into_child`), for the **second half of the class**: a helper that builds a guard and returns a `PathBuf` pointing inside it deletes the tree at its own `return`. It derefs to the path, so converting a `-> PathBuf` helper touches no call site. **Every surviving site measured before it was touched, since a latent bug fixed without a reproduction is a guess** — six crates, **25 distinct vulnerable tests**, concurrent runs of one test binary: `noeta-cli` lib 4 tests (6 runs failed 0/2/2/2/2/1), `tests/doc_samples` 5 of 6 runs (`write sample: Os { code: 2, kind: NotFound }` — the fixture deleted between `create_dir_all` and `write`), `noeta-ide` **11** tests (16 runs failed 2/0/1/4/3/2/2/2/2/1/2/3/1/3/1/2 — the enumeration listed 5 *sites*; every consumer of the two fixture helpers was exposed, and 6 runs found only 1 of the 11), `noeta-mcp` 3 tests (16 runs, 10 failing), `noeta-prof` 2 (path-dep test 7 of 16 runs), `noeta-host-real` 3 (12 runs failed 3/3/3/0/3/1/3/1/1/3/1/1). After conversion each was re-run at the same concurrency: **zero failures**, up to 16 concurrent runs. Two leads in the enumeration were wrong in instructive ways. (1) `noeta-prof`'s `fixture` looked *safe* — it only `create_dir_all`s and writes, nothing deletes — but `fs::write` **truncates before it writes**, so a run reading the program inside another run's truncation window profiles an empty file: 24 concurrent runs of three short tests caught `program stdout is forwarded verbatim, left: ""`, i.e. the program ran fine and printed nothing because it *was* nothing. (2) `tests/serve.rs` shares a fixed *file*, and a per-process root covers the file and **misses the actual failure**: 4 concurrent `--ignored` runs failed 3, on the fixed **port** 8231, where the losing server cannot bind and dies while the client reports only `Connection reset by peer`. A fixture root cannot fix a port; `noeta_test_temp::free_port()` (bind `127.0.0.1:0`, read the assignment, release) can, and the seven sibling `#[ignore]`d server tests that pinned 8459/8473/8477/8479/8481/8491/8493 took it too. The already-safe list was accurate and was left alone (`noeta-cache`, `noeta-cli/src/cmd/cache.rs`, `noeta-ide/tests/expansion.rs`, `noeta-conformance/tests/ext_directive_seam.rs`, `noeta-pm/src/git.rs`, and the `noeta-cli` integration fixtures on `CARGO_TARGET_TMPDIR`). Do **not** answer a recurrence with a test mutex or `--test-threads=1`: the sharing is between *processes*, so serializing threads inside one binary cannot touch it, and slowing the suite down to widen the losing window is the opposite of a fix. **Two real bugs fell out of the sweep, both hidden by the fixed paths.** (a) `noeta-host-real::fs_append` **silently lost data**: it wrote through a `tokio::fs::File` and dropped it without flushing, and that type buffers — `write_all` returning `Ok` means only that the bytes reached the buffer. With isolated paths `real_host_disk_round_trip` failed 10 of 12 concurrent runs (`left: "hello disk", right: "hello disk + more"`); one `flush().await` fixes it. Nobody had seen it because `/tmp` is **tmpfs**, where the blocking pool's write lands in the page cache before the next statement can observe anything missing — the fixed path was on a RAM disk, hiding a data-loss bug as well as being one. (b) `hot_serve` and `hot_live` had been **failing on `main` in isolation**, unnoticed: their embedded `.noe` fixtures predate the rule that a named function does not implicitly see a top-level binding, so both died at startup with `E0005: cannot find \`count\` in this scope` — piped to `/dev/null`, leaving only `server did not accept within 4s`. Fixed per the diagnostic's own help (`fn fetch(req: Request) use (count)`), and the L3 showcase is a live gate again. That is what `#[ignore]` costs: nothing runs these unless asked, so they rot. Gates: two clean `cargo test --workspace --no-fail-fast` runs at 168 suites / 2852 passed / 0 failed (a third run was needed: one intervening run lost the two `bench --baseline` tests to the load-induced zero-measurement wart in the row below, which is unrelated to fixtures and was chased down rather than shrugged at), 1087 conformance + full differential agreement, `cargo build --workspace` at 0 warnings, `cargo +1.97.0 clippy --workspace --all-targets -D warnings` and `fmt --check` clean | pm-test-isolation slice (2026-07-29), then the fixture-paths sweep (2026-07-30). Third and final occurrence of this class: the CLI integration fixtures (which once left 169 stray `/tmp/noeta_cli_test_*` directories), `noeta-pm`, then these six crates — each of the first two rolled its own helper, which is why there was a next time. New tests take a fixture from `noeta-test-temp` rather than building a path |
| ~~**bug** — `noeta bench --save-baseline` can persist a baseline of `0.0`, which then compares against nothing, silently~~ **FIXED (2026-07-30)** — the diagnosis in this row was exact; the fix is the one it argued for, in three parts. (1) The `.max(0.0)` clamp is gone: a two-point subtraction that did not resolve (`t(2N) <= t(N)`) is now a **bench failure** with a reason — "measurement did not resolve: … the body may be too fast to time, or the machine too loaded; raise `--iterations`" — because a per-iteration cost of zero is not a measurement of zero, it is the absence of one. (2) `save_bench_baseline` **refuses** to write a non-positive entry (naming the bench) *and* refuses to write a baseline with no entries at all — the same silence by a different route, and it wrote one before. It writes nothing when it refuses, so there is no half-saved baseline. (3) At compare time a `--baseline` that produced no delta says which comparison it could not make and why — a missing entry (a bench added since the baseline was saved) or a stored zero from an older version — in the human line and as `baselineNote` in `--json`, so a delta consumer can tell "unchanged" from "never compared". Swept for other readers of the clamp: there is no profiler or second report reading it, and the only other `.max(0.0)` in the tree is the playground's JS clock. Four unit tests in `cmd/bench.rs` pin all three parts | fixture-paths sweep (2026-07-30), found running the gates; fixed 2026-07-30 |
| ~~**The pid-qualified fixture paths are collision-free but never collected: 10,258 stray directories / 136 MB on `/tmp` right now, ten days' worth.**~~ **FIXED (test-temp-isolation slice, 2026-07-31) — and the row was right about the leak while missing the sharper defect underneath it.** A pid-named path is safe from the collision and solves nothing about accumulation, as the row says; what it did not say is that a pid-named *file* dropped straight into `/tmp` is not isolated at all, because **the loader links the entry file's sibling directory as the project**. `noeta-runner`'s `temp_bundle` wrote its fixtures there, and the runner compiles any non-bundle file as `.noe` source — so `treats_a_non_bundle_file_as_source` failed with `[E0019] no module para.aether in this project` over `/tmp/c.noe`, `/tmp/dn129.noe`, `/tmp/et.noe` and a dozen more scratch files a sibling agent session had left behind. Nine of nine pass under `TMPDIR=<empty dir>`, which is the proof that none of it was the test's own doing. The hazard is Noeta-specific: a bare temp fixture that would be harmless in another language is not harmless in a language whose project boundary is a directory. **Nineteen sites converted to `noeta_test_temp::TempDir`/`unique_path`** — `noeta-runner` (tests + `compile`), `noeta-wasm-runner`, `noeta-dap`, `noeta-loader/derive`, `noeta-conformance/ext_directive_seam`, `noeta-ide/expansion`, `noeta-cache`, `noeta-cli` (`cmd/cache`, `compose`, and the serve / hot-reload / impact-watch integration fixtures), `noeta-wasi-host` — retiring four hand-rolled pid+counter helpers and every `remove_dir_all` cleanup they carried. `noeta-runner`'s `a_fixture_is_alone_in_its_directory` now asserts the property directly (the fixture's directory holds exactly the fixture, and is never the system temp dir), so a future edit cannot quietly move them back, and the fix was proven by rerunning the suite with a deliberately broken `.noe` file planted in `/tmp`. Two `temp_dir()` call sites in test code are deliberately kept: `noeta init`'s git test must run *outside* a git worktree and the target directory is inside one, and `noeta-conformance`'s wasm differential workdir is harness runtime holding only bundles. The remaining production sites (`cmd/native`'s AOT scratch, `cmd/ide`'s staging, `pm::git::authorship`'s blobless clone, the wasm workdir) all delete their own trees and are correct to use the system temp dir — they are the user's processes, not the test suite's. | fixture-paths sweep (2026-07-30), measured while checking fixture hygiene; closed by the test-temp-isolation slice (2026-07-31) after the accumulation it predicted turned into a *failing* `noeta-runner` suite on a machine running several agents |
| ~~IDE hover asymmetry for `@validated`~~ ✅ **DONE** (audited 2026-08-12) — the `Validated \| Tier => None` arm is gone: every built-in directive's hover prose now comes from the one `BuiltinDirective::info()` metadata table, so a directive without a `doc` does not compile. `@tier` was the second silent case (excused as hovering "through `hover_tier`", which only walks tier *blocks*). Pinned by `every_builtin_directive_hovers`, which iterates the closed set rather than a hand-written list, so a new directive is covered the day it is added | w8 directive-enum refactor (2026-07-19) |
| ~~**bug** — **`noeta bench --max-regress` reported success on a run that measured nothing**, and the whole `bench::` CLI family was chronically flaky under load~~ **FIXED (bench-flake slice, 2026-07-31)** — one root, three symptoms, and the fix people expected was measured and rejected. The root is the two-point estimator: `(t(2N) − t(N)) / N` assumes the fixed per-run overhead is identical at both points, and CPU contention breaks that premise by handing the two points different amounts of CPU — inverting the difference, which the `.max(0.0)` clamp then reports as `0.0`, the **absence** of a measurement. Symptom (a): `--max-regress -1000`, a deliberately impossible limit, **passed** — no measurement ⇒ no delta ⇒ nothing above the limit ⇒ exit `0`, byte-identical to "measured, and fine". Symptom (b): `--save-baseline` correctly *refuses* an unresolved measurement (the row above), so the two baseline tests died on their setup step with exit 2. Symptom (c): `bench_directory_baselines_stay_keyed_per_entry_file` asserted on the substring `vs gate`, which the *failure* note (`no comparison vs gate: …`) also contains — a loose assertion that passed on exactly the runs it existed to catch. **"Give the fixtures more work" does not fix this, and 40 samples per body under 3× oversubscription say so:** ~50 ms of work per point failed 2/40 and ~100 ms failed 3/40, i.e. doubling the wall time (4.2 s → 8.4 s per measurement) moved the failure rate not at all — the noise is *multiplicative*, so a bigger body scales signal and noise together. Repetition is the only lever: `noeta bench` now **retries a non-resolving subtraction up to three times** (`BENCH_MEASUREMENT_ATTEMPTS`), taking a few percent to a few parts in ten thousand, and the note stops mis-advising ("…or measure on a less contended machine" is now one of the three fixes it names). **Product decision on the gate:** `--max-regress` exits **2** — this command's existing "could not do what you asked" — when any bench produced no comparison at all, because a gate is read by exit code precisely so nobody reads stdout, and `0` there converts an unmeasurable benchmark into a standing green tick. `1` stays "measured, and a benchmark regressed"; a plain `--baseline` run is a *report*, prints why each comparison did not happen, and stays `0`. `--json` gained `ungated`. Fixtures are sized only for what sizing does buy — clearing the additive floor an idle machine still has (`work(500)`'s ambient samples spread 3×, `work(2000)`'s 1.6×) — and no further. Docs: `docs/Benchmarking.md` (exit-code table, "When the gate cannot measure", the retry in "How timing works"). Gates: three full `-p noeta-cli --test cli` runs at 287/287, a fourth under 24 spinning burners at 287/287, **25 consecutive runs of the `bench::` family under the same saturation at 15/15 each** (375 executions, zero failures), conformance corpus 7/7, `clippy -p noeta-cli --all-targets -D warnings` and `fmt --check` clean. Family wall time unchanged (5.6 s for 15 tests, was 5.2 s for 14) | found rerunning `bench::` reflexively; two full-suite runs failed a *different* pair of members each time (2026-07-31) |
| **The `noeta docs` retrieval ratchet can be decided by a 0.07% score margin, so an unrelated wiki edit flips it.** `guide::tests::retrieval_answers_the_relevance_set` pins top-1 accuracy at 16/22. One of those 16 was "string interpolation", won by Language-Tour at 6.50896 against Extending-Tiers at 6.504135 — a gap of 0.07%, decided by the corpus-wide average document length. Adding two paragraphs to an unrelated page (Native-Extensions) moved avgdl enough to reverse it, and the ratchet failed on a change that had nothing to do with retrieval. The instance is closed the right way — Syntax-Basics, the page that teaches the feature, now calls it by its name, so it wins that query at 12.4 against 6.3 rather than by luck — but the class is open: any other near-tie in the relevance set has the same property, and the next one will surface as a red test on a docs commit that did nothing wrong. Wanted: a check that reports the *margin* of each top-1 decision, so a knife-edge is visible before it flips, and the pages behind it can be fixed on their merits | found adding a paragraph to `docs/Native-Extensions.md` (2026-08-16). Trigger: the second time this fails on an unrelated docs edit |

## Performance

**Not before someone profiles user-shaped programs.** The five rows below are held, deliberately, and none is a quick win: they are a JIT loop-shape recognizer, monomorphic specialization, interning the checker's core `Type` tree, VM inline caches plus an ABI-facing argument projection, and a map-key probe adapter. Each is substantial, and — more to the point — none has a measurement that says which one is worth doing. Their triggers used to read like conditions ("a workload demonstrating its value", "a keyed-lookup profile") while nothing in the project measures user workloads: the instructions-retired ratchet guards the VM against *regression*, which is a different question and cannot fire any of these. So each now states the same real condition rather than a profile nobody runs, and any of them may be picked up early on a specific measurement that names it.

| Item | Source / trigger |
|---|---|
| ~~**A top-level read-modify-write copies the whole map, and the spelling decides.**~~ **FIXED** — the receiver's stale reference is released at its last use, so the in-place guard sees the sole owner it really is. The cliff was never about maps: a top-level binding lives in a global slot, so reading it materializes a *retained copy* into a register, and in `m[k] = m.get_or(k, 0) + 1` that copy sat dead in a register the statement never read again while `TakeGlobal` moved the map out into a different one — refcount 2, guard declines, O(entries) copy per iteration. Which register the copy landed in was **coalescing luck**: the named spelling happened to fuse the two, so its load's release-on-overwrite killed the stale reference and `wordcount.noe` was fast by accident. `regalloc::release_stale_global_loads` makes it explicit — for each `TakeGlobal`, a backward scan of the same basic block emits `Op::Drop` for a `LoadGlobal` of that same global whose register liveness proves dead. Measured, 20k iterations, 500 entries: inline key **11,548.8M → 85.5M (135x)**; 50 entries 981.3M → 82.7M, so the O(entries) scaling is gone (83.2M at 500 against 82.7M at 50, flat); `Map<int,int>` 7,249.4M → 51.6M. Genuine aliasing still copies, measured: with `t = m` live across the update the same program stays at 11,574.8M → 11,575.9M, and `t = s; s = s ~ "y"` is unchanged at 295.2M. `wordcount`, `assoc`, `loop`, `loop_fn` and the ratchet's `map` fixture compile to **byte-identical bytecode**, so no headline number moved. Conformance: `gc/global_self_update_aliasing`, `gc/global_self_update_destructor` | Found by the 2026-08-16 re-measurement; fixed 2026-08-16 |
| ~~**The same algorithm is 2.83x slower at top level than inside a `fn`.**~~ **FIXED** — a top-level binding nothing outside the top level can reach by name now lives in a register of `main`'s frame, so the top level and a `fn` body compile to the *same* instructions. The gap was never the loop: a top-level binding *was* a global slot, so the body spent four `LoadGlobal` and two `StoreGlobal` per iteration where the function-scope twin read and wrote registers, and tier 1 compiled those as memory traffic rather than keeping the value in a register across the loop. `TopLevelStorage` + `ModuleCompiler::promotable_top_level` decide it per binding — including the one reference that names no site, a free-function `invoke(name, args)` resolving a runtime string against the global-name table, which cost a real backend divergence before it was caught; the decision is *verified* after the entry chunk compiles (a promoted name that acquired a global slot is a broken analysis, and asserts). Measured, matched pair, instructions retired, min of 7: **1,143.2M → 392.9M (2.91x)** on the tier-1 loop, against 389.7M for the identical loop inside a `fn` — a 2.90x gap closed to **1.01x**; interpreted, 560.6M → 552.5M against 552.8M in a `fn`, a 1.014x gap closed to **0.999x**. Ratchet: `loop_jit` −66.6%, `arith` −1.43%, `map` −4.8%, `version`/`startup` flat, every tier-1 expectation held. Conformance: `bindings/top_level_storage`, `gc/top_level_storage_destructors`, `functions/sealed_fn_cannot_read_top_level`, `reflection/invoke_free_value_binding` | Found by the 2026-08-16 re-measurement; fixed 2026-08-17 |
| **Kernels in Noeta** — JIT/AOT loop-shape recognition over packed lists, so `for p in ps { total += p.r }` compiles to the tight buffer loop a native kernel is; demotes the native-kernel ABI to an escape hatch | Held, and the 2026-08-16 field says so rather than assuming it: **this appears nowhere in the profiles** of the only benchmarks Noeta is behind on. Trigger unchanged — a workload demonstrating its value. Packed lists are not exercised anywhere in the field |
| **P-MONO** — monomorphic shape specialization (generics are erased-for-storage) + reflection cross-`dyn` element recovery (`List<int>`'s `int` after a `dyn` boundary) | Held, and the 2026-08-16 field says so rather than assuming it: **this appears nowhere in the profiles** of the only benchmarks Noeta is behind on. Trigger unchanged — a real workload showing erased-for-storage cost. The benchmark set has **no objects at all**, so this run is silent on it rather than negative |
| `TypeId` interning in `noeta-types` (`Type` is a plain owned tree) | Held, and the 2026-08-16 field says so rather than assuming it: **this appears nowhere in the profiles** of the only benchmarks Noeta is behind on. Trigger unchanged — a checker profile showing clone cost. The 08-16 run is runtime-side and neither supports nor refutes it; empty-program `check` is 3.05M instructions and per-sibling cost is flat |
| Inline caches for extern-type methods; borrowed-arg projection for registry dispatch (owned `NativeValue::Str` clones per call) | Held, and the 2026-08-16 field says so rather than assuming it: **this appears nowhere in the profiles** of the only benchmarks Noeta is behind on. Trigger unchanged — a hot extern-method workload. No extern-type method appears in any measured profile |
| ~~Zero-alloc map-key probe adapter (`Equivalent<MapKey>` + canonical hash off object slots)~~ **ALREADY BUILT — the row described shipped work.** `impl Equivalent<MapKey> for str`, `ExternKeyRef` and `Vm::map_probe` all exist and read with a borrowed `&str`; the canonical-hash half is for packed/object keys no measured workload exercises. **What is real is the other half, now with a number:** `Set` materializes an *owned* `MapKey` on every call, including the 199,500 of 200,000 iterations of `wordcount` that merely overwrite — `map_update_key` is **23%** of that benchmark. Narrowed to: probe before building the owned key on `Set`. | Re-measured 2026-08-16 at `57091de12` (`scratch-bench/xlang/RESULTS.md`), on a quiet box, instructions retired. Trigger: fired — the 23% is the measurement |
| ~~VM-throughput next-gap investigation — the disassembly-driven inventory of remaining PHP-gap slices~~ **DONE (2026-08-16).** It ran, and its finding is that the gap is not where the list said. Maps remain the only benchmark family behind (assoc **2.3×**, wordcount **3.0×** the PHP baseline) and **tier 1 is genuinely running on them** — 1.35–1.37× the interpreter, corroborated by OSR-window counts, `perf` seeing JIT frames on anonymous exec pages, and the ratchet asserting `native`. But the gap is a **string-and-allocation** gap, not a hash-table one: 48% of assoc is *building the key* (`alloc_with` 25.1%, `push_str` 16.3%, `itoa` 6.5%) against ~11% for insert/get/hash, and the same benchmark over `Map<int,int>` drops `alloc_with` off the profile entirely. The two rows this spawned are below and both outrank everything left in this section | Re-measured 2026-08-16 at `57091de12` (`scratch-bench/xlang/RESULTS.md`), on a quiet box, instructions retired, closed 2026-08-16 |

## Stdlib follow-ons (all trigger-gated)

| Item | Source / trigger |
|---|---|
| Crypto: blake3; argon2/scrypt (needs the algorithm-tagged verify design); AEAD encryption (AES-GCM / chacha20-poly1305 — only WITH a key-management story) | cut from a shipped arc, kept as inventory rather than tracked: nothing here fires on its own. Trigger: a program that needs one — take it off the list then |
| HTTP client residuals — **what is left after re-reading the list**, which is most of the value in this row. Shipped since it was written: the per-request **timeout**, **streaming bodies** (the `stream(…)`/`Frame` surface), and **retry** configuration; `hmac_sha256`/`hmac_sha512` ship as crypto primitives. Genuinely still cut: **redirect** configuration, a true per-client **cookie jar** (the `Cookie` *type* shipped with the cookie arc — automatic storage across requests did not), and **request-signing helpers** built on the HMAC primitives. | http arc scope cuts. Trigger: a program that needs one — take it off the list then. **Re-read before trusting it**: three of the five items this row listed had shipped without it being touched |
| ~~**`std.session`** — the stateless signed-cookie codec + `Session` request/response pair~~ ✅ DONE — shipped as S2/S3 of the cookie arc: `keyring`/`encode`/`decode` + `open`/`attach`, verify-before-parse, mandatory `exp`, rotation keyring, 4096-byte hard error, required `secure` argument | cookie arc S1 tail |
| Manifest `optional` / `[suggests]` dependencies — a package declaring a dependency an app opts into. Rings are the wrong layer (native-payload DCE, unknown to `noeta-pm`); `Dependency::Scope` covers the `para/aether` + `para/db` case today, and the diagnostic is the better discoverability surface | http-sessions S4 design question. Trigger: a second use case that `Scope` does not cover |
| ~~**bug** — the subprocess doors abort where every failure is remote-input-shaped~~ **FIXED (2026-07-28)** — the recoverable twins landed as `os.try_spawn(cmd, args?): Result<Process, OsError>` and `Process.try_write(text): Result<void, OsError>`, with the aborting doors kept unchanged (that pair is the language's shape, and removing a half would break scripts that want a missing program to stop the run). `OsError` is a registered `std.os` extern type declaring `Error` + `Display`, with `message()`/`kind()` — `not_found`, `permission_denied`, `broken_pipe`, `stdin_closed`, `other` — classified from the real `io::Error` **at the source**, so the check/write race is reportable as the distinct condition it is instead of a message to sniff. The `Os` seam was re-rooted rather than extended: `os_try_spawn`/`os_proc_try_write_stdin` are the required primitives and the aborting `os_spawn`/`os_proc_write_stdin` are *defaults derived from them*, so the two doors of each pair report the identical message by construction. The sandbox grew the model the differential needs — the scripted `dead` command spawns a child whose stdin is broken from the start, and `close_stdin` genuinely closes — so both `Err` kinds are reachable in-oracle. Measured after: `os.try_spawn("definitely-not-a-real-binary-xyz")` → `Err`, `not_found`, program continues; `try_write` to an exited child → `Err`, `broken_pipe`, program continues; both aborting doors unchanged. `ABI_VERSION` 7 → 8. Conformance: `std/os_try_spawn`, `std/os_try_write`, `std/os_write_dead_aborts`. Docs: `docs/Error-Handling.md` (a new "Aborting and recoverable doors" section naming the whole pattern) | found building para/ai's MCP stdio transport (2026-07-28) |
| Raw TCP/UDP (`std.net`) for non-HTTP protocols — needs a scripted-peer deterministic sandbox model (harder than the HTTP responder) | http-server design question. YAGNI until a concrete protocol |
| ~~Kernel bundles: `IVec2`/`IVec3`, `f64` vectors, `Color`~~ ✅ **DONE** — three of the four had already shipped without this row being struck, as **structural bindings rather than named types**: `impl vec.Kernels for IVec3 {}` binds any uniform `@packed` shape of two or more `i8`…`u64` components, the same binding serves an `f64` vector, and `impl vec.SatKernels for Color {}` gives saturating `u8` channel math (`tests/conformance/std/{ivec,dvec,color}.noe`). What outlived them was the **operation set, not the widths**, and that is what the closing slice fixed: the bundle carried `add`/`sub`/`scale`/`min`/`max`/`abs`/`dot`/`length`/`normalize`, while `cross`, `distance`, `lerp`, `clamp` and `reflect` existed only as the free `f32` `vec.*` functions — so "the same operations at double precision" was half true and an `f64` or `i32` vector could not take a cross product or interpolate. All five are bundle methods now, one generic body per op over the `Scalar` element trait, which gained the promoted-domain rungs (`from_float`, `float_from_f64`, `float_add`/`float_sub`/`float_mul`) they needed. Two narrow their domain by what the operation means rather than by width — `cross` needs exactly three components, `reflect` a float element, each refused by name — and `distance`/`lerp` compute in `Self::Float`, so an unsigned difference cannot wrap and an integer interpolation keeps its fraction, closing with the float-to-integer rounding rule (nearest, half away from zero, saturating) rather than the lane ops' wrap. The `f32` methods are bit-identical to their free-function twins, pinned in the corpus. Conformance: `std/vec_geometry` plus `std/vec_geometry_{cross_arity,reflect_integer,operand_types}`. Docs: `docs/Native-Extensions.md`, `docs/Fixed-Width-Integers.md` | kernel-methods arc scope cut; the three bundles closed by the array-ops + scalar-unification arcs, the operation-set gap 2026-08-16 |
| **`Transform` (TRS)** — a translation/rotation/scale triple with composition and point/vector application. **Cut deliberately, and it should not come back as a `std` bundle.** Every other member of this surface is *structural* — a rule over field kinds and arity, so the user names the type — and a TRS is three sub-values with different roles that no shape constraint can distinguish from any other ten-float struct; it would have to be the first nominal type here. Worse, TRS is not closed under composition: with non-uniform scale and rotation the combined linear part is a general 3×3 matrix no translation/rotation/scale triple can represent, so a `compose` either silently approximates or drags in a `Mat4` and a decomposition — a graphics-math library, not a language stdlib. The primitives a program needs are already present: `quat` composes and applies rotations, `vec.Kernels` translates and scales. | Trigger: a program that wants TRS composition **and can state which convention it needs** (multiply order, whether scale precedes rotation, what happens to non-uniform scale). Then it lands as a package with a `Mat4`, not in `std` |
| ~~**`regex.compile` has no recoverable twin.**~~ **FIXED (2026-07-29)** — `regex.try_compile(pattern): Result<Pattern, string>` ships alongside `compile` in `REGEX_FNS`, on the `json.parse`/`json.try_parse` model exactly. The module was **re-rooted** on it rather than merely extended: `regex::try_compile` is the primitive and the aborting `regex::compile` is *derived* from it (`try_compile(p).map_err(|m| StdError { message: format!("`regex.compile`: {m}") })`), the same discipline the `os.try_spawn` row above established and `json.parse` keeps over `json.try_parse` — so the two doors cannot report different things about the same pattern, and the abort text is unchanged byte for byte. The `Err` side is a plain `string`, not a classified extern error: a pattern has exactly one way to be invalid, so a `kind()` would be a constant, and the engine's caret-and-span diagnostic is the whole value (it rides verbatim and unprefixed; the aborting door adds its own name, as `json.parse` does). The dispatch never uses the seam's `Err(StdError)` channel — that channel *is* the abort — returning the whole `Result` inside the `NativeOut`, so both backends materialize one tree. The aborting door is untouched and stays: a script that wants a bad pattern to stop the run should not have to write a `match`. `ABI_VERSION` 10 → 11. Conformance: `std/regex_try_compile` (valid pattern, malformed pattern with the engine's own text, lookaround/backreference through the same door, program continues); `std/regex_invalid_pattern` still pins the aborting half. Rust unit tests pin the derivation itself — the abort message *is* the recoverable message under the prefix. Docs: `docs/Error-Handling.md` (a fourth row in the doors table, plus what decides whether an `Err` side is a type or a string) | para/ai phase 5 (`Redact`), 2026-07. Trigger: a redaction/routing rule loaded from config |
| ~~**`std.tracing` exposes no `current_span`.**~~ ✅ **DONE** (audited 2026-08-12) — the *capability* shipped, deliberately not in the shape this row proposed. `set_attribute(key, value)`, `add_event(name)`, `add_event_with(…)` and `record_error(message)` are free functions that annotate the **active** span — the one the caller is inside, which no handle names — returning `bool` for "a live active span received it". Not a `current_span() -> ?Span`, because materializing the ambient span would hand out `.end()` on a span the caller never opened: a `with_span` body could end the span `with_span` is about to end, and a handler could end the auto-instrumented SERVER span out from under `http.serve`. So para/ai's span *event* on the enclosing span is expressible today. Original report: `span`, `with_span`, `span_from` and `current_context` are all there (`crates/noeta-stdlib/src/tracing.rs`), but a module handed no span has no way to add an event or an attribute to the one that is open — so "annotate the enclosing span" is not expressible from a library that does not own it. para/ai's guard telemetry wanted a span *event* on the run's `invoke_agent` span (DESIGN §9) and emits a short child span instead: context propagation nests it in the same place and it carries attributes natively, so the divergence is cosmetic, but the event form is the one the OTel conventions are written in. Wanted: `tracing.current_span(): Span`, the read half of the context `current_context` already exposes | para/ai phase 5 (guard telemetry), 2026-07. Trigger: a second library wanting to annotate a span it did not open |
| ~~**The two unrecoverable process doors**~~ ✅ **DONE** (subprocess-doors arc; audited 2026-08-12) — both recoverable twins shipped: `os_try_spawn` and `os_proc_try_write_stdin` (`crates/noeta-stdlib/src/host.rs`), failing into `OsError`. Verified at the language surface: `os.try_spawn("definitely-not-a-real-binary-xyz", [])` returns `Err(spawn: cannot start …: No such file or directory (os error 2))` rather than aborting, so para/ai's PATH-resolution and `try_wait`-before-write workarounds in `mcp.noe` can go | para/ai phase 6, restated phase 5 |

## Design proposals (no slice yet)

- ~~**Vulnerability-intake trust model**~~ — DECIDED + built (advisory-intake arc, 2026-07-19): three tiers (operator-issued / scope-owner self-service with keyless provenance / OSV-GHSA-RUSTSEC import via operator-curated name map), a public report queue that only becomes an advisory on operator/owner promote, and `noeta advisory watch`. See the advisory-intake residuals row above for what's left.
- **Packed-field-kind enum dedup** — four phase-appropriate `PackedKind` encodings; revisit only if a shared public layout vocabulary earns its keep across the package boundary (the `PackedView` ABI may already be that vocabulary — check before building).
- **WASM revisit conditions** (recorded, not planned): direct wasm codegen only on perf data; wasm-threads isolates only on multi-core edge demand; p2p-in-browser is its own arc.
