# Backlog — everything open, in one place

The single registry of work we have said we will do and have not yet done. It merges the old
`deferred.md` (slice deferrals), the old design-proposal backlog, and the open tails of every
completed arc directory (deleted from `plans/` once shipped — their full slice histories and
design sketches live in git history).

**Discipline:** when an arc defers something non-gate, add a row here in the same commit. When a
slice or arc picks an item up, strike the row and point to the work that closed it. Each row names
its **source** and, where useful, a concrete **trigger** — the condition that should make us do it,
so we neither do it prematurely nor forget it. Items marked *(active)* are worth picking up on their
own; everything else is trigger-gated.

Nothing here is a correctness gap in shipped behavior unless explicitly marked **bug**.

## Language & type system

| Item | Source / trigger |
|---|---|
| ~~**bug** — `?` on a `Result` inside a `void` function silently discards the error~~ **SHIPPED (2026-07)** — the deliberate call was **both**: reject it where a return type exists, abort where none does. A `?` on a `Result` in a function whose declared return is neither a `Result` nor deferring is now **E0012** — the same code, span, and shape as the `Option` half (`check_try_error` no longer bails on a non-`Result` return; its help names the fix). Where there is no declared return to check — top-level code, a `dyn`/inferred return — an `Err` that reaches the top **aborts** with the error's `message()` and a non-zero exit, the new **E0069** `UnhandledError`, raised by both backends through one shared builder in the catalog. Measured after: the `void` repro is E0012 at `noeta check` (exit 1, was 0 errors/exit 0); the top-level repro prints `before` then aborts E0069 with the transport message at exit 1 (was silent at exit 0). Blast radius was **7** corpus cases, not the 6 predicted — `std/http_options.noe` also carried an `async fn run(): void { …? }` — plus the `docs/Concurrency.md` sample; all rewritten to declare the `Result` their `?` needs. Conformance: `results/try_result_return_type`, `results/try_unhandled_at_top_level`, `results/try_result_deferred_return`. Docs: `docs/Error-Handling.md` (both halves as one rule) | stream-status session (2026-07) |
| **Payload-carrying variant as a first-class constructor** — `map(Shape.Circle)`. `some`/`Ok`/`Err`/`panic` are first-class callables (poly-values F3) but a *user* enum's payload variant is not: it is a clean `E0007` in value position (`enums/payload_variant_is_not_a_value.noe`). It previously type-checked and then died in the backend; the guard closed that, and making it *work* needs a variant-constructor value form both backends construct identically (`Fn(payload…) -> Enum`), the enum analogue of `Op::LoadNativeFn` | stream-status session (2026-07), found via the now-located `internal error`. Trigger: real code wanting to pass a variant constructor to a higher-order function |
| **`Members`/`DynamicCall` protocol dispatch** — registered in the trait table (`get(name)` / `call(name, args)`) but with no behavior: no language construct consumes dynamic member access today (`obj.f(args)` field-calls, `Callable`, and `next`-driven iterators all shipped without it). Deliberately deferred rather than shipping speculative machinery | M1.8b tail (the last unwired protocol names). Trigger: a concrete consumer — a dynamic-proxy/ORM-style use case, or reflection-driven member access |
| ~~**Generic-method call-site-typed forwarding**~~ **SHIPPED (generic-forwarding arc, 2026-07)** — a generic METHOD's own type parameter now forwards into a call-site-typed position (`json.try_parse::<T>`, `attributes_of::<T>`, `type_name::<T>()`) inside a method body. **The measurement above was right about the obstacle and wrong about the fix.** The obstacle was real: five entry points, four name-keyed with no static receiver type, all binding positionally — so a *prepended parameter* corrupts arity on four of them. But the answer was not a trailing defaulted parameter; it was to stop putting type arguments in the value-argument list at all. `Rvalue::Call`/`Rvalue::Method` (and `Op::Call`/`Op::CallGlobal`/`Op::CallMethod`) carry a dedicated `type_args` operand, and `Func::hidden`/`Chunk::hidden` say how many leading slots it fills; value parameter positions no longer shift, so the four dynamic entry points simply supply *nothing* and abort naming the callee (`no_instantiation_message`, the precedent `generics/class_type_param_unknown_instantiation.noe` set), while a checked call by name fills them. The predicted costs mostly evaporated with the premise: `supplied` now survives verbatim at a forwarding call (it used to be discarded — a *pre-existing* wart this removed), `params_of` never saw `$ty` (it reads the reflection artifact, not the closure), and the JIT's direct-call path falls back rather than needing default-filling. What it did cost: `Chunk::hidden_base`, because a method's receiver sits at register 0 *ahead* of the hidden block while an associated call passes that receiver as an ordinary argument. Conformance: `generic_methods/method_forwarding`, `generic_methods/method_forwarding_unreachable`; `generics/class_type_param_shadowed` and `reflection/type_name_diagnostics` turned from refusals into answers. **Still deferred, and re-measured 2026-07-29 — the boundary is narrower than this row claimed:** transitive forwarding through a member call is deferred only for a **compound receiver** (`self.inner.load::<T>(x)`, `f().load::<T>(x)`). `self.other::<T>(x)` — the very spelling written here — *works*, and so do `s.load::<T>(x)` (a local) and `Store.load::<T>(x)`: all three parse as the module-call atom `recv.func::<T>(args)` on a **bare identifier**, which is the pre-pass's recipe consumer, so the caller gets its slot and the call passes it through. Verified end to end (`generic_methods/forward_receiver_bound_local.noe`). What the pre-pass genuinely cannot do is name the callee behind a receiver that is itself an expression, so *that* stays refused — with an accurate diagnostic now (see the E0058 row below) whose help names the local-binding route that does work. A second, narrower refusal shares the seam: a bare-name member turbofish whose callee's own slot is a **composite** (`h.mid::<T>(xs)` against `mid<V>`'s `List<V>` slot) — the pre-pass registers the turbofish type as written, not the callee's substituted template, because substituting it means resolving the receiver | poly-deferrals arc D3 (2026-07); re-measured and shipped in the `generic-forwarding` arc, 2026-07; boundary re-measured 2026-07-29 |
| **Reflection-intrinsic forwarding** — `roles_of::<T>`/`from_bytes::<T>` with a forwarded type parameter stay clean checker errors (composite `List<T>`, nested-`fn`, `attributes_of::<T>`, forwarding-fn-as-value and now `type_name::<T>()` all shipped). The **name-keyed** turbofishes (`field_specs_of::<T>`, `variants_of::<T>`, `construct::<T>`) also stay errors, but are no longer a dead end: their turbofish arm is a compile-time key by design, and their runtime-string arm is now reachable from inside a generic body as `field_specs_of(type_name::<T>())` — the E0058 help says so where the route is open. Making the *turbofish* spelling forward too is pure sugar over that and is one site each: `lower_type_operand` (`crates/noeta-ir/src/lower.rs`) is a single choke point, so it would emit the existing `Rvalue::TypeSlotName` as the name operand instead of a `Const::Str`. Each remaining case is an additive extension of the same hidden-slot machinery | poly-values arc F2b (2026-07). Trigger: a reflection-driven generic wanting per-`T` role/decode lookup |
| **Trait-method generics** — a trait's required-method set stays monomorphic: a per-method `<U>` on a trait method is a clean `E0058` (poly-deferrals D3 pinned this — the trait is dispatched dynamically, so each `impl` would have to agree on the method's own parameters). Generic methods live on concrete `class`/`struct`/`enum` types. Supporting them needs per-impl method-parameter unification at the dispatch seam | poly-deferrals arc D3 (2026-07). Trigger: a trait abstraction wanting a method-scoped type parameter |
| ~~**bug** — the E0058 forwarding diagnostic misdescribed the language~~ **FIXED (2026-07-29)** — one message served every way a call can reach the forwarding seam with no slot to pass through, and it was false in all of them: it claimed "call-site-typed forwarding is supported in top-level generic functions only" (generic methods have forwarded since the generic-forwarding arc — in the very repro that raised it, `Store.load<T>`'s own `json.try_parse::<T>` compiles — and the *inferred* case fires from inside a top-level generic `fn`), and its help advised spelling an explicit turbofish that two of the three reaching spellings already write. The fix is that **spelling**, not the resolved callee, is what the syntactic pre-pass's reach is a property of: a new `ForwardSpelling` (`Inferred` / `Turbofish` / `CompoundReceiver`) rides beside the `hidden_site` key from the five producers into `check_generic_call_seeded`, and the seam answers each in its own terms. `Inferred` keeps the turbofish help, which is the real fix there and only there. `CompoundReceiver` says the receiver is typed by checking while the slots are computed before it, and helps with the route measured to work — bind the receiver to a local, and the call is spelled on a bare name again. `Turbofish` (a bare-name member turbofish against a callee whose slot is a composite) names the missing slot and gives **no** help, there being no route to point at; a help line that repeats what the source already does is worse than none. Conformance: `generic_methods/forward_compound_receiver` (the refusal), `generic_methods/forward_receiver_bound_local` (the help's route, end to end). Wording is pinned where conformance cannot reach it — `noeta-check`'s new `forwarding_refusal_gallery` snapshot renders all three. Docs: `docs/Generics-and-Traits.md` (“Forwarding `T`” gained the bare-name-spelling boundary and the local-binding sample) | found reading E0058 against the shipped generic-forwarding arc, 2026-07-29 |
| **Editions S3/S4** — the first real edition-gated behavior (S3, pending a deliberate language divergence) and edition-aware diagnostics + a `noeta fix` migrator (S4, depends on S3) | editions arc. Trigger: a breaking language change we want to ship |
| ~~Validating-constructor hook for typed JSON decode~~ **SHIPPED (validation arc, 2026-07)** — the `Validate` built-in trait (`validate(): Result<void, E>`, `E` a `string` or an `Error`), auto-run **bottom-up** at every recipe door: `json.parse::<T>` (aborts, E0007), `json.try_parse::<T>`/`decode_typed` (recoverable `Result.Err(JsonError)` with `field[i]: <msg>` path), and `from_bytes::<T>` (packed, aborts at `[i]`). Plus `@validated` static construction channeling — outside-the-impl literal/record-update construction is E0060, forcing a validating constructor. Docs: `docs/Validation.md`. Conformance: `tests/conformance/validation/`. **Deferred within-arc:** `@validated` on an `enum`/`trait` is silently dropped rather than a misplacement diagnostic (structs/classes are the only literal-constructed types); dev-tier (`in_dev_tier`) does not bypass E0060 (a white-box test builds through the constructor). Trigger for the enum/trait diagnostic: a user hitting the silent drop |
| **Multi-source `From` impls on one target** — `impl From<JsonError>` + `impl From<IoError>` on the same `AppError`. Barred today by construction: impl-block methods flatten into the type's method table by NAME (no overloading), so a second `From` is a coherence conflict (E0027); supporting it needs source-typed dispatch for `from` (overloading or mangled per-source methods) — a new dispatch category. The `?`-conversion checker side (`try_conversion_sites` keyed by resolved source) is already shaped for it | error-ergonomics arc (2026-07). Trigger: a real pipeline funneling ≥2 error types into one wrapper |
| **`From<Source>` as a generic bound** (`<T: From<int>>`) — `From` impls are recorded with their instantiation, but the built-in bound checker (`satisfies`) is name-only; an instantiated *built-in* bound needs the user-trait bound machinery (`TraitBound` args) extended to built-ins. Explicit `Target.from(x)` calls and the `?` position work today | error-ergonomics arc (2026-07). Trigger: first generic helper wanting to construct over a conversion |
| ~~**bug** — a bare `Ok(v)`/`Err(e)`/`[]`/`{}`/`none` **assigned to an already-annotated `mut` binding** is E0023~~ **FIXED (2026-07-28)** — the un-annotated `Stmt::Binding` arm now **resolves the name before it checks the value**, exactly as the entry named. When the name resolves to a binding whose type is *resolved*, that type is the expectation, and it is fed into `check` for the literal forms that can absorb it (`Checker::absorbs_expectation`, kept beside the absorbing arms of `check_inner` it mirrors: `[]`/`{}`/`none`/`some`/`Ok`/`Err`, a target-typed `.{ … }`, and a closure against an `Fn`). `E0023` is now understood as a *binding-site* diagnostic: it fires for a fresh binding, and for a reassignment whose target is still an unresolved hole (`mut acc = []` then `acc = []` refines nothing), never for one whose type is already known. Both must-not-regress rules hold: an expectation the value cannot absorb is *not* flowed, so the tailored `E0007` that names the binding and offers the union stays the single report on a mismatch, and nothing here introduces a binding. Bonus: `mut p: Point = …` then `p = .{ … }` (the same bug for a target-typed literal, not in the entry) now works, and `mut xs: List<int> = [1]` then `xs = Ok(3)` reports once instead of E0007 *and* a spurious E0023. Conformance: `bindings/reassign_annotated_mut_literal`, `bindings/reassign_annotated_mut_mismatch` | found writing para/ai's MCP client (2026-07-28) |
| Const generics → explicit SIMD (`Simd<T, N>`) | bitwise Tier P. Trigger: user-code SIMD demand the columnar/autovectorization path can't meet |
| ~~**bug** — **`construct(name, fields)` does not type-check a nominal field's value**~~ **FIXED (enum-construction arc, 2026-07-29)** — `plan_construct`/`plan_construct_named` validated a supplied value only when `enforced_scalar(spec.ty)` answered `Some`, so a field declared as a struct/class/enum was unchecked. Both planners now run one shared `check_field_value`, so the positional and named forms cannot accept different things, and a nominal mismatch is the same clean `Err` a scalar mismatch already was (`` field `inner` of `Outer` expects Inner, got map ``). **Measured worse than this entry recorded:** after `.as<Outer>()` narrowing, `type_of` on the bad field reported `Type.Struct(Inner, [])` — the *declared* type — so the value was not merely wrongly stored but actively misdescribed, which is why it survived so far from its cause. An enum-typed field took a bare `int` the same way (`Holder {m: 7}`). Names are compared **kind-agnostically** (a declaration reaches reflection as `Type.Named(Inner)`, a value classifies as `Type.Struct(Inner)`; both qualify consistently under a `namespace` — measured), and generic arguments are deliberately not compared since they are erased on some runtime paths. A `dyn Trait` field has its own repr, so an implementor is never false-rejected. Fixed here rather than deferred because the enum-payload construction shares this exact code path. Conformance: `reflection/construct_nominal_field_checked` | para/ai phase 5 (structured output + struct-typed tool parameters), 2026-07 |
| ~~**No runtime door builds an `enum` value from JSON — `variants_of` closed only half of the gap it was asked for**~~ **FIXED (enum-construction arc, 2026-07-29)** — all four doors open, on one principle: **an enum is constructible from the same data its schema advertises.** (1) `TypeRecipe` gained an `Enum` form (`VariantRecipe`/`VariantTag`), so `type_to_recipe` stops answering `None` and `@derive(Deserialize<Json>)` accepts an enum-typed field; (3) came with it — `json.try_parse::<T>`/`json.parse::<T>`/`decode_typed` all walk that one recipe. A **backed** enum decodes by its BACKING (`enum Plan: string { Free = "free" }` decodes `"free"`, and refuses `"Free"`), a **plain** enum by its CASE NAME — exactly what each derives as its `{"enum": […]}` schema. Backings of any scalar type work, so an `int`-backed enum decodes from JSON numbers. (2) `construct("Enum.Variant", payload)` builds a case: the variant is named where the type name goes (as in source, and as the attribute manifest already keys a variant target) and `fields` is the payload, in either of `construct`'s two existing shapes — validated by the very planners a struct's fields go through, against the very `List<FieldSpec>` payload schema `variants_of` reports. A bare enum name is a teaching rejection. (4) `Enum.try_from(v)`/`Enum.from(v)` now match **backing first, then case name**, so the recoverable door finally reads what comes off a wire; the aborting door is kept. `variant_for_wire` owns that rule for both backends, and the compiler orders `Op::EnumFromStr`'s cases by declaration index so an ambiguous probe resolves the same way in both. **Validation holds at the door:** a value naming no case is a path-carrying `JsonError` of the new `unknown_variant` kind listing every accepted value — never a panic, never a silently-wrong value; an enum implementing `Validate` re-enters like a struct. An enum with a **payload-carrying** variant still declines *as a whole* (a data-carrying sum has no canonical JSON spelling, and decoding its payload-free half would accept documents against a schema that cannot describe the type) — that case is built by `construct` instead. `ABI_VERSION` 9 → 10 (`TypeRecipe::Enum`, plus `NativeOut::Variant`'s new required `has_validator`). Conformance: `enums/decode_plain_enum_field`, `enums/decode_backed_enum_by_backing`, `enums/from_backing`, `enums/from_backing_panic`, `reflection/construct_enum_variant`, `reflection/construct_enum_rejections`. Docs: `docs/Derives.md` ("Enum-typed fields"), `docs/Attributes-and-Reflection.md` ("Constructing an enum case"), `docs/Structs-Classes-and-Enums.md` ("Converting a wire value to a case"), `docs/Validation.md`, and `std.json`'s registry prose | para/ai phase 5 (DESIGN §6/§8.5's U-6 tail), 2026-07 |
| ~~**Name-keyed reflection forwarding for `type_name::<T>()`**~~ **SHIPPED (2026-07)** — the premise held exactly: the hidden type-argument slot already carried the instantiation's name in the same `TypeArgInfo` entry `attributes_of::<T>()` has read since F2b, so this needed a name-only consumer in the forwarding pre-pass, a `dynamic_type_name_sites` map, and one new `Rvalue::TypeSlotName`/`Op::TypeSlotName` in place of the folded constant. No recipe is involved, so it forwards for instantiations that have none (an enum). Fixed a live bug on the way in: the table interned the instantiation's **`Display`** form, which renders the SHORT name, while every name-keyed registry is keyed on the linker's qualified name — so a forwarded `attributes_of::<T>()` under a `namespace` silently answered the empty list. It now interns `Type::head_name`, the lattice twin of the `TypeRef::head_name` the static form folds to. Boundaries preserved: bare parameters only (`type_name::<List<T>>()` heads at `List` and stays folded), unpinnable instantiation still E0023, forwarding-fn-as-value still E0058, and a generic METHOD's own `<U>` still E0058. Consequence for consumers: a generic body can now name its own `T`, so `field_specs_of(type_name::<T>())` answers the real per-instantiation schema and the doubled call-site name (`Output.of(type_name::<Extraction>())`) is no longer forced. Conformance: `poly_values/forward_type_name`, `poly_values/forward_type_name_namespaced/`, `poly_values/forward_attributes_namespaced/`, `reflection/type_name_diagnostics` (rewritten onto the method case, which is what still holds) | para/ai phase 5, 2026-07 |
| Range-*checked* narrowing conversions (`checked_to_u8(): u8?`) alongside the wrapping casts | bitwise W4 note. Trigger: first fallible-narrowing use case |
| E7 — editor injection for `${…}` holes inside foreign-language expr-tier bodies | expr-tiers, gated on the text-tiers editor grammar slices |
| ~~**bug** — a `use` inside a tier block resolves an attribute's name but does not qualify it, so the attribute silently does nothing~~ **FIXED (2026-07-28)** — `noeta-loader` now builds a **per-tier-block overlay** next to each unit's rewrite tables (`qualify::UnitMap::tier_scopes`, keyed by the block's span) and `qualify_stmt_scoped` swaps it in while that block is qualified. So `@test { use std.test.{Skip}  #[Skip("…")] fn f(…) }` resolves to `std.test.Skip` and the runner skips `f`, matching the top-level-`use` and inline-FQN spellings, while the import stays **block-scoped** (the same name outside the block is still the E0029 "cannot be used as an attribute") and stays **inside the block** (a rewrite table, not a hoisted import — an inactive build drops the `use` with the block and leaves no dangling import). Top-level blocks only: a block in *statement* position is code, not a file scope, and a `use` there was and remains a loud `E0005`. Gates: `tests/conformance/tiers/block_scoped_use_strips.noe`, two `noeta test` CLI tests, three `noeta-loader` unit tests | found writing para/ai's `#[Tool]` test fixtures (2026-07-28) |
| ~~**bug** — four tests fail on `main` in extension/session registry scoping~~ **CLOSED (2026-07-29), and the row was wrong about the diagnosis.** The four were `noeta-embed/tests/ext_constraint_enforcement.rs` `an_extension_tier_site_restriction_is_enforced` (`unknown directive @audit`), `instance_registry.rs` `hot_swap_checks_against_the_session_registry` and `the_checker_scopes_the_tier_namespace_to_the_session_registry`, and `noeta-ext-abi/tests/constraint_fields.rs` `every_classified_field_has_a_live_reader`. **They do not "fail deterministically in isolation"** — measured, each passes alone, its whole test binary passes, and its whole crate passes. They failed only under whole-workspace concurrency, and the failure *set* grew with parallel pressure: one workspace run failed 12 tests across 5 suites, two competing runs failed 21 suites and pulled in a test neither failed alone. A set that widens under load is shared mutable state, not a regression — which is why "not bisected to a commit" was never going to succeed. Two independent fixes closed them: `eee54fd45` "resolve an ext tier globally when no binding regime is in effect" (another slice, landed 51 minutes *after* this row was written), and `9d14cd76a`, the `noeta-pm` fixture sweep — these four were collateral from machine-global fixed-name fixture paths that concurrent test binaries `remove_dir_all`'d out from under each other, so fixing one crate's fixtures turned all twelve green. Verified: two full `cargo test --workspace --no-fail-fast` runs on merged `main`, 166 suites, 2847 passed, 0 failed, all four green by name. **The row's closing argument was right even though its diagnosis was wrong** — a red suite on `main` does stop the next agent telling its own breakage from inherited breakage, and it cost exactly that here: two agents were briefed to hunt a deterministic regression that did not exist. The deeper miss is that `cargo test --workspace` was not in the gate set at all, so this surface went unwatched while conformance, the differential oracle, examples, doc samples, clippy and fmt were all green; it is in the gate set now | found by the tier-block `use` slice running the full suite as its gate (2026-07-29); re-measured and closed the same day |
| ~~**bug** — **the prelude enums are invisible to `variants_of` / `field_specs_of`**~~ **FIXED (2026-07-29)** — all five now answer the pair the way every other enum does, and so does every **native** (`ExtEnum`) enum, which the row guessed at and nobody had measured: `variants_of("std.http.Framing")` was `[]` on the very identity `type_of` stamps on its values. `reflect::build` walks the program AST and neither kind is in it, so both are seeded by `extend_reflection` from the tables every other consumer already reads — the shared `reflect::prelude_enums()` projected through a new `reflect::prelude_type_infos()` (payload types via `PreludeFieldTy::repr`, the reflection twin of the checker's `prelude_field_type`, so `Type.List`'s payload reports `Type.Enum(Type, [])` and not a hole), and the registry's `ExtEnum`s through `extension_enum_types()` (payload types via the existing `sig_type_to_repr`; a **backed** native enum's per-variant constant rides through as `backing`). Seeded under the guard the extension attribute shapes already used — *the program's own declaration wins* — so a user's `enum Ordering` still shadows, in a REPL session too. Both backends read one accessor (`ReflectionInfo::variant_specs`/`field_specs`), so the differential agrees by construction. **Two more bugs found standing in this one, both fixed here.** (1) A **leaf**-imported native enum's short name (`use std.http.{Framing}` → `variants_of::<Framing>()`) answered `[]` right after the program imported the type: the loader deliberately aliases only attribute structs there, so the turbofish folded to a name nothing is registered under. `noeta_ir::lower` now resolves it from the program's own `use` statements (`native_enum_imports` — import-driven, so a program's own `Framing` is never redirected to a native type it never mentioned), and `type_name::<T>()` folds through the same helper, since its whole job is to produce the key the others consume. A *dynamic* operand is untouched: `variants_of("Framing")` still means the literal name. (2) The **tree-walker REPL** (`noeta_eval::Session`) never re-embedded after `accumulate`, where the VM's `SessionCompiler` always has — so `variants_of("Ordering")` was 3 on one REPL and 0 on the other, and the same asymmetry had been hiding the *extension attribute* shapes from the tree-walker REPL since they were introduced. `session_parity` covers it now. **`Op::EnumFromStr`'s baking stays, and the reason is measured, not assumed:** its `cases` carry an interned `Shape` id beside each name and backing, and only the compiler can mint one — the artifact makes the *data* recoverable at run time (same table, same declaration order, case for case) but a dynamic path would additionally need runtime shape interning, so the baking is now redundant as data and still load-bearing as shapes. **The other queries, checked rather than assumed:** `attributes_of` reads this same `TypeInfo` table and was already seeded; `traits_of` reads `trait_impls`, which `native_trait_impls` already joins over `registry.enums()` — and a prelude enum declaring no trait is the correct empty, not a hole; `roles_of` reads role *bindings*, not type declarations. `params_of`/`returns_of` do have a native blind spot, but a different one — its own row below. Gates: 1083 conformance (0 failures), differential in full agreement, `noeta-cli` examples + doc samples, `cargo test --workspace`, clippy, fmt. Conformance: `reflection/prelude_enums_reflect`, `reflection/variants_of_native_enum`, `session_parity::prelude_enum_reflection_agrees_across_entries`. Docs: `docs/Attributes-and-Reflection.md` ("The prelude enums are ordinary enums", both halves) | found measuring `Enum.try_from`'s baked-case table during the enum-construction arc (2026-07-29): the VM cannot resolve a prelude enum through reflection, which is why `Op::EnumFromStr` still bakes its cases |
| **bug** — **`params_of` / `returns_of` report every NATIVE callable as nonexistent.** `params_of("std.math.sqrt")` is `[]` and `returns_of("std.math.sqrt")` is `none` — and `returns_of`'s `none` is documented to mean *this target names no known callable*, chosen deliberately over folding into a `void` so that a typo'd target is distinguishable from a real one. A shipped stdlib function is therefore reported as a typo. Cause is the prelude-enum row's exactly: `extend_reflection` seeds *types*, and nothing seeds `ReflectionInfo::params` from the registry, so a callable with no AST declaration has no `ParamRecord`. Fix shape: project each `ExtFn` (`params`, `ret`, and `param_names`) into a `ParamRecord` keyed by the module's qualified target (`std.math.sqrt`), through the `sig_type_to_repr` that already exists for the type side. One thing to measure first: shipped `ExtFn`s mostly leave `param_names` empty, so the *names* a DI framework injects by would come out blank — the row is only fully closed once those are filled, and reporting `""` for a real parameter name would be its own silently-wrong answer | found measuring the adjacent queries while closing the prelude-enum reflection row (2026-07-29) |
| **bug** — **a native fielded type's fields are invisible to `field_specs_of`** (`field_specs_of("std.http.Frame")` is `[]`, and with `variants_of` also `[]` that is the pair that reads as *I know nothing about this name*). `extension_attribute_types` seeds only the `ExtFielded`s that are `@attribute`s; a native **value struct** or **class** is skipped, though its `ExtField` list carries exactly the `SigType`s the attribute arm already projects. Fix shape: widen that arm to all of `reg.fielded()` (+ `ExtStruct`). **Deliberately not done in the prelude-enum pass, for a reason worth keeping:** a seeded `TypeInfo` is also what makes `construct(name, …)` resolve, so seeding a native **class** would make `construct("std.http.Client", {})` mint an extern-handle class with *no handle* — the slice has to decide whether `construct` refuses a native class (it should) before the reflection half lands. The struct half has no such hazard | found measuring native reflection while closing the prelude-enum row (2026-07-29) |
| **bug** — **the prelude *structs* are invisible the same way the prelude enums were**: `field_specs_of("FieldSpec")` and `variants_of("FieldSpec")` are both `[]`, for all eight of `Attributed`, `RoleBinding`, `ParamInfo`, `FieldEntry`, `FieldSpec`, `VariantSpec`, `TierRoot`, `TierText` — the very types a reflection consumer walks *while* reflecting. The fix is **not** symmetric with the enum one, which is why it was split off rather than folded in: `reflect::prelude_structs()` carries field NAMES only, because the checker's lattice is not visible to `noeta-ast`, so each struct's field *types* are hand-listed at its `register_prelude_struct` call site. Seeding names with `Type.Dyn` types would be worse than seeding nothing — a wrong type travels, an absent one is caught. Clean fix: carry `TypeRepr`s in `PreludeStruct` and derive the checker's lattice `Type` *from* them (a `repr → Type` projection; every type used is in the closed set `string`/`bool`/`dyn`/`List`/`?`/`() -> void`/`Type`/the abstract `Enum` kind), which also deletes the hand-written lists and the drift they invite — the same move `prelude_enums` made for the enum half | found measuring while closing the prelude-enum reflection row (2026-07-29) |
| **the conformance single-file harness never LINKS**, so anything the loader's rewrite tables decide is invisible to it: it lexes, parses and checks, and `noeta_loader` is only on the *directory*-case path. Measured concretely — `variants_of::<http.Framing>()` (the group-import spelling) answers correctly under `noeta run` and answers `[]` in the harness, because the loader's `add_native_type_aliases` never ran. Not a language bug, but it means a whole class of behavior can only be pinned by a directory case or a doc sample, and a single-file case asserting it will pass or fail for the wrong reason | found writing the native-enum reflection case (2026-07-29) |
| ~~**bug** — a `use` inside a tier block resolves a **std** name but not a **package** name, and `noeta check` says nothing. Minimal, three files, no dependencies: package `probe/lib`, `side.noe` declaring `pub struct Thing`, and `entry.noe` whose whole body is `@test { use probe.lib.side.{Thing}  fn t(): void { x = Thing { n: 3 } } }`. `noeta check entry.noe` reports **0 errors**; `noeta test entry.noe` fails the test with `cannot find type probe.lib.side.Thing in this scope`. Both import forms fail (`use probe.lib.side.{Thing}` and whole-module `use probe.lib.side` + `side.Thing`), the same `use` at **top level** works, and the same shapes over **std** work inside the block (`use std.math` → `math.abs(-3)`, and `use std.http.{Frame}` → a `Frame` literal, both pass). So the tier-scope overlay above qualifies the name correctly and the module is never **linked**: a std module resolves through the registry, a package module has to be in the unit graph, and the graph is built from a unit's *top-level* imports before tier activation hoists the block's.~~ **FIXED (2026-07-29)** — the **full linking** fix, not the check-time diagnostic: `link_core`'s entry loop now drives a top-level tier block's own `use`s through the same `drive_use` a top-level `use` goes through, against a block-scoped name table (the unit's, plus the block's own declarations). Driven **unconditionally**, not per active tier — which tiers are live is the build target's call, taken downstream in `noeta_check::tiers`, and the linked program is one memoized salsa value shared by `check`/`run`/`test`, so making linking tier-dependent would have been the restructure this slice was told to avoid. Merging unconditionally is safe because every merged declaration lands under its **qualified** identity: it binds no short name, collides with nothing, and is unreferenced (hence stripped with the block) when the tier is inactive — exactly what a top-level `use` that only `@test` references already does. The unresolved remainder is **dropped** rather than retained, so the `use` stays *inside* the block and an inactive build still drops it with the block, leaving no dangling import. Both properties the sibling fix established therefore hold unchanged: the import is block-scoped (the same name outside is still E0029/E0005) and is not hoisted. Only the **entry**'s blocks are driven — a pooled module's tier block is never merged into the program (a `TierBlock` is not a qualifiable declaration), so only the entry's blocks can ever activate. A side effect worth having: an import naming no such module inside a block is now an **E0019 at check time** with a "did you mean", where it used to check clean and fail at every use site under `noeta test`. Gates: `tests/conformance/tiers/block_use_links_package` (inactive half — output unchanged, both backends agree, nothing hoisted) and `tests/conformance/tiers/block_use_unknown_module` (the load-bearing half, verified to fail on the pre-fix loader by ablation), three `noeta test` CLI tests, three `noeta-loader` unit tests | found writing para/ai's phase-7 AG-UI suite (2026-07-28), where a `@test`-only `use para.ai.mock.Mock` checked clean and failed every use site at `noeta test` |

## Concurrency & runtime

| Item | Source / trigger |
|---|---|
| App-lifetime `TaskScope` patterns: DI-managed workers, durable queues, schedulers; overlaps the "background-work extensions" proposal below | §7.2 design. Framework/extension patterns, not language constructs |
| Safepoint GC inside **re-entrant runs**: mid-run collection is gated to the outermost interpreter loop (a nested run's outer register stacks live in Rust locals the poll cannot enumerate), so cycles built inside `map`-applied closures, `NativeCtx`-driven loops (the HTTP serve loop), or a single mammoth un-awaited task body still accumulate until the next outermost safepoint / exit. Rooting the outer stacks (an active-stack registry) would lift the gate | memory-management 6.x follow-up (safepoint GC shipped; this is the residual scope) |
| ~~**A blocking child read parks the whole scheduler**, so a *synchronous* API cannot bound a `Process.read_line` without killing the child~~ **CLOSED by the awaitable twins (2026-07-28)** — `Process` now has one for **every** blocking read, not just the one the entry named: `read_line_async()`, `read_err_line_async()`, and `read_async(n)`, each `Future<?string>`, alongside the existing `wait_async`. All three, deliberately — the entry's reasoning is about the *blocking* read parking the isolate, and stderr and the count-read park it identically, so a chosen subset would have left the same hole under a different method name (`read_err_line` argued explicitly rather than defaulted). They ride the `ExternIo` seam: one new `Os::os_proc_read_spawn(handle, ProcRead)` builds the descriptor, whose default resolves through the blocking methods at spawn (deterministic in the sandbox, in-oracle) and which `RealHost` overrides with a `RealBody::Blocking` body over the child's shared stream buffer. The read cursor moved *into* `SharedStream`, under the buffer's own lock, so a sync read and an async read on one child share a position by construction rather than by copy-out/write-back (which an off-thread body has nowhere to write back to). Measured on a child that speaks after 3 s: `read_line()` held the isolate **3.07 s**; `race([p.read_line_async(), deadline(300)])` returned `none` at **308 ms** and the next statement ran 5 ms later. **Left open on purpose:** items (3) and (4) of the entry — `h.cancel()` on an isolate parked in `sleep` reporting `Err(Cancelled)` while the isolate runs on, and `concurrent { … }` being E0040 in a synchronous `fn`. Neither is a subprocess concern and neither is closed by this; they want their own look. Conformance: `async/proc_read_async`. Docs: `docs/Concurrency.md` (a new "Streaming a subprocess" section) | found building para/ai's MCP stdio transport (2026-07-28) |
| Intrusive free-list registry — closes the trace collector's ~10% acyclic overhead on alloc-churn micro-benches | memory-management 6.4 |

## Reactivity & web

| Item | Source / trigger |
|---|---|
| OTEL: **observable/async instruments** (`ObservableCounter`/`ObservableGauge` with pull callbacks) — sync `counter`/`up_down_counter`/`histogram`/`gauge` shipped; callback-driven instruments deferred | native-otel metrics-logs, §deferred |
| OTEL: **histogram views / custom buckets / delta temporality** — every histogram uses the OTel default explicit bounds and only cumulative is emitted (the `Temporality::Delta` variant exists but is unused); custom bucket config, per-instrument views, and delta export deferred | native-otel metrics-logs, §deferred |
| OTEL: **metrics cardinality limits** — no per-metric attribute-set cap today (unbounded `BTreeMap` per instrument); a hard cardinality-limit policy slice deferred | native-otel metrics-logs, §deferred |
| OTEL: **stdout/structured-logging bridge** — `std.log` records go to the OTLP sink only; mirroring them to stdout (a `print`-bridge) is a separate decision, deferred | native-otel metrics-logs, §decision 3 |
| Synced **store** (a whole reactive dataset, the §9.12 merge point) and `.history()`/time-travel over the p2p append log | p2p "later/open" R&D |

## Database (para/db)

| Item | Source / trigger |
| --- | --- |
| **Per-dialect migration overrides** — v1 runs each migration body verbatim in the target dialect's native SQL (write portable SQL). A `migrations/postgres/` (or `sqlite/`) sub-directory shadowing a base file for a divergent backend (e.g. `SERIAL` vs `AUTOINCREMENT`), selected by the connected driver's dialect. `load_dir` already ignores sub-directories, leaving room for this | migration engine (aether DB6, 2026-07-19). Trigger: a project genuinely needing to target both SQLite and Postgres from one migration set |
| ~~**A native package's own `.noe` cannot be checked standalone** — `noeta check packages/para-db/…` failed at `use para.db` (E0019)~~ ✅ FIXED — `resolve_graph` now adds the **root** package's own `package.native` as a `NativeCrate` (no self-trust gate, like cargo and your own `build.rs`), so `compose::maybe_delegate` composes it; a self-contained module of a native package (`query.noe`, `sql.noe`) now checks clean standalone. **Still open, and separate:** a file that imports a *sibling* module checks the sibling's body *without the sibling's own `use` imports*, so `reactive.noe` (imports `repo.noe`) still reports false errors in `repo.noe`. This is the single-file-in-a-multi-module-package limitation — NOT native-specific, pre-existing, orthogonal to the compose fix | native-compose half fixed 2026-07-22; sibling-body half *(active)* — trigger: wanting `noeta check <pkg-file>` to be a full-package gate |
| **Out-of-order migration detection** — the runner applies pending files in filename-sort order and gates history integrity via checksum + deleted-file checks; it does not warn when a *newly added* migration sorts before an already-applied one (a rebase inserting an earlier prefix). Timestamps make this rare; a warning (not a hard error, to keep legitimate merges unblocked) could flag it | migration engine (aether DB6, 2026-07-19). Trigger: an out-of-order incident in practice |

## Packages, registry & distribution

| Item | Source / trigger |
|---|---|
| **Publish the toolchain + registry repos** — ✅ the para half is DONE locally: all 7 `para` packages extracted to standalone repos at `/home/niklas/Code/para/` (para-html, para-cli, para-api, para-db, para-aether, para-aether-db, para-p2p; each tagged v0.1.0, pre-publish `file://` git-dep form) and removed from the monorepo. The remaining half: publish the toolchain repo (local `main` is hundreds of commits ahead of `origin/main`; publishing is a deliberate user decision) and flip the packages' git URLs from local paths to public repos | para out-of-tree follow-on *(the keystone item)* |
| Dynamic extension loading (the dyn dispatch tables exist; every compiled-in extension monomorphizes past them) | higher-order-abi. Trigger: a plugin that can't be compiled in |
| TUF-based Sigstore trust-root refresh (today: build-time-embedded root + env override); registry-side keyless *requirement* is a policy flag away | keyless-signing v-next |
| Per-dependency **capability enforcement** (a dep's `[trust]` grant actually bounding what it can reach) — research phase; static effect analysis is the tractable first step | package-manager phase-4 L3 |
| Git-deps of *published* packages aren't expressible in the index `Dep` shape | package-manager v-next |
| **Advisory-intake residuals** (arc landed 2026-07-19: three-tier feed — operator/publisher/imported — + public report queue + `noeta watch-scope`; merged both repos, unpushed). ~~(a) client verb to **promote a report**~~ ✅ DONE — `noeta advisory promote <report-id>` (operator via `NOETA_REGISTRY_ADMIN_TOKEN` → operator advisory; scope owner → keyless publisher advisory, prefilled from the report) + `noeta advisory reports [--scope S]`; new `GET /v1/reports/{id}`. ~~(b) **CVSS-vector scoring**~~ ✅ DONE — CVSS v3.1 base-metric equations both sides (`src/cvss.ts` / `noeta-pm::cvss`, unit-tested vs published vectors); import derives the band from an OSV `CVSS_V3` / GHSA vector, `noeta audit` shows band + re-derived score; text severity is the fallback. ~~(c) per-ecosystem source adapters + pagination~~ ✅ DONE — `src/sources.ts`: OSV api.osv.dev (per mapped package), GHSA GraphQL (`GITHUB_TOKEN`), RUSTSEC OSV feed, paginated, per-source env-gated, dedup by upstream id; `OSV_IMPORT_URL` kept as manual override. **(d) OPEN — design only:** `watch-scope` state is a local file; a shared/attested watch ledger — see the sketch in [`plans/attested-watch-ledger.md`](attested-watch-ledger.md) (recommendation: near-term opt-in committed signed-state file; long-term witness cosigning à la CT/Sigsum; trigger = a second registry operator or a cross-party compliance need) | advisory-intake arc (2026-07-19) |
| Hosted edge-platform proof (Fastly/Fermyon Spin) — **guide + script pre-written** (`docs/Edge-Deployment.md`, `examples/edge-hello/`; the `--serve` component verified serving on local Spin 4.0.2 — all routes + real entropy/wall-clock). **Remaining:** an account + one `spin deploy` + screenshot/notes; and Fastly Compute confirmation (unverified — its SDK world differs from the `wasi:http` proxy world we emit) | wasm W4.2. Needs an account; stays a user action |
| **Package the prebuilt `noeta-wasm-serve` component with the toolchain** — the bridge to offline one-click `noeta build --serve`. Today it resolves via `NOETA_WASM_SERVE` → sibling `noeta-wasm-serve.wasm` → on-demand `cargo build` (`wasm32-wasip2`); a binary-only released toolchain has neither of the first two, so `--serve` can't build offline there. Same distribution decision already pending for the AOT runtime + wasip1 runner | wasm W4.2 follow-on (surfaced writing `docs/Edge-Deployment.md`) |
| Desktop packaging (Tauri); with it the p2p packaging polish (within-feature DCE pruning, capability-gating) | M3 roadmap item; the one roadmap entry the README carries |

## Tooling

| Item | Source / trigger |
|---|---|
| Profiler: continuous / attach-to-running-`serve` profiling; differential A/B flamegraph compare; column-precise attribution | profiler deferred tail |
| MCP: prompts, semantic/embedding retrieval, long-lived analysis sessions, TCP transport | mcp deferred |
| ~~**bug** — `noeta mcp` gives its tokio workers no `thread_stack_size`, so a debug build overflows the stack on ordinary real-world files~~ **FIXED (2026-07-28)** — two mechanisms. (1) `noeta_parser::parse_in` now asks `stacker::remaining_stack()` and offloads to its deep-stack worker whenever the caller has less than `INLINE_PARSE_HEADROOM` (6 MiB) free, instead of trusting a nesting-depth heuristic documented against a "~2 MiB test thread" — measured, **four** nested `if`s in one function overflow 2 MiB in a debug build while the inline threshold allows sixteen levels, so ordinary code aborted the process. This makes the guarantee independent of the caller, which is the half that removes the guess. (2) All three servers now size their threads explicitly (`noeta_parser::SERVER_STACK_SIZE`, 16 MiB): the MCP and LSP tokio runtimes (which covers `spawn_blocking` too) and the DAP's hand-rolled run worker, which ran the whole compile front end on `std::thread`'s 2 MiB default. Measured: each mechanism alone is sufficient for the MCP case. Plus `NoetaMcp::call_tool` wraps every dispatch in `catch_unwind`, so a panicking tool is a JSON-RPC `INTERNAL_ERROR` naming the tool rather than a request the client waits on forever (rmcp spawns each request as a task, so the panic killed the reply, not the server). Gates: `crates/noeta-cli/tests/cli/mcp.rs` (a real `noeta mcp` process over stdio), a `noeta-parser` 2 MiB-thread test, `noeta-mcp` unit tests | found running `reflect` over para/ai from a debug `noeta` (2026-07-28) |
| ~~**bug** — a legal-but-very-deeply-nested file still aborts the process instead of parsing or diagnosing~~ **FIXED (2026-07-29)** — and the row's own premise was false, which is what changed the shape of the fix. Re-measured by binary-searching the abort depth against the worker's stack size: the cliff is **exactly linear** in `DEEP_PARSE_STACK` (16 MiB → 38 levels, 32 → 78, 64 → 158, 128 → past 255), so "raising the worker's stack does not close it — depth 255 overflows a 1 GiB stack too" is wrong; stack size and depth limit are two knobs on one budget, and the old row moved neither far enough. The **red-zone mechanism is real but explains something else**: a debug build costs ~412 KiB of stack per nesting level, six times `chumsky`'s hard-coded 64 KiB `stacker::maybe_grow` red zone, so `maybe_grow` waves the recursion through with far less left than the next level needs and a fresh 1 MiB segment is overrun after two levels — heap segments cannot rescue a debug parse, which is *why* raw stack is the binding resource. Release frames fit inside the red zone, so a release build survives depth 255 on a **1 MiB** stack; the debug/release gap is the whole bug. Also measured: the old limit of 256 was 55–97 levels beyond what the worker could hold (abort between 159 and 202 depending on shape — worst is nested `fn(){}` values at ~412 KiB/level, ordinary `if` nesting ~330 KiB, bare `[[[…]]]` far less). Fix: the three budgets are now one derivation from the one measured constant `STACK_PER_NESTING_LEVEL` (512 KiB = worst measured, rounded up) — `MAX_NESTING_DEPTH` 256 → **128**, `DEEP_PARSE_STACK` 64 → **256 MiB** (4× what the deepest legal parse needs; reserved address space, committed page by page, one worker per file). The same sweep found a **second overflow window on the inline path**: `INLINE_NESTING_DEPTH` (16) needed ~6.6 MiB while `INLINE_PARSE_HEADROOM` asked for 6, so a caller holding *just over* the headroom passed the check and then overflowed (measured: a 6.2 MiB caller aborts on 15 nested function values; 6.0 MiB is safe only because falling short gets it offloaded) — inline range now **8**, which fits the headroom with half again to spare and costs nothing, since the deepest of this repo's 1128 `.noe` files nests 8. Gates: two `const` assertions keep the budgets one derivation; `the_deepest_legal_parse_fits_its_modeled_budget` parses the worst-known shape at the limit on a thread sized to the model with *no* margin, so a grammar change that makes frames heavier is red long before production's 4× margin is gone; `a_caller_just_over_the_headroom_survives_the_deepest_inline_parse` pins the inline half; conformance `diagnostics/nesting_{at_the_limit,too_deep}.noe` pin the language-visible boundary from both sides, generated by `scripts/gen-nesting-cases.py` | measured while fixing the `noeta mcp` stack overflow (2026-07-28); re-measured and fixed 2026-07-29 |
| ~~**bug** — an `else if` chain overflows the parser stack, with no diagnostic and no protection from `MAX_NESTING_DEPTH`~~ **FIXED (2026-07-30)** — the row's numbers were wrong in both directions and the re-measurement is what settled the "pre-pass or grammar, or both" question: **both**, because each half closes something the other cannot. Re-measured on an 8 MiB main thread: the chain aborts the parser at **725–730** branches, not ~330 (~11.3 KiB/branch, not ~25), and `noeta run` — check, IR lowering and the Core-IR interpreter, all walking the same right-nesting on the *caller's* stack — aborts at **740–800**, so the parser was the first wall by only ~40 branches and flattening the grammar alone would have moved the cliff, not removed it. Fix: (1) the `if` grammar is now **iterative** (`repeated()` + a right fold into the same right-nested AST, spans identical) instead of a `recursive` handle descending once per branch — the parser now takes ≥ 8192 branches; (2) the pre-pass counts **chain length per delimiter depth** and reports E0032 past a limit, since the stages after the parse cannot be offloaded. The limit is derived, not chosen: `MIN_PIPELINE_STACK` (8 MiB, the CLI's main thread — the smallest stack a *whole pipeline* runs on; the servers give themselves 16 MiB) over the measured per-branch cost rounded up ⇒ **512** branches, 1.5× under the measured abort. Counting is per depth with a reset on a bare `if`, so sibling chains never sum (the over-counting that would have rejected ordinary code). **The sweep also found a second shape in the same class that this row did not mention**: the conditional *expression* chain `if c then a else if c then b …` desugars to a nested `match` per branch, costs ~4× as much, and aborted **inside the parser at ~200 branches, non-monotonically** (200 and 210 abort, 215–230 survive, 250 aborts) — the `stacker` red-zone signature, and a cliff that is not monotone in the input cannot be bounded by a limit at all. So a long chain is now **offloaded** to the deep worker (`INLINE_CHAIN_BRANCHES`, the chain analogue of `INLINE_NESTING_DEPTH`; a chain is invisible to the depth test, which is why it never was) and its limit is **128** from the downstream cost. Gates: four `const` assertions keep the derivation one; six parser tests (both boundaries, the AST shape and spans, sibling chains, the offload decision, a parse at the limit on exactly `MIN_PIPELINE_STACK`); two generated conformance pairs (`{else,ternary}_chain_{at_the_limit,too_long}.noe`) pin both boundaries end-to-end, the accepting side *running* because the parser is no longer the binding stage. **Still open in this class** (all far away, all recursion-per-token with no delimiter): `.b()` call chains abort at ~42k, `a.b.c…` at ~50k, `1 + 1 + …` at ~85k, `!!!…` at ~90k — per-token costs of 90–200 bytes, so they need a 200 KB+ file, and the pre-pass now has the shape to price them if one ever shows up | measured while re-measuring the nesting-depth row (2026-07-29); re-measured and fixed 2026-07-30 |
| ~~**bug** — nested `match` parses in exponential time~~ **FIXED (2026-07-30)** — the suspected cause was the right one and it was confirmed by measurement before touching anything: a block-bodied arm `=> { … }` is ambiguous between a map literal and a statement block, and `arm_body` resolved it by parsing the brace body **twice** — once as a map (which fails) and once as a block. Nested one level per arm, that is 2^depth. Confirmed by removing the ambiguity rather than by reading the code: the same nesting with a *parenthesized* arm body (`_ => (…)`) or a bare one (`_ => match …`) parses in 0.3 s at depth 30, while the brace form took 4.5 s at 12, 23 s at 14 and 62 s at 16. Fix is the complexity, not a cap: `.memoized()` on the value-expression alternative (chumsky's `memoization` feature, which caches a rule's *failures* at an offset — exactly the fact "a map literal cannot start here" that every level below re-asks). Depth 16: **62 s → 0.02 s**; the shape is linear to the depth limit. No grammar change and no behaviour change — `=> {}` / `=> {"k": v}` are still map literals, `=> { f(x) }` still a block, pinned by a new test. Memoization is applied to that one rule, not to `sub`: a garden path is a property of the ambiguity, and memoizing the whole expression grammar would cost every parse a hash lookup per node. Also swept the other nested shapes for the same pattern — closures, map literals, mixed match/map arms are all flat in time at depth 40, so this was the grammar's only garden path | measured while re-measuring the nesting-depth row (2026-07-29); fixed 2026-07-30 |
| REPL: JIT at the prompt | repl-on-vm follow-on. Trigger: demand |
| Salsa: a deleted file's **fixed-size input struct** cannot be freed — salsa 0.27 has no public input-delete API (append-only input table; `evict_lru`/revision-GC act only on LRU tracked functions). Mitigated, not fully closed: on deletion `noeta_db::release_source` reclaims all *unbounded* content (input text + downstream `ast`/`Sites`/`Module` memos, overwritten with empty-program equivalents) and `WorkspaceCache` reuses tombstoned slots for the next new file, so the input table is bounded by the *concurrent* file high-water mark. Remaining residual: N distinct files created-then-deleted with no later adds leaves N small empty input slots. Closes if salsa gains real input deletion (upstream) or we switch to an interned-path input keyed by a reusable id. | audit F9 residual a (partial) |
| IDE treats a **resolved bare variant pattern as a local binding**: `DefUse::build` (`noeta-ide/src/resolve.rs`) walks the bare AST with no checker sites, so a `String => …` arm on a `Type` scrutinee — which now resolves to the variant and binds nothing — still registers `String` as a definition in the arm's scope (hover/go-to-def/completion see a phantom local). Cosmetic: any *reference* to the name in the arm body is already E0005, so nothing mis-resolves. Fix needs `Sites::variant_pattern_sites` threaded into the IDE resolve (its API takes only a `Program` today). | variant-patterns arc (2026-07-28) |
| ~~**bug** — **test fixtures at machine-global fixed temp paths, still in six crates**~~ **FIXED (fixture-paths sweep, 2026-07-30) — and the helper promoted out of the crate that first needed it, so there is no fourth sweep to do.** `noeta-pm`'s unit tests each built their fixture at a *fixed name* under the system temp dir (`/tmp/noeta_git_forge_test`, `/tmp/noeta_graph_test_editions`, …) and opened by `remove_dir_all`ing it — so every checkout and every concurrently-running test binary on the machine shared one path and deleted each other's tree mid-setup. **Measured rather than inferred, and worse than the symptom suggested**: six concurrent runs of the pre-fix `noeta-pm` lib test binary failed **8–14 tests per run**, union **24 distinct tests** across `git`, `git_forge`, `graph`, `manifest`, `registry`, `sources` and `store` — not the 8 the report named, which were only the ones that lost the race most often. The panic names the resource outright once the assertion carries git's stderr (`git ["init", …]: fatal: cannot copy … to '/tmp/noeta_git_forge_parse_fail_mixed/host/acme/thing/.git/info/exclude'`); a bare `assert!(out.status.success(), "git {args:?} failed")` is what hid it. Also a *false* claim corrected on the way: these did **not** "fail deterministically in isolation" — each passes alone every time, and the failure *count* rises with load, which is the signature that separates shared mutable state from a regression. **Now one crate builds every test fixture path: `noeta-test-temp`** (dev-dependency-only leaf, like `noeta-alloc-probe`), roots at `<target-dir>/tmp/noeta-tests/p<pid>.<binary>/` — derived from `current_exe()`, since cargo sets `CARGO_TARGET_TMPDIR` only for integration tests and benches, never for the `src/` unit tests where most of these fixtures live — plus a per-call counter, `Drop` cleanup, and dead-pid pruning. `noeta-pm/src/test_temp.rs` is a re-export of it. The new piece is `TempPath` (`TempDir::into_child`), for the **second half of the class**: a helper that builds a guard and returns a `PathBuf` pointing inside it deletes the tree at its own `return`. It derefs to the path, so converting a `-> PathBuf` helper touches no call site. **Every surviving site measured before it was touched, since a latent bug fixed without a reproduction is a guess** — six crates, **25 distinct vulnerable tests**, concurrent runs of one test binary: `noeta-cli` lib 4 tests (6 runs failed 0/2/2/2/2/1), `tests/doc_samples` 5 of 6 runs (`write sample: Os { code: 2, kind: NotFound }` — the fixture deleted between `create_dir_all` and `write`), `noeta-ide` **11** tests (16 runs failed 2/0/1/4/3/2/2/2/2/1/2/3/1/3/1/2 — the enumeration listed 5 *sites*; every consumer of the two fixture helpers was exposed, and 6 runs found only 1 of the 11), `noeta-mcp` 3 tests (16 runs, 10 failing), `noeta-prof` 2 (path-dep test 7 of 16 runs), `noeta-host-real` 3 (12 runs failed 3/3/3/0/3/1/3/1/1/3/1/1). After conversion each was re-run at the same concurrency: **zero failures**, up to 16 concurrent runs. Two leads in the enumeration were wrong in instructive ways. (1) `noeta-prof`'s `fixture` looked *safe* — it only `create_dir_all`s and writes, nothing deletes — but `fs::write` **truncates before it writes**, so a run reading the program inside another run's truncation window profiles an empty file: 24 concurrent runs of three short tests caught `program stdout is forwarded verbatim, left: ""`, i.e. the program ran fine and printed nothing because it *was* nothing. (2) `tests/serve.rs` shares a fixed *file*, and a per-process root covers the file and **misses the actual failure**: 4 concurrent `--ignored` runs failed 3, on the fixed **port** 8231, where the losing server cannot bind and dies while the client reports only `Connection reset by peer`. A fixture root cannot fix a port; `noeta_test_temp::free_port()` (bind `127.0.0.1:0`, read the assignment, release) can, and the seven sibling `#[ignore]`d server tests that pinned 8459/8473/8477/8479/8481/8491/8493 took it too. The already-safe list was accurate and was left alone (`noeta-cache`, `noeta-cli/src/cmd/cache.rs`, `noeta-ide/tests/expansion.rs`, `noeta-conformance/tests/ext_directive_seam.rs`, `noeta-pm/src/git.rs`, and the `noeta-cli` integration fixtures on `CARGO_TARGET_TMPDIR`). Do **not** answer a recurrence with a test mutex or `--test-threads=1`: the sharing is between *processes*, so serializing threads inside one binary cannot touch it, and slowing the suite down to widen the losing window is the opposite of a fix. **Two real bugs fell out of the sweep, both hidden by the fixed paths.** (a) `noeta-host-real::fs_append` **silently lost data**: it wrote through a `tokio::fs::File` and dropped it without flushing, and that type buffers — `write_all` returning `Ok` means only that the bytes reached the buffer. With isolated paths `real_host_disk_round_trip` failed 10 of 12 concurrent runs (`left: "hello disk", right: "hello disk + more"`); one `flush().await` fixes it. Nobody had seen it because `/tmp` is **tmpfs**, where the blocking pool's write lands in the page cache before the next statement can observe anything missing — the fixed path was on a RAM disk, hiding a data-loss bug as well as being one. (b) `hot_serve` and `hot_live` had been **failing on `main` in isolation**, unnoticed: their embedded `.noe` fixtures predate the rule that a named function does not implicitly see a top-level binding, so both died at startup with `E0005: cannot find \`count\` in this scope` — piped to `/dev/null`, leaving only `server did not accept within 4s`. Fixed per the diagnostic's own help (`fn fetch(req: Request) use (count)`), and the L3 showcase is a live gate again. That is what `#[ignore]` costs: nothing runs these unless asked, so they rot. Gates: two clean `cargo test --workspace --no-fail-fast` runs at 168 suites / 2852 passed / 0 failed (a third run was needed: one intervening run lost the two `bench --baseline` tests to the load-induced zero-measurement wart in the row below, which is unrelated to fixtures and was chased down rather than shrugged at), 1087 conformance + full differential agreement, `cargo build --workspace` at 0 warnings, `cargo +1.97.0 clippy --workspace --all-targets -D warnings` and `fmt --check` clean | pm-test-isolation slice (2026-07-29), then the fixture-paths sweep (2026-07-30). Third and final occurrence of this class: the CLI integration fixtures (which once left 169 stray `/tmp/noeta_cli_test_*` directories), `noeta-pm`, then these six crates — each of the first two rolled its own helper, which is why there was a next time. New tests take a fixture from `noeta-test-temp` rather than building a path |
| ~~**bug** — `noeta bench --save-baseline` can persist a baseline of `0.0`, which then compares against nothing, silently~~ **FIXED (2026-07-30)** — the diagnosis in this row was exact; the fix is the one it argued for, in three parts. (1) The `.max(0.0)` clamp is gone: a two-point subtraction that did not resolve (`t(2N) <= t(N)`) is now a **bench failure** with a reason — "measurement did not resolve: … the body may be too fast to time, or the machine too loaded; raise `--iterations`" — because a per-iteration cost of zero is not a measurement of zero, it is the absence of one. (2) `save_bench_baseline` **refuses** to write a non-positive entry (naming the bench) *and* refuses to write a baseline with no entries at all — the same silence by a different route, and it wrote one before. It writes nothing when it refuses, so there is no half-saved baseline. (3) At compare time a `--baseline` that produced no delta says which comparison it could not make and why — a missing entry (a bench added since the baseline was saved) or a stored zero from an older version — in the human line and as `baselineNote` in `--json`, so a delta consumer can tell "unchanged" from "never compared". Swept for other readers of the clamp: there is no profiler or second report reading it, and the only other `.max(0.0)` in the tree is the playground's JS clock. Four unit tests in `cmd/bench.rs` pin all three parts | fixture-paths sweep (2026-07-30), found running the gates; fixed 2026-07-30 |
| **The pid-qualified fixture paths are collision-free but never collected: 10,258 stray directories / 136 MB on `/tmp` right now, ten days' worth.** The sites the fixture-path sweeps called *safe* — `std::env::temp_dir().join(format!("…-{}", std::process::id()))` — are safe from the *collision*, which was the bug being hunted, and solve nothing about accumulation: a pid-named directory is a new name every run and nothing ever removes it. Measured 2026-07-30 on this machine: **10,258** `/tmp/noeta*` directories spanning 2026-07-20 to 2026-07-30, 136 MB, dominated by `noeta-dap` (4,610), `noeta-cache` (3,269), `noeta-cli`'s compose/patch fixtures (1,179 + 227) and `noeta-pm`'s `graph`/`authorship` (637 + 207) — the same shape as the 169 stray `/tmp/noeta_cli_test_*` that opened this whole thread, sixty times over, and invisible because `/tmp` is tmpfs and nothing complains until it fills. The fix is now one line per site: `noeta_test_temp::TempDir`/`unique_path` put fixtures under `<target-dir>/tmp/noeta-tests/`, where a `Drop` removes each tree, a dead-pid prune collects what a killed run left, and `cargo clean` sweeps the rest. Deliberately **not** done in the fixture-paths sweep: those sites were on the do-not-churn list, and the defect is accumulation rather than the (already fixed) cross-process deletion. | fixture-paths sweep (2026-07-30), measured while checking fixture hygiene. Trigger: a `/tmp` full on a dev box or CI runner, or the next slice touching `noeta-dap`/`noeta-cache` tests |
| IDE hover asymmetry for `@validated`: `hover_directive` (token-level hover, `noeta-ide/src/lib.rs`) has no descriptor for `@validated` (and none for `@tier`, which hovers via `hover_tier`), while completion's `decorator_detail` does. Now *explicit* after the `BuiltinDirective` refactor (the `Validated \| Tier => None` arm), so it is a visible decision, not a silent miss. Trigger: decide whether `@validated` (and the other no-hover directives) deserve a token-level hover descriptor; if yes, add the arm — behavior change, hence deferred out of the behavior-preserving refactor. | w8 directive-enum refactor (2026-07-19) |

## Performance

| Item | Source / trigger |
|---|---|
| **Kernels in Noeta** — JIT/AOT loop-shape recognition over packed lists, so `for p in ps { total += p.r }` compiles to the tight buffer loop a native kernel is; demotes the native-kernel ABI to an escape hatch | kernel design discussion (2026-07-09). Trigger: packed bulk math in *user* code as a demonstrated bottleneck |
| **P-MONO** — monomorphic shape specialization (generics are erased-for-storage) + reflection cross-`dyn` element recovery (`List<int>`'s `int` after a `dyn` boundary) | M1.8 tail / perf ledger. Trigger: a workload demonstrating its value |
| `TypeId` interning in `noeta-types` (`Type` is a plain owned tree) | Trigger: a checker profile showing clone cost (the audit's env-borrow work already cut checker allocs ~39%) |
| Inline caches for extern-type methods; borrowed-arg projection for registry dispatch (owned `NativeValue::Str` clones per call) | extern-types follow-ons. Trigger: a hot extern-method workload |
| Zero-alloc map-key probe adapter (`Equivalent<MapKey>` + canonical hash off object slots) + reuse-analysis recycling of per-iteration key temps | packed-keys follow-up. Trigger: keyed-lookup profile |
| VM-throughput next-gap investigation — the disassembly-driven inventory of remaining PHP-gap slices (post-P-VMT) | standing investigation, `scratch-bench/xlang/RESULTS.md` for the current numbers |

## Stdlib follow-ons (all trigger-gated)

| Item | Source / trigger |
|---|---|
| Crypto: blake3; argon2/scrypt (needs the algorithm-tagged verify design); AEAD encryption (AES-GCM / chacha20-poly1305 — only WITH a key-management story) | crypto arc scope cuts |
| HTTP client: per-request timeout; streaming bodies (rides the `ReadSource` model); cookie jar / redirect / retry config; HMAC request-signing helpers | http arc scope cuts |
| ~~**`std.session`** — the stateless signed-cookie codec + `Session` request/response pair~~ ✅ DONE — shipped as S2/S3 of the cookie arc: `keyring`/`encode`/`decode` + `open`/`attach`, verify-before-parse, mandatory `exp`, rotation keyring, 4096-byte hard error, required `secure` argument | cookie arc S1 tail |
| Manifest `optional` / `[suggests]` dependencies — a package declaring a dependency an app opts into. Rings are the wrong layer (native-payload DCE, unknown to `noeta-pm`); `Dependency::Scope` covers the `para/aether` + `para/db` case today, and the diagnostic is the better discoverability surface | http-sessions S4 design question. Trigger: a second use case that `Scope` does not cover |
| ~~**bug** — the subprocess doors abort where every failure is remote-input-shaped~~ **FIXED (2026-07-28)** — the recoverable twins landed as `os.try_spawn(cmd, args?): Result<Process, OsError>` and `Process.try_write(text): Result<void, OsError>`, with the aborting doors kept unchanged (that pair is the language's shape, and removing a half would break scripts that want a missing program to stop the run). `OsError` is a registered `std.os` extern type declaring `Error` + `Display`, with `message()`/`kind()` — `not_found`, `permission_denied`, `broken_pipe`, `stdin_closed`, `other` — classified from the real `io::Error` **at the source**, so the check/write race is reportable as the distinct condition it is instead of a message to sniff. The `Os` seam was re-rooted rather than extended: `os_try_spawn`/`os_proc_try_write_stdin` are the required primitives and the aborting `os_spawn`/`os_proc_write_stdin` are *defaults derived from them*, so the two doors of each pair report the identical message by construction. The sandbox grew the model the differential needs — the scripted `dead` command spawns a child whose stdin is broken from the start, and `close_stdin` genuinely closes — so both `Err` kinds are reachable in-oracle. Measured after: `os.try_spawn("definitely-not-a-real-binary-xyz")` → `Err`, `not_found`, program continues; `try_write` to an exited child → `Err`, `broken_pipe`, program continues; both aborting doors unchanged. `ABI_VERSION` 7 → 8. Conformance: `std/os_try_spawn`, `std/os_try_write`, `std/os_write_dead_aborts`. Docs: `docs/Error-Handling.md` (a new "Aborting and recoverable doors" section naming the whole pattern) | found building para/ai's MCP stdio transport (2026-07-28) |
| Raw TCP/UDP (`std.net`) for non-HTTP protocols — needs a scripted-peer deterministic sandbox model (harder than the HTTP responder) | http-server design question. YAGNI until a concrete protocol |
| CRDTs: last-write-wins register; add/remove OR-Set | p2p arc |
| Kernel bundles: `IVec2`/`IVec3`, `f64` vectors, `Transform` (TRS), `Color` | kernel-methods deferred |
| ~~**`regex.compile` has no recoverable twin.**~~ **FIXED (2026-07-29)** — `regex.try_compile(pattern): Result<Pattern, string>` ships alongside `compile` in `REGEX_FNS`, on the `json.parse`/`json.try_parse` model exactly. The module was **re-rooted** on it rather than merely extended: `regex::try_compile` is the primitive and the aborting `regex::compile` is *derived* from it (`try_compile(p).map_err(|m| StdError { message: format!("`regex.compile`: {m}") })`), the same discipline the `os.try_spawn` row above established and `json.parse` keeps over `json.try_parse` — so the two doors cannot report different things about the same pattern, and the abort text is unchanged byte for byte. The `Err` side is a plain `string`, not a classified extern error: a pattern has exactly one way to be invalid, so a `kind()` would be a constant, and the engine's caret-and-span diagnostic is the whole value (it rides verbatim and unprefixed; the aborting door adds its own name, as `json.parse` does). The dispatch never uses the seam's `Err(StdError)` channel — that channel *is* the abort — returning the whole `Result` inside the `NativeOut`, so both backends materialize one tree. The aborting door is untouched and stays: a script that wants a bad pattern to stop the run should not have to write a `match`. `ABI_VERSION` 10 → 11. Conformance: `std/regex_try_compile` (valid pattern, malformed pattern with the engine's own text, lookaround/backreference through the same door, program continues); `std/regex_invalid_pattern` still pins the aborting half. Rust unit tests pin the derivation itself — the abort message *is* the recoverable message under the prefix. Docs: `docs/Error-Handling.md` (a fourth row in the doors table, plus what decides whether an `Err` side is a type or a string) | para/ai phase 5 (`Redact`), 2026-07. Trigger: a redaction/routing rule loaded from config |
| **`std.tracing` exposes no `current_span`.** `span`, `with_span`, `span_from` and `current_context` are all there (`crates/noeta-stdlib/src/tracing.rs`), but a module handed no span has no way to add an event or an attribute to the one that is open — so "annotate the enclosing span" is not expressible from a library that does not own it. para/ai's guard telemetry wanted a span *event* on the run's `invoke_agent` span (DESIGN §9) and emits a short child span instead: context propagation nests it in the same place and it carries attributes natively, so the divergence is cosmetic, but the event form is the one the OTel conventions are written in. Wanted: `tracing.current_span(): Span`, the read half of the context `current_context` already exposes | para/ai phase 5 (guard telemetry), 2026-07. Trigger: a second library wanting to annotate a span it did not open |
| **The two unrecoverable process doors** (still open, restated with a second consumer): `os.spawn` aborts when the command does not exist and `Process.write` aborts on a broken pipe, which are both ordinary conditions for an MCP client. Wanted: `os.try_spawn` and a `Result`-returning `write`, the same shape `json.try_parse` has next to `json.parse`. para/ai works around both in `mcp.noe` (PATH resolution before spawn, a `try_wait` liveness check before every write) and cannot close the residual race from Noeta | para/ai phase 6, restated phase 5. Trigger: any second consumer spawning an untrusted child |

## Design proposals (no slice yet)

- ~~**Vulnerability-intake trust model**~~ — DECIDED + built (advisory-intake arc, 2026-07-19): three tiers (operator-issued / scope-owner self-service with keyless provenance / OSV-GHSA-RUSTSEC import via operator-curated name map), a public report queue that only becomes an advisory on operator/owner promote, and `noeta watch-scope`. See the advisory-intake residuals row above for what's left.
- **Packed-field-kind enum dedup** — four phase-appropriate `PackedKind` encodings; revisit only if a shared public layout vocabulary earns its keep across the package boundary (the `PackedView` ABI may already be that vocabulary — check before building).
- **WASM revisit conditions** (recorded, not planned): direct wasm codegen only on perf data; wasm-threads isolates only on multi-core edge demand; p2p-in-browser is its own arc.
