[package]
name = "noeta-reactive-abi"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "The contract between Noeta's reactive engine and a foreign source node — the `ReactiveSource` capability an extension implements to participate in the reactive graph."
homepage = "https://noeta.dev"
documentation = "https://docs.noeta.dev"
keywords = ["noeta", "extension", "abi", "plugin"]
categories = ["development-tools::ffi"]

[dependencies]
# The capability trait speaks in NativeCtx re-entries and arena `Retained` cells…
noeta-ext-abi = { workspace = true }
# …over `NodeId` handles into the shared reactive graph. Both are the only vocabulary that crosses
# the seam — the engine's representation and the consumer's node types stay on their own sides.

[lints]
workspace = true
